# VCC v3.2.9 再次审查 — 2026-09-19

结论：**上次 Reader 关闭期间取消仍提交的 P2 已关闭，本轮未发现新的可确认缺陷。** 本轮代码复审没有新增阻塞项；Windows、Excel/WPS 和完整性能验收仍待完成，不能据此宣称整版已具备发布条件。

## 范围与版本

- 分支：`codex/v3.2.9-vcc-fin-op-multisheet-review-export`；HEAD / 本地 main 均为 `2ba9ef14fe972363b604955636cff0c9ac53700f`。功能仍在未提交工作区。
- 依据：[Spec R3](v3.2.9_VCC_Spec.md)、[TechDoc R3](v3.2.9_VCC_TechDoc.md)、[上次审查](review-2026-09-18-rereview.md) 和 [第五轮修复记录](REVIEW5_FIXES.md)。Spec / TechDoc 未变。
- 当前隔离快照：`/private/tmp/vcc329-review-20260919-plt348tk`；对照上次快照：`/private/tmp/vcc329-rereview-r3v4hp8h`。
- 相对上次仅 `system-op-importer.js`、`multisheet-import.test.js` 的实现/测试内容变化；其余变化为修复文档及门禁记录。没有新增其他实现文件。[审查增量](evidence/review-2026-09-19/reviewed-delta.patch)
- 审查聚焦取消检查、真实资源关闭、当前组停止、前组保留、后续工作停止和失败收口；补跑此前主体导出的原样本。没有修改实现、提交、推送或访问真实业务数据库，只新增本审查文档及证据。

## 上次 P2 验证

修复在每个 `await workbook.close()` 完成后复用 `throwIfCancelled()`。取消已经生效时，不再打开下一物理文件或进入同步系统 OP 提交。检查到同步提交之间没有新增异步让出；新引用未形成循环依赖，旧同步入口未变。

主审原样复跑上次异步关闭探针，结果从 `success / snapshots=1` 变为：

```text
close-start       cancelled=false  snapshots=0
cancel-delivered  cancelled=true   snapshots=0
close-complete    cancelled=true   snapshots=0
complete          cancelled=true   snapshots=0
error.code = vcc-import-cancelled
```

取消后日志仍出现一次 `BEGIN IMMEDIATE`，来源是 `failImportBatch()` 的失败记录收口，不能将其误认成有效快照提交，也不能宣称取消路径完全没有数据库 DML。[本次原探针日志](evidence/review-2026-09-19/original-async-cancel-probe.txt)

新增回归确认：

- 仅系统 OP 取消：快照与快照尝试均为 0，`partialCommitted=false`。
- 前一充值组已提交：充值逐字段保留，`partialCommitted=true`；当前系统 OP 不提交，后续通道不读取。
- 多个系统 OP 文件：第一个关闭期间取消，第二个不再打开。
- 最后读取进度同步取消：先完成资源关闭，再返回取消。

独立故障探针还覆盖“无取消”和“关闭期间同时取消”两种场景：在真实关闭返回处注入 `EIO`，原错误对象和错误码均完整传播，没有被新取消检查覆盖；已提交充值保留，系统快照/尝试均为 0，后续类型不执行。[探针](evidence/review-2026-09-19/import-close-error-probe.js) · [日志](evidence/review-2026-09-19/import-close-error-probe.txt)

这些探针使用真实 Reader、SQLite 和异步事件循环；取消标志由受控回调投递，未执行人工 UI 点击或真实 Worker IPC 取消投递。

## 本次验证

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| 多 Sheet、系统 OP、Service、来源追溯及 Review IPC，5 个文件 | 93 PASS / 0 FAIL / 0 SKIP | [focused-tests.txt](evidence/review-2026-09-19/focused-tests.txt) |
| 上次异步关闭取消原探针 | 1 PASS | [original-async-cancel-probe.txt](evidence/review-2026-09-19/original-async-cancel-probe.txt) |
| 1,026 行进度、中段取消、末端取消原探针 | 3 PASS | [original-progress-cancel-probe.txt](evidence/review-2026-09-19/original-progress-cancel-probe.txt) |
| 关闭错误故障探针 | 3 PASS，含两个场景及父级统计 | [import-close-error-probe.txt](evidence/review-2026-09-19/import-close-error-probe.txt) |
| 普通主体、CNH 主体、格式化主体 000123 的真实 handler → Service → Worker 导出 | 3 个样本通过，均为 8 张 Sheet / 7 条附页记录，结果仍为 calculated | [earlier-export-cases.txt](evidence/review-2026-09-19/earlier-export-cases.txt) |

五个相关测试文件的执行命令，在隔离快照中运行：

```sh
GIT_DIR=/Users/pzhong/Desktop/Project/bank-bill-excel-tool/.git node --test --test-concurrency=2 \
  tests/unit/backend/vcc-financial-op/multisheet-import.test.js \
  tests/unit/backend/vcc-financial-op/system-op-importer.test.js \
  tests/unit/main-process/vcc-financial-op-service.test.js \
  tests/unit/main-process/vcc-financial-op-archive-lineage.test.js \
  tests/unit/main-process/vcc-financial-op-review-ipc.test.js
```

第五轮已有完整门禁记录的 65 个源码/测试/脚本 SHA-256 均匹配当前内容，指纹文件与门禁日志 SHA-256 也与摘要相符。已有记录为单元 **7,394 PASS / 3 Windows skip**，集成 **53 个脚本 / 2,488 个断言通过**。本次只核对这些证据，**没有重新执行完整 release-check**，不与本次专项数字相加。

## 保留的验收边界

本轮未重跑专用 Electron/ASAR 探针；此前真实 Electron 记录属于更早版本的审查证据。Windows 安装版、原生 SaveAs/文件占用、Excel/WPS 人工核对、完整 PF01–PF05、生产完整冷/热启动和人工备份恢复仍未执行。

审查结束时原工作区快照文件哈希未变，`git diff --check` 通过。源码/文档指纹、精确范围及结果见 [review-summary.json](evidence/review-2026-09-19/review-summary.json)。本结论限于本轮修复及相关调用链，不构成对未执行平台验收的替代。
