# 复审报告｜v3.2.9 工具箱按行拆分（第三轮）

日期：2026-09-18。**结论：第二轮 R05 已修复并通过复验；本轮未发现新增可确认问题。** 该结论针对本次修复及关联链路，不等于完整发布验收通过。

## 1. 审查边界

| 项目 | 本轮范围 |
|---|---|
| 分支 | `codex/v3.2.9-toolbox-split-by-rows` |
| HEAD / main 基线 | `2ba9ef14fe972363b604955636cff0c9ac53700f` |
| 工作区 | `/Users/pzhong/Desktop/Project/bank-bill-excel-tool/tmp/worktrees/v329-toolbox-split-by-rows` |
| 本轮隔离快照 | `/private/tmp/toolbox-rows-review-r3-f8ouloqk` |
| 对比快照 | 第二轮 `/private/tmp/toolbox-rows-rereview-_cdsv1kq` |
| 本轮代码差异 | 产品源码仅 `src/main.js` 的 rows 发布目标映射一处；新增 `tests/unit/main-process/toolbox-row-split-parent-identity.test.js` |
| 文档依据 | 当前 [Spec](spec.md)、[TechDoc](techdoc.md)、[第二轮审查](review-2026-09-18-r2.md)及[对应修复记录](review-fixes-2026-09-18-r2.md) |
| 快照记录 | 70 个修改或新增文件，含历轮文档和证据；见 [源文件清单](evidence/review-2026-09-18-r3/source-manifest.json) |

功能及修复仍是未提交工作区内容；本轮审查未修改源码、提交、推送或发布。报告交付前已核对被审查工作区与快照记录一致。保留前两轮报告，不覆盖历史结论。

## 2. R05 复验结论

**R05 / P2：冻结的输出父目录身份没有传给 Publisher——已关闭。**

修复位于 `src/main.js:21088–21091`：直接从冻结的 `taskContext.fileEvidence.filePlan.outputs` 构造目标项，将 `filePath` 传为 `targetPath`、`targetParentIdentity` 传为 `expectedTargetParentIdentity`。文件级 `targetSnapshots` 继续保留，没有重新采集当前目录身份替代原确认身份。

已核对传递链：Main rows → `publishToolboxArtifacts` → dispatcher → Worker Publisher → journal/index → 恢复检查。公共 wrapper 对目标对象的展开保留父目录身份，dispatcher/worker 继续透传，Publisher 在产物检查与正式目标变更前复核，正常发布的 journal 保存同一确认身份。

### 本轮实际复验

| 场景 | 结果 |
|---|---|
| 生命周期预检通过后、生成前重命名输出目录并新建同名目录 | 拒绝发布，错误码 `TOOLBOX_PUBLICATION_TARGET_PARENT_CHANGED`；无正式输出，原目录和新目录中的用户文件保留，归档结算不执行 |
| 产物校验完成后、进入 Worker 发布前替换输出父目录 | 识别父目录变化并拒绝；两个目录均无正式输出，归档结算不执行 |
| 父目录不变 | 两份输出成功；回读表头和数据正确；journal 两个条目保存原确认父目录身份，归档结算调用一次 |
| 第二轮原始复现脚本仅调整仓库路径后重跑 | 从第二轮的 `success` 变为 `failed / TOOLBOX_PUBLICATION_TARGET_PARENT_CHANGED` |

三个新增回归包含在本轮单测总数中，不重复累计。测试执行真实 Main prepare/execute 源码、公共 wrapper、生成器和 Worker Publisher；系统对话框、后台调度及归档结算是替身，未启动完整业务 Main 或完整桌面 TaskLifecycle 流程。

证据：[测试日志](evidence/review-2026-09-18-r3/focused-tests.txt)、[上轮 probe 的本轮副本](evidence/review-2026-09-18-r3/parent-identity-probe.js)、[probe 结果](evidence/review-2026-09-18-r3/parent-identity-result.txt)、[本轮 Main 差异](evidence/review-2026-09-18-r3/main.diff)。probe 保留本轮临时快照路径，在其他位置执行需先调整路径。

## 3. 首轮问题状态

| 问题 | 本轮状态 |
|---|---|
| R01：未经确认覆盖新增/替换的目标文件 | 相关 8 项回归本轮重新通过；FilePlan/IPC 修复内容与第二轮一致 |
| R02：结果弹窗裁切 | 第二轮已通过；renderer、样式、UI 验证脚本经 SHA-256 对比未变化，本轮未重复执行 UI |
| R03：勾选后焦点 | 同上，保留第二轮已验证结论，不计为本轮新执行 |
| R04：错误提示文案 | 同上，保留第二轮已验证结论，不计为本轮新执行 |

本轮没有新增 findings，未发现 R05 修复引入的正常发布、原覆盖保护或已覆盖恢复用例回归。

## 4. 验证记录

环境：本机 macOS；集中单测、集成及上轮 probe 使用 Node 24.13.0，另一个 Publisher 恢复 probe 使用 Node 25.8.0。以下均为本轮执行结果。

| 检查 | 结果 | 证据 |
|---|---|---|
| rows、文件覆盖竞态、父目录竞态、Main IPC、IPC 合同、FilePlan、TaskLifecycle、Worker Publisher、归档与发布恢复单测 | **210/210 PASS，0 失败、0 跳过** | [focused-tests.txt](evidence/review-2026-09-18-r3/focused-tests.txt) |
| `scripts/integration/toolbox-roundtrip.js` | **30/30 PASS** | [roundtrip.txt](evidence/review-2026-09-18-r3/roundtrip.txt) |
| 第二轮原 probe | 正确拒绝父目录替换 | [parent-identity-result.txt](evidence/review-2026-09-18-r3/parent-identity-result.txt) |
| Publisher 恢复补充 probe | PASS：父目录变化时拒绝恢复，index 和新目录文件保持不变；恢复原目录对象后正常清理 prepared 任务 | [恢复 probe](evidence/review-2026-09-18-r3/optional-parent-recovery.js)、[结果](evidence/review-2026-09-18-r3/optional-parent-recovery.txt) |
| Main 与新增测试 eslint、Main 语法、`git diff --check` | PASS | [检查命令记录](evidence/review-2026-09-18-r3/checks.txt) |

恢复补充 probe 与 rows 一致，仅提供父目录身份而不设置 `requireTargetParentIdentity`；使用真实文件系统及 Publisher，在 prepared 阶段模拟崩溃。产物是不透明字节，验证恢复身份保护，不代表 XLSX 或完整 Main 验收；不计入 210 项单测。无法可靠提供设备号/inode 的文件系统仍沿共享 helper 的既有处理方式，本轮未验证网络卷等平台边界。

本轮没有重复执行已无改动的 UI、smoke 和 K=999/1000 实际生成容量测试；对应既有记录继续保留，未冒充本轮结果。

## 5. 尚未完成的发布验收

- 完整 `release-check` 本轮未重跑。实施记录中的整轮状态仍为 **FAIL**；此前 Biz OP 超时用例单独通过不替代完整门禁。
- Windows、Excel/WPS 人工打开、网络卷及真实 Main 系统对话框全流程仍未验收。
- 本轮可以关闭 R05 代码问题；发布就绪仍需依据完整门禁及相关平台验收结果判断。
