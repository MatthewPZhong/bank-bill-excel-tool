# Implementation Notes

## Baseline

- Goal/spec: 以用户冻结源包 `background-execution-v3.2.x-codex-implementation-ready-fixed 2/` 为唯一来源建立 E00-F 合同基线，并根据 PR0 审查 finding 在独立 target 目录做可追溯修订；源包保持字节不变。
- Initial plan: 先机械复制冻结源包，再只在 target 内闭合 public Protocol、Policy、Resource、Recovery Control/Result 与启动顺序；随后同步权威文档、Codex 输入、fixtures、validator、报告与包级校验和。
- Done when:
  - 无状态迁移的检查/瞬时失败/结算恢复事件可以合法、幂等、原子地追加，且不能伪造状态迁移。
  - 一次恢复动作涉及多个控制对象时，Main 可显式开启一个外层事务，所有迁移与观察事件加入同一事务且不得各自提交。
  - `TaskRunTransitionV1` 明确只接管恢复相关迁移；常规 prepared/running/final 生命周期仍由既有 TaskLifecycle/ArchiveRepository 管理。
  - Critical Intent 状态图与命令前置状态唯一一致。
  - 校验器包含上述契约的正向检查和负向自测，并能拒绝旧/漂移写法。
  - 自包含目录包的校验、哈希与目录完整性可复现；任何未执行项及原因被显式报告。ZIP 不纳入本 PR，也不创建或提交 target ZIP。

## Reviewer Round1 Goal / Context / Constraints / Done when

- Goal：关闭 recovery idempotency/audit lineage 的 5 个 P1 与 2 个 P2 finding，使同一 exact request 在跨重启、实体后续推进后仍可机器判定并返回 immutable 首次结果。
- Context：当前只修订冻结合同包；生产源码提供 TaskPolicy 与 archive DDL 证据，但不在本任务修改范围。
- Constraints：仅修改当前合同包；不改业务源码、rules、旧顶层 3.2.0～3.2.5 changes、用户源包，不 stage/commit/switch/push；JCS、完整 request envelope 与内部 hash owner 已由 root ratify。
- Done when：machine binding、RFC 8785/JCS、persistent owner、exact input/union/result、每 branch 逐 leaf sensitivity、SHA-256/sha1 gate、physical SQL、PENDING 人工红线 checklist、报告与 checksum 全部自洽；锁定 runtime default + no-write validator、syntax/hygiene/scope 全通过，且无未关闭 BLOCK。

## Reviewer Round2 Goal / Context / Constraints / Done when

- Goal：关闭 requestKey owner 自授权、owner/event equality、Batch failure 输入、binding provenance、result writer domain、schema inventory、raw JCS loss、SHA/Proxy 与 physical SQL mutation 的 round2 findings。
- Context：round1 map 内容保持不变且 JCS digest 已由 root ratify；新增 Batch mark-interrupted 两个 required failure leaf，因此 full-envelope sensitivity 总数必须从 289 演进为 291，不能为保旧计数排除字段。
- Constraints：只修改本合同包并适配已 staged round1 snapshot；不改 index、业务源码、rules、旧 changes 或用户未跟踪源包；人工红线仍为 `PENDING_HUMAN_REVIEW`。
- Done when：20 个 requestKey namespace/tuple、owner/event 五项 composite equality、60 条独立 binding provenance、duplicate-aware raw parser、16/4/20/2 schema inventory、291 leaf、可执行 SQLite/marked SQL mutations、27-gate metadata 自洽及全部最终门禁通过。

## Reviewer Round3 Goal / Context / Constraints / Done when

- Goal：关闭 observation 跨重启 retry identity、result lineage 独立推导、负向 unsafe integer、Hold source collision 与 binding source self-authorization finding。
- Context：Round2 的 291 leaf 已是正确历史基线；四个 observation branch 新增 required `observationAttemptId` 后必须完整计入 295。Manifest snapshot 不能作为运行时自授权 source，因此需最小生产 registry。
- Constraints：合同主体仍只改本包；经 root 精确授权，包外仅可新增 `src/main-process/background-execution/action-task-binding-registry.js`、更新 background-execution index、在真实 TaskPolicyRegistry 构造后由 `src/main.js` 构造/保留 registry，并新增一个对应 unit test。不得改变 TaskPolicy get/list/require shape、IPC、DB、production action enablement、rules、旧 changes 或用户未跟踪文件；不操作 index/git history。
- Done when：生产 source authority 与 52/122/60/52/70/digest/provenance fail-fast、四类 durable observation ordinal、20-request→20-field exact projection、400 field/60 owner mutations、正负 unsafe raw KAT、Hold identical/collision executable simulation、稳定结构化 validator failure、295 leaf、默认/no-write validator、checksum 与全量项目门禁通过；人工红线保持 `PENDING_HUMAN_REVIEW`。

## Reviewer Round4 Goal / Context / Constraints / Done when

- Goal：关闭 caller 可替换 binding authority、TaskPolicy inventory 只校验数量、result mapper 自证，以及 hostile 场景未经过真实 Node API 的 findings。
- Context：Round3 的 52/60 binding 内容与 295 leaf 保持不变；Round4 只收紧 authority ownership、冻结完整 122-key inventory digest，并新增独立 20-branch × 20-field result KAT 与真实 SQLite round-trip，不扩产品 action/IPC/DB。
- Constraints：适配当前 staged snapshot，不操作 index/git history；包外仍仅限 root 已授权的 production registry、background-execution index、Main startup 接线与对应 unit test，`rules/integration-test-policy.md` 只允许既有 runner 机械刷新。人工红线保持 `PENDING_HUMAN_REVIEW`。
- Done when：factory 无 caller binding 注入、module-owned snapshot/private Sets/frozen copies、真实 policy list 单次读取与 exact shape/digest、11 类真实 Node hostile case、20 independent result KAT + 20 baseline/460 mutation SQLite round-trip、default/no-write validator、checksum、targeted/background/lint/smoke/full unit/integration/release-check 全部闭合，且无未关闭 BLOCK。

## Reviewer Round5 Goal / Context / Constraints / Done when

- Goal：关闭 source/local/report 可协调漂移、Main binding fail-fast 时序只靠宽松文本匹配，以及 initializer 接受 prototype/Map/hostile error cause 的 findings。
- Context：Round4 的 binding map、122-key inventory、20-branch result KAT 与 295 leaf 内容保持不变；Round5 增加一份独立、non-generated public Contract Authority v1 revision 1，锁定三项 digest、hard counts 与 source contract version，并以真实 Node/AST 顺序门禁证明 binding freeze 早于 DB/IPC。
- Constraints：适配 staged snapshot，不操作 index/git history或用户未跟踪文件；包外仍只改 root 已授权的 production registry、background-execution index、Main startup 与对应 unit test，`rules/integration-test-policy.md` 仅由既有 runner 机械刷新。人工红线保持 `PENDING_HUMAN_REVIEW`。
- Done when：authority 与 source/local/schema/fixture/manifest/CODEX/E00/Platform/errata/checklist/report machine 同值，两组 coordinated mutation fail closed；registry host 只接受 frozen exact plain API，稳定拒绝 Map/prototype/hostile message getter；TaskPolicy create→binding freeze 严格早于 DB/IPC 且 binding failure 时调用数均为 0；真实 Node hostile/order、default/no-write validator、checksum、targeted/background/lint/smoke/full unit/integration/release-check 全部闭合，且无未关闭 BLOCK。

## Reviewer Round6 Goal / Context / Constraints / Done when

- Goal：关闭 machine JSON duplicate ambiguity、Main startup initializer 不可达/可吞错、`assertPair` hostile coercion，以及 genesis authority 以当前值自证的信任模型 finding。
- Context：当前 Contract Authority 是本 PR 首次引入；`origin/v3.2.0` merge-base `f7c4d5aadc93ed44f8d1b7cf48be7050a826e6e9` 不含 authority 文件，因此 revision 1 只能声明 `genesis=true` 且保持 `PENDING_HUMAN_REVIEW`，不能宣称机器批准或 production enablement。
- Constraints：适配当前 staged snapshot，不操作 index/git history或用户未跟踪文件；包外仍只改 root 已授权的 production registry、background-execution index、Main startup 与对应 unit test，`rules/integration-test-policy.md` 仅允许既有 runner 机械刷新。previous authority 必须来自显式包外路径或 merge-base，禁止从当前 anchor 生成。
- Done when：所有 machine JSON 递归拒绝 root/nested duplicate；version/revision 只接受正整数；validator 明确支持 repo/external previous/genesis 三种受控来源并验证 revision 演进；真实 Main 使用同一 pure startup seam，Program.body direct declaration/call、exact host、唯一 reachability、DB/IPC continuation 顺序与 fail-fast 均由 AST + Node 执行门禁证明；hostile `assertPair` 不发生读取/coercion；default/repo、explicit genesis no-write、checksum、targeted/background/lint/smoke/full unit/integration/release-check 全部闭合，人工红线仍为 `PENDING_HUMAN_REVIEW`。

## Reviewer Round7 Goal / Context / Constraints / Done when

- Goal：关闭 genesis 未纳入 change-control、Main production import/run 可被伪造或变为不可达、Espree 偶然解析、authority v1/version 双重承诺，以及 external previous 审计身份与实际 invocation provenance 漂移。
- Context：当前 authority 仍是本 PR 首次引入的 v1 revision 1，`genesis=true`、`approvalStatus=PENDING_HUMAN_REVIEW`、`mergeReady=false`、`productionEnablementAllowed=false` 均不得由机器门禁改变；Round6 的 production binding/result/inventory 内容及三项 digest 不变。
- Constraints：适配 staged snapshot，不操作 index/git history或用户未跟踪文件；除既授权合同包、production registry/index/Main/对应 unit test 外，root 额外精确授权 `tests/unit/main-startup-contract.test.js` 与 `tests/unit/archive-center-ui-contract.test.js`，仅把两处过时 substring 启动顺序 gate 改为真实 AST seam gate。不得扩 TaskPolicy/IPC/DB/public recovery API 或伪造 HR1-HR6 完成。
- Done when：完整 authority 的 unchanged post-merge、same-revision genesis flip、rev1→rev2 semantic change、unchanged revision bump 四个 KAT 符合预期；external previous 单次读取字节的 resolved path/size/SHA-256 可审计；v1 只允许 revision +1；Main exact require/no-shadow/no-reassign 与 app.whenReady success reachability/顺序 mutants 全 fail closed；Espree 10.4.0 由 wrapper 和 validator 显式锁版/锁路径，staged-only copy 在未设置 NODE_PATH 时可运行；default/no-write/repo/genesis/external、checksum 与项目全量 gates 闭合。

## Reviewer Round8 Goal / Context / Constraints / Done when

- Goal：关闭 CommonJS 隐式 loader 可在真实 registry import 前被重绑定/替换，以及 Git 仓库内显式 genesis 可绕过已存在 previous authority 的两个 P1 finding。
- Context：Round7 的 authority v1 revision 1 仍是当前 PR 首次引入、HR1-HR6 仍为 `PENDING_HUMAN_REVIEW`；生产 Main 的 exact import/run 与 authority transition 主路径不改，只增加 loader 身份和 previous-absent 证据的 fail-closed 证明。
- Constraints：适配当前 staged snapshot，不操作 index/git history或用户未跟踪文件；仍只允许既授权 31 个 tracked 路径，不新增测试文件，不扩 TaskPolicy/IPC/DB/public recovery API，也不把 detached/index-only 验证伪装成 merge evidence。
- Done when：AST 对 exact CommonJS `require` binding 做作用域解析，并拒绝 import 前的 assignment/update/destructuring、`arguments[1]`/alias、target cache 与 module-loader replacement；执行从 Main 原始源码抽取的 exact import statement 并证明它加载真实 production module/export；Git 环境的 explicit genesis 必须先解析 merge-base，previous 存在时稳定返回 `AUTHORITY_GENESIS_PREVIOUS_EXISTS`，非 Git explicit genesis 明确报告 `non-merge-evidence`；协调 mutants、repo/genesis/external/default/no-write、checksum 与全量项目 gates 全部闭合。

## Reviewer Round9 Goal / Context / Constraints / Done when

- Goal：关闭 Main 前 70 行不在真实 loader proof 内，以及 authority Git resolver 可被 ambient repository/object/config 环境重定向的两个 P1 finding。
- Context：Round8 的 Contract Authority v1 revision 1、三项 public digest、52/122/60 binding inventory 与 295 recovery request leaf 全部保持不变；本轮只收紧 bootstrap/validation evidence，HR1-HR6 继续 `PENDING_HUMAN_REVIEW`。
- Constraints：适配当前 staged snapshot，不操作 index/git history或用户未跟踪文件；仍只改 root 已授权 31 个 tracked 路径，不新增 production/test API，不改变 TaskPolicy、IPC、DB、recovery DTO 或 production enablement。所有源码编辑使用 `apply_patch`。
- Done when：真实 registry exact `require` 是 Main 除 directive 外第一个 Program.body statement；unit/validator 从 byte 0 执行完整 import prefix并 fresh-load exact target/export，helper wrapper/前置 side effect 等 mutants fail closed；所有 Git subprocess 走单一 clean-env helper，物理 toplevel/gitDir/commonDir/HEAD 与 Git 输出 exact 一致，普通/linked worktree 均正确、ambient Git controls 与 nested other worktree 不可改证据来源；repo/genesis/external/default/no-write、报告/checksum 与全量项目 gates 闭合。

## Reviewer Round10 Goal / Context / Constraints / Done when

- Goal：关闭 `--no-write-report` 只比 trust disposition、未绑定实际 authority provenance 与 canonical command，导致 published repo report 可在 external identical-authority invocation 中错误复用的唯一 P1 finding。
- Context：Round9 的 authority v1/rev1/genesis/PENDING、三项 public digest、Main startup/Git physical identity、20 recovery request/result/SQL 合同均不变；published `validation-report.json` 的 canonical evidence mode 由 root 裁决固定为 repo/default。
- Constraints：仍只改既授权 31 个 tracked 路径，不操作 index/git history或用户未跟踪文件，不新增 production/test API，不改变 TaskPolicy、IPC、DB、recovery DTO 或 production enablement；HR1-HR6 全部保持 `PENDING_HUMAN_REVIEW`。
- Done when：no-write 对 selected report target 的 complete normalized authority provenance、canonical generation command、reportVersion、validationReadInputs 与 inputHashes 全部 exact 比较；repo→external、external→repo、different base/merge-base、different HEAD/physical identity 协调 mutant 全 fail closed；package report 只由 repo/default 生成并由 repo no-write复验，external/detached 正向证据只使用包外临时 report 与同 provenance no-write；checksum、targeted/background/startup、lint/smoke/unit/integration/release-check/check-vars/scope/hygiene 全部闭合。

## Unknowns Register

| 未知 | 分级 | 当前处理 | 关闭证据 |
| --- | --- | --- | --- |
| 无状态迁移的恢复审计事件是否允许独立写入 | BLOCK → 已关闭 | 允许，但只能通过事务作用域内的 `appendObservationEvent()`；事件类型白名单、`previousState/nextState = null`，禁止修改控制状态 | 平台契约、E00 TechDoc、CODEX 文档与校验器一致 |
| 多个控制对象如何共享一个 SQLite 事务 | BLOCK → 已关闭 | 增加 Main-owned `runInControlTransaction()`，回调获得 `RecoveryControlTransactionV1`；作用域内方法只加入事务，不独立 BEGIN/COMMIT | API 契约、伪代码和校验器一致 |
| RecoveryControlRepository 是否接管所有 TaskRun 状态 | BLOCK → 已关闭 | 只接管中断/恢复相关迁移；常规执行生命周期保持既有所有权 | TaskRunTransitionV1 范围条款 |
| `committed` Critical Intent 是否可转 `recovered` | BLOCK → 已关闭 | 不允许；保持 `prepared -> acked -> committed -> closed` 与 `prepared/acked -> recovered -> closed`，已 committed 的恢复结果记录在 Task/Hold/Event，不倒改 intent 语义 | 状态图与 `mark-recovered` 前置状态一致 |
| 本机能否复现锁定版本 `jsonschema==4.26.0` 的权威校验 | PROBE → 已关闭 | 在 `/private/tmp` 隔离 venv 安装锁定依赖并运行，不污染项目 | 当前 Validator repo default/no-write 与包外临时 report 的 explicit genesis/external default→same-provenance no-write 均 `29/29 PASS`，runtime check 记录 4.26.0 |
| `archive_task_runs.task_key` 是否等于 canonical actionKey | BLOCK → 已关闭 | 源码证据显示它是 legacy TaskLifecycle key；生产 `ActionTaskBindingRegistry` 从真实 TaskPolicy inventory fail-fast，command 同时携带双身份；Action Manifest v3 只作审计 snapshot | 122-key production inventory、60 exact pairs、source digest/provenance 与 adapter mutations |
| Windows directory fsync 是否具备可证明 durability | PROBE → 未关闭 | 合同要求必须尝试；仅明确 unsupported 可记录 capability，失败保持 source open 并建 hold | target-post-image 资金 action production=false，等待 packaged probe |
| Batch overlay event 如何取得 canonical action identity | BLOCK → 已关闭 | 四个 Batch command exact 携带 actionKey/expectedTaskKey/operationKey；adapter 验证 binding，Repository CAS Task/Batch identity | E00 exact union、跨文档摘要与 validator mutations |
| eventId replay/conflict 能否跨进程重启判定 | BLOCK → 已关闭 | persistent request owner 保存 stable requestKey/eventId/createdAt/完整 RFC8785-JCS request/hash；event 关联 owner，先按 requestKey 加载 owner 并验证五项 equality，再 replay 或 CAS | owner/event DDL、shared JCS KAT、A→B→restart→replay A result contract |
| exact replay 在实体后续推进后应返回什么 | BLOCK → 已关闭 | 两个 result DTO 严格等于首次 immutable persisted event projection，不含 replay/current state | RecoveryControl schema/result fixtures 与 result-hash cross-check |
| `batchId` 对应哪一个物理列 | BLOCK → 已关闭 | 源码 DDL 证明 `archive_batches` 主键为 `id`，`batch_id` 只存在于 overlay/child 表 | E00 §9.2.1 exact join/CAS/changes=1 与 SQL mutations |
| canonical JSON 算法和运行时值域 | BLOCK → 已关闭 | root ratify RFC 8785/JCS；Node duplicate-aware raw/runtime implementation + Python/Node shared KAT | 5 fixed digest KAT、15 runtime rejections、9 raw vectors（含正负 unsafe）、Proxy/positive-only guard mutations 与4处实际 SHA-1 injection |
| requestKey 如何同时稳定复用并使 changed request conflict | BLOCK → 已关闭 | 20 个 branch 按 writer/command/eventType namespace + durable entity/attempt tuple 计算；RFC8785-JCS array 禁止 delimiter concatenation；Hold create-or-get 只用持久 UNIQUE source pair；eventId/createdAt/safePayload 等 volatile leaf 不入 key但全部进入 request hash | fixture `requestKeyContract`、20 个派生 KAT、Hold holdId same-key conflict、A→B→restart→replay A wrong-key/conflict simulation |
| owner 与 event 的 eventId/hash/时间是否可由 DDL 强制相等 | BLOCK → 已关闭 | owner 增加五列 composite UNIQUE，event 使用同五列 composite FK；不能只引用 request_key | SQLite in-memory 对五项逐一 mismatch 全部 IntegrityError，正确 INSERT + prepared→committed changes=1 |
| 60 pair 是否由 map 自身循环授权 | BLOCK → 已关闭 | reverse index 只从 60 条 spec/call-site/TaskPolicy provenance 构造；map 固定 JCS digest 与硬计数 | bank-bu pair swap 与 acquiring second-pair deletion mutants 均 fail |
| authority 合并后仍保留 genesis rev1 是否应失败 | BLOCK → 已关闭 | 完整 authority 未变化时允许 previous/current 都保持 `genesis=true`、v1/rev1；`genesis` 自身纳入受控 payload，任何 same-revision flip 必须失败 | 四个 full-authority transition KAT，含 post-merge unchanged PASS 与 same-revision flip FAIL |
| v1 authority 是否已经定义跨 contractVersion 轮换 | BLOCK → 已关闭 | 没有 v2 文件选择、previous lookup 或人工 redline 迁移合同；本 PR 仅冻结 `recovery-contract-authority.v1.json` 内 revision 精确 +1 | authority machine field、validator transition、全部 normative/docs 统一删除 contractVersion+1 承诺 |
| Espree 是否由偶然 NODE_PATH/全仓依赖解析 | PROBE → 已关闭 | 锁定 10.4.0；默认只接受 repo `node_modules` 的 resolved module，staged-only copy 必须显式传入已验证绝对 module path | runner dependency preflight、validator stable dependency error 与 NODE_PATH unset staged-only gate |
| legacy startup test 为什么需要两行 production 注释 | PROBE → 已关闭 | `tests/unit/main-startup-contract.test.js` 与 `archive-center-ui-contract.test.js` 各有 raw substring；经 root 精确扩展授权，两者均改为解析真实 initializer continuation 与 awaited run/window 顺序 | 删除注释后两份 focused test 均通过；fake import/shadow/conditional/swallowed/early-return mutants 由同一 AST contract 拒绝 |
| 不加载 Electron Main 全部副作用时，如何证明 exact registry import 使用真实 CommonJS loader/export | PROBE → 已关闭 | AST 锁定 CommonJS wrapper `require` 与 `arguments[1]` 的 scope/alias/cache/module-loader 无修改；再用 package-private `Module` 实例执行 Main 源码中的原始 import statement，spy 实际 request 且对比真实 module export identity | 不增加 production seam/API；loader 重绑定、destructuring、arguments alias、cache alias 与 `_load` mutants 均 fail closed |
| 非 Git staged/index-only 副本如何执行首次引入 genesis，又不冒充 merge evidence | BLOCK → 已关闭 | Git worktree 内 explicit genesis 仍强制解析 merge-base 并证明 previous 缺失；previous 已存在稳定拒绝。非 Git 环境只允许显式 genesis，并把 provenance 标为 `detached-genesis-non-merge-evidence`/`mergeEvidence=false` | post-merge explicit genesis、repo controlled revision、true first-introduction 与 detached evidence KAT；生成报告保留实际 mode/command/evidence class |
| Main 首个 registry import 之前的源码是否全部受 loader proof 覆盖 | PROBE → 已关闭 | exact require 移到除 directive 外第一个 Program.body statement；结构 gate 禁止任何前置可执行 statement/side effect/helper wrapper，runtime probe 从 byte 0 执行完整 prefix并 fresh-load真实 target | production source、两份 startup unit AST、21 个 validator startup mutant 与完整 prefix/export identity proof |
| Git previous authority 是否可能被 ambient repo/object/config controls 重定向 | BLOCK → 已关闭 | 所有 Git subprocess 只经一个 helper，先删除全部 inherited `GIT_*` 再加入 owned no-replace/config policy；Git 返回值必须与物理 `.git` marker、commondir、HEAD/ref exact 匹配 | 12 个 Git/genesis KAT 覆盖 ambient GIT_DIR/worktree/object/alternate/config、nested repo、ordinary/linked physical identity与 clean env |
| published repo report 能否被不同 authority invocation 的 no-write 复用 | PROBE → 已关闭 | 原始 external-identical reproduction 实际 `29/29 PASS`，证明旧 gate 只核部分 disposition；现改为 selected report target 的完整规范化 provenance、canonical generation command 与 input hash evidence exact 比较 | 5 个对称 provenance KAT；published repo report 对 external/detached 预期 FAIL，external 正向以包外临时 report 同 provenance default→no-write PASS |

## Decisions

| 决定 | 原因与证据 | 放弃的方案 | 影响 |
| --- | --- | --- | --- |
| 将恢复写入口拆为“仓储事务入口 + 事务作用域写方法”两层 | 既要维持单一状态写入口，又要支持一个恢复动作跨 Task/Batch/Intent/Hold 原子收口 | 顶层暴露多个可独立提交的 repository 方法 | Main 明确拥有事务边界；实现不得在作用域内隐式提交 |
| 观察事件采用封闭白名单，且状态前后值必须为 `null` | 检查完成和瞬时检查失败是事实记录，不是状态迁移 | 用同态 `state -> state` 或虚构状态迁移写审计 | 审计语义可验证，避免状态机被噪声污染 |
| 从 `mark-recovered` 移除 `committed` 前置状态 | 与既有 Critical Intent 状态图一致；committed 已表达外部副作用确认 | 新增 `committed -> recovered` 回退边 | 避免已确认事实被恢复动作重解释 |
| 闭环报告归类为 evidence，不列作 normative contract | 报告用于证明修订，不应覆盖 Spec/TechDoc/Schema 的优先级 | 继续称其为 `newNormativeFiles` | Manifest 治理语义更清晰 |
| 冻结 `RECOVERY_TRANSITION_EVENT_MAP_V1`，Batch 使用判别式 command union | transition eventType 由 Repository 推导时必须有唯一映射；原 `upsert` 类型允许同态与跳跃状态 | 让实现者自行命名 eventType，并仅在运行时检查宽泛 upsert | Validator 可逐 command 比对；Batch 只允许 `absent → interrupted → recovering → resolved` |
| Hold create-or-get 使用 persistent request owner | 仅写“按 source 派生 eventId”没有冻结 createdAt/full request，重启仍可能生成不同 hash | 每次扫描重新派生 holdId/eventId 或创建时间 | requestKey 精确使用 Hold 表 UNIQUE sourceKind/sourceRef，首次保存 holdId/eventId/createdAt/request_jcs/hash；重扫复用，draft 任一 leaf 变化 conflict |
| Recovery event 固化 action/operation/task/source 血缘，Provider recovery 固化幂等键 | Publisher journal 等 source 可能没有 Intent/Hold；只把身份放 JSON 不利于审计，provider outcome 落库前 crash 会重复调用 | 仅依赖 safe payload 和实现惯例 | 事件可按操作/source 查询；重复 recovery 不得造成二次 publish/mutation |
| Batch 首次 interruption 把基础兼容 failed、overlay interrupted 与 event 视为一个逻辑 transition | 三项分别提交会出现旧查询与新查询对同一 Batch 给出不可解释的中间状态 | 允许既有 TaskLifecycle 先写基础 failed、再补 overlay | `batch-overlay.mark-interrupted` 同事务完成三项；恢复只 resolve overlay，基础 interruption 历史保留 |
| Protocol v1 冻结 exact context/wrapper、双 scope `last+1` 与统一 262144-byte ceiling | Supervisor 不能猜 payload；多字节消息必须按 UTF-8 bytes fail closed | 只用开放 object 或业务 action 自行决定首版 ceiling | 52 policies 都要求 protocolLimits；fixtures 覆盖全 Job operation、gap/duplicate/backtrack/old generation |
| 新增 pure-compute canary，保留 durable canary | E00-F 纯计算探针与 E02-C2 durable recovery 的依赖/commit 语义不同 | 复用同一 actionKey 随阶段改变含义 | Registry 52 policies，版本表 59 行；八个 platform:* 明确是 scenario labels |
| Recovery result exact keys 进入 RecoverySource Schema `$defs` | 单一机器权威可同时约束 Inspector/Provider identity/hash/outcome | 在 Lifecycle/TechDoc 再定义第二套 DTO | 正负 fixtures 与 mutation 校验 canonical hash、UTF-8 bytes、identity mismatch |
| Registry startup 必须 register 全量后 freeze | 恢复扫描前需证明 static key 完整且禁止运行时漂移 | 边 initialize 模块边动态注册 | Main DB init → register/freeze → source/hold scan → owner initialize/cleanup |
| Service command/event direction 各自维护 seq | 权威 scope 已包含 direction；跨方向 exchange 应由 control/resource identity 关联 | reply 复制或校验对向 seq | revoke 交错正例证明 grant/adopt/release/close reply 可使用不同 seq，同时各 direction 仍严格 last+1 |
| PreparedIntentInput coordinationKind 使用持久枚举 | SQLite CHECK 与 policy 派生已冻结 `worker-critical/main-owned-settlement` | `worker-handshake/main-owned` 这组协议层误名 | input、SQL CHECK、policy derivation 三方结构检查与 3 个 mutation 一致 |
| request hash 使用完整 exact request、RFC 8785/JCS 且按 writer 分域 | same canonical request 才能 exact replay；createdAt 或任一 evidence/transition 变化必须 conflict | Python sort_keys、只 hash safePayload、排除 createdAt、接受 caller requestHash | Repository 内部 lowercase SHA-256；persistent owner 保存 request_jcs/hash；逐 leaf sensitivity |
| Batch canonical identity 不持久进 legacy Task/Batch key | 现有 `task_key` 是 legacy TaskLifecycle key，改写会破坏兼容；canonical actionKey 由 adapter binding 验证 | 把 actionKey 写回 task_key，或从 sourceRef/payload 推导 | command 同时携带 canonical/legacy identity；Repository CAS 旧 key 且 event 写 canonical key |
| RecoveryControl exact shape 新增独立 JSON Schema | reviewer 实测额外 state/eventType/identity 仍通过旧 24 gates；仅解析 TypeScript 不能成为 runtime authority | 继续只把 E00 TechDoc 当机器 shape authority | schema 对所有层级 `additionalProperties:false`；16+4+20+2 inventory、128 mutation assertions与327 required deletions |
| Result 采用 immutable event projection 而不新增 result_json | append-only event 行含所需完整投影；新增 attempt 后为 20 fields，足以重放首次结果 | 从当前实体状态重建 result，或额外持久重复 result_json | 独立冻结 20-branch full-result KAT；mapper 从 request/CAS/writer 生成 candidate，经真实 SQLite insert/SELECT 后逐字段比对；400 field/60 owner mutations 不以 candidate 自证 |
| observation 相同 source 可发生多次真实 retry，requestKey 如何既 replay 又 append | BLOCK → 已关闭 | owner reserve 前按不含 volatile leaf 的 durable scope 原子分配持久正安全整数 ordinal；同 ordinal replay，下一 ordinal append | `observationAttemptContract`、prepared/bound/committed fixture、两次 transient executable scenario 与 attempt/event composite FK |
| binding map 能否由未消费的合同 JSON 自授权 | BLOCK → 已关闭 | production `ActionTaskBindingRegistry` 是唯一 runtime pair source，独立 Contract Authority 是 public digest/count/version anchor；Main 启动以真实 TaskPolicyRegistry frozen exact host 构造并 fail-fast，单次读取后持有 snapshot/private Sets；manifest 只取 source snapshot + 独立 provenance | targeted unit 13/13、完整 122-key digest、14 类真实 Node hostile/order API、swap/delete/coordinated mutations |
| Hold 同 source pair 多行是否可任取 first row | BLOCK → 已关闭 | 仅完整 actionKey/operationKey/taskRunId owner tuple 相同可 dedupe inspect-once；任一不同为 unknown+Hold，Inspector/Provider=0 | identical/collision executable scenarios 与 first-row-continue mutant |
| 当前 authority revision 1 的 previous authority 从何取得 | BLOCK → 已关闭 | repo 模式只读取 `git merge-base HEAD <base-ref>` 后的 `git show`；显式 previous 只能是当前合同包外路径。base 无文件时仅允许 exact genesis revision 1 + `PENDING_HUMAN_REVIEW` | 当前 merge-base 无 authority；Round6 genesis gate 不把 current anchor 作为 previous，也不宣称 merge-ready/production-enable |
| Main 中 source-order initializer 是否必然可达且错误不可吞 | PROBE → 已关闭 | 生产 Main 直接使用 exported pure startup seam；AST 锁定 Program.body direct exact declaration/call，Node 执行 success/failure continuation spies | `if(false)`、early-return、swallowed try/catch、duplicate/unused call mutants 与真实 throw path |
| machine JSON 的重复键是否会被 Python last-wins 隐藏 | PROBE → 已关闭 | 所有权威/manifest/schema/fixtures/report 经同一 recursive `object_pairs_hook` loader；root/nested duplicate 输出稳定结构化错误码 | duplicate revision、nested digest/count mutants |
| Batch/Task physical SQL 与 row-count gate 进入合同 | 文字 CAS 未冻结 `archive_batches.id`、完整 predicate 与 0-row 行为 | 让实现按字段名猜测或把 overlay.batch_id 混入基础表 | exact join/update/result SQL；每个 write `changes()===1`，任一失败 outer rollback |
| requestKey 与 exact request hash 分离 | key 要在重扫/重启稳定，而 exact hash 必须对任一 request leaf 敏感；把 volatile leaf 放进 key 会把 conflict 误变成新请求 | 用 eventId/hash 本身作 key，或只按 source/scan 猜测 | tuple 固定 durable identity；changed request 同 key、不同 hash，Repository 在 CAS 前 conflict |
| Result DTO 按 writer/domain 收窄但仍复用 event projection | append-only event 的20列足够返回 immutable result，但通用 projection 允许 transition/observation 交叉 | 额外存 result_json，或保留通用 writer/event enum | transition 与 observation schema 各自 const writer/event domain；attempt null/positive 分域、observation null states/non-manual source；cross-DTO reject |
| 生产 source 与生成报告之上增加独立 Contract Authority | source/local/unit/manifest/report 可以被一次协调修改而共同自证；Reviewer 要求 public contract 值不能由 runtime 或 report 反向生成 | 继续把 production source constant 或 `validation-report.json` 当 public anchor | `recovery-contract-authority.v1.json` 独立锁定 binding/result/inventory digest、counts 与 source version；v1 任一受控 value 变化必须精确提升 revision +1 并保持人工 redline PENDING |
| TaskPolicy host 与 Main fail-fast 顺序采用真实 Node/AST seam | `includes`/宽松 regex 不能阻止把 binding 初始化移动到 DB/IPC 之后；prototype/Map 与 hostile error accessor 可旁路普通 shape/catch | 只核 source SHA、函数名存在或把不可信 `error.message` 透传 | host 必须是 frozen exact plain own `{list}`；catch 使用固定 code/message/cause-safe detail；AST 唯一计数与 source-order simulation 证明 binding throw 时 DB/IPC=0 |
| authority change-control 只信 external previous 或 merge-base | Round5 的 current-anchor 内存比较无法证明相同 revision 的全协调改写；本 PR merge-base 没有 authority | 用 current report/checksum/anchor 自比，或静默把 current 当 previous | genesis 只允许 v1/rev1/PENDING；future semantic change 精确 +1，technical PASS 永不替代人工批准，报告固定 mergeReady/production=false |
| Main startup 改用 production pure seam | source-order 不能证明 conditional/try/function 中的调用真实可达，也不能证明 DB/IPC continuation 由同一入口拥有 | 继续从 AST 排序任意 call node或在测试中模拟步骤 | Program.body direct exact initializer 立即冻结 binding；唯一 awaited `run()` 执行 DB→IPC，真实 success/failure spies 与 11 类 initializer/run/import mutants 共用 production API |
| 所有 machine JSON 共用递归 duplicate-safe loader | Python `json.loads` 默认 last-wins，可让 root revision 或 nested digest/count 被覆盖 | 只在 JCS raw request parser 检查 duplicate | authority/manifest/schema/fixtures/report 全部 `object_pairs_hook` reject；错误码稳定，version/revision exact positive integer |
| Contract Authority v1 只采用 revision 演进 | 包内只有 versioned v1 authority 文件，没有 v2 authority selector、跨版本 previous 解析或独立批准流程；继续写 contractVersion+1 会形成不可执行的第二真相 | 本轮顺带设计 authority v2 rotation 与动态文件选择 | v1 的 contractVersion 固定为 1；任一受控 payload 变化必须 revision 精确 +1，且 genesis 只能在首个后续变化中 true→false；未来 v2 需独立合同与人工 redline |
| external previous 身份以一次 open/read 得到的 bytes 为准 | 先 parse 后再 stat/hash 会让路径替换产生 TOCTOU 审计错配 | 分别调用 `read_text`、`stat`、`sha256_file` | 同一 raw bytes 同时用于 parse、size 与 SHA-256；报告记录 resolved path，optional expected digest fail closed |
| CommonJS loader identity 采用 import 前结构约束加真实 statement 执行证明 | 锁定 imported identifier 不能阻止先重绑 wrapper `require`、`arguments[1]`、cache 或 `_load` 后再完成同形 require；直接加载完整 Electron Main 会触发无关副作用 | 枚举任意 JavaScript 改写，或给 production Main 增加测试专用 loader 注入 API | AST 锁定 bootstrap prefix 的 wrapper binding/alias/mutation；package-private `Module` probe 只执行从 Main 源码抽取的 exact import statement并比较真实 export identity，不扩 production API |
| explicit genesis 在 Git 与 detached 环境分域 | `--authority-mode genesis` 若直接返回 previous=None，会在已合并基线上绕过 revision gate；但 tracked/index-only 导出没有 `.git`，仍需可复现校验 | 禁止所有非 Git 校验，或把 detached PASS 伪装为 merge-base previous-absent | Git worktree 必须解析 merge-base并证明 previous 缺失；已存在稳定报 `AUTHORITY_GENESIS_PREVIOUS_EXISTS`。非 Git只产生 `detached-genesis-non-merge-evidence`/`mergeEvidence=false` |
| Main loader proof 采用首语句结构约束与完整源码前缀执行 | Round8 从第 71 行抽取单条 statement，无法证明之前的 module side effect/helper 没有重绑 loader | 继续枚举有限 assignment/cache 黑名单，或加载完整 Electron Main | exact require 是首个 non-directive statement；probe 从 byte 0 执行至其结尾、清缓存 fresh-load target并比对真实 export，不新增 production seam |
| Git authority resolver 绑定物理 workspace，而非仅信 subprocess | inherited `GIT_DIR`/object/alternate/config 可让相同 cwd 读取另一个 repository；symbolic HEAD 也可能在多次命令之间漂移 | 只传 `git -C`、只检查 toplevel，或把任意 Git 失败降级为 genesis | 单一 clean-env helper；filesystem marker/ref 给出 expected identity，单次 rev-parse同时返回 toplevel/gitDir/commonDir/HEAD并 exact 比较，merge-base使用已捕获 OID；linked worktree保留 distinct dirs |
| published report 与 invocation provenance 采用 selected-target exact binding | 同一 authority 内容并不等于同一审计来源；旧 trust projection 丢失 mode/base/head/external identity，`--no-write-report` 可复用错误报告 | 只比较 genesis/PENDING disposition，或规范化时删除绝对路径、digest、mode | report v17 完整保留并规范化 source/transition 字段；canonical command 去除复验专用 no-write flag但保留 mode/base/external path/digest/custom report；package report repo-only，所有 custom report target 强制位于包外 |

## Assumptions

| 假设 | 依据 | 失效影响 | 验证与回滚 |
| --- | --- | --- | --- |
| 除 root 精确授权的 binding authority/startup/unit test 四处外，本轮不扩 production 源码范围 | Round3 证明未被 runtime 消费的 manifest 会自授权，root 随后明确授予最小 source ownership | 若扩到 adapter/DB/IPC 将越过合同勘误范围 | git scope guard；TaskPolicy/IPC/DB/production enablement 不变；check-vars Important-skeleton review |
| `inspection-completed`、`inspection-failed-transient`、`settlement-resumed`、`settlement-failed-transient` 是唯一 observation-only 事件 | 生命周期映射中它们不对应控制状态迁移；Inspector 与 Provider transient failure 必须可区分 | 漏掉或混记观察事件会使故障阶段不可审计 | 校验器冻结白名单；sourceKind 使用不含 manual 的 RecoverySource enum；新增类型必须先改权威契约和 validator |

## Deviations

| 原计划 | 实际方案 | 原因 | 影响 | Spec 已同步 |
| --- | --- | --- | --- | --- |
| 原计划只修复评审已指出的 observation 与外层事务 API | 盲区复核后同时冻结 command→event 映射、Batch 邻接与 Hold create-or-get 幂等 | 否则“eventType 由 Repository 推导”仍不可实现，且 `upsert` 可制造同态/跳跃状态 | 不改变业务结果，增加机器可验证约束 | 是 |
| 初始审查指令曾把 persisted task_key 与 actionKey 视为相同 | 源码证据后改为 canonical actionKey + legacy expectedTaskKey 双身份 | 真实 key 示例不同，等价会破坏旧 Task 并污染 event 血缘 | Adapter 校验 binding；Repository CAS task_key/operation_key 且禁止改写旧 key | 是 |
| 首轮 Service sequence fixture 的 request/reply seq 数值恰好相同 | 增加 revoke 导致双向计数交错的正例，并删除四类跨 direction equality guard | direction 已在 scope 中，数值相同只是样例巧合而非合同 | 不削弱单向连续性；新增 event-direction gap/duplicate/backtrack 与双向 old-generation 负例 | 是 |
| 冻结源包原有 60 文件 | target 增加 2 个 recovery result fixture，形成 62 文件审查性修订包 | 新 public result contract 必须有独立正负 fixture | checksum 与 validator input 统计按 target 实际集合重建 | 是 |
| C1 实施前 baseline 曾宣称 Batch/eventId 合同可直接编码 | 新增 E00-G 合同勘误：补 Batch exact identity、持久 request hash 与跨重启 replay 顺序 | preflight 证明旧 command/DDL 无法满足自身 event lineage/idempotency 条款 | 不扩产品接口；只修合同、validator、evidence 与生成物 | 是 |
| Round0 只持久 event.request_hash 且用含糊 canonical sorter | Round1 增加 persistent request owner、RFC 8785/JCS 与 immutable result projection | request_hash 单列不能稳定拥有 eventId/createdAt，也不能定义 restart result；旧 serializer 跨语言不等价 | 首次 reserve 独立持久，writer commit 关联 owner；shared KAT 锁定 bytes/digest | 是 |
| Round0 actionKey↔expectedTaskKey 仅有文字 | Round1 先以 Action Manifest v2 增加 TaskPolicy inventory/source hash 与 allowedLegacyTaskKeys 审计 snapshot；Round3 已由 production `ActionTaskBindingRegistry` 取代其 authority 地位 | Policy Schema additionalProperties=false，不能临时扩字段；未被 runtime 消费的 manifest 不能自授权，生产也不存在 reviewer 指出的伪 key | manifest 仅作 snapshot/provenance；adapter 必须注入 production registry 并 fail closed | 是 |
| Round0 validator 只解析少数 Batch 字段与 7 个 hash samples | 新增 exact RecoveryControl Schema、每 branch mutations 与 full leaf sensitivity | reviewer 三类 caller 字段 mutation 仍 24/24 PASS | 27 gates；20 branch/291 leaf、16 optional-state、122 mutation assertions、321 required deletions | 是 |
| Round1 sensitivity 总数 289 | Round2 Batch mark-interrupted exact DTO 新增 required failureCode/failureMessage 后改为 291 | 两字段是阻断 safePayload 猜测的正式输入，必须参与完整 envelope hash | +2 仅来自该 branch 两个 required leaf；manifest/report/docs 同步，未排除字段 | 是 |
| Round2 sensitivity 总数 291 | Round3 四个 observation branch 各新增 required `observationAttemptId` 后改为 295 | 同 source 的多次真实 observation 需要持久 ordinal 区分，同时 same ordinal 仍可跨重启 replay | +4 精确来自四个 branch；manifest/report/docs 同步，未排除字段 | 是 |
| Round3 初始仍限定合同包 | 经 root 授权增加最小 production registry、index export、Main 启动 fail-fast 与单元测试 | 未消费的 JSON map 无法成为真实 source authority，Reviewer 判定为 self-authorization | 不改 TaskPolicy public shape、IPC、DB 或 enablement；implementation adapter 后续必须注入同一 registry | 是 |
| Round3 原 scope 不允许 rules 手改 | 全量 integration runner 按仓库固定机制自动刷新 `rules/integration-test-policy.md` §七时间戳/耗时，并在发生后立即报告 root 后保留 | runner 只在 49 个脚本全 PASS 后原地生成该章节，root 已预先授权这一机械例外 | 不改变规则语义、脚本清单或断言数；仅刷新本次 2419/2419 PASS 证据 | 是（机械生成） |
| Round0 无物理 Batch SQL 与人工资损复核入口 | 冻结 `archive_batches.id` join/CAS/result SQL，并新增 PENDING checklist | overlay.batch_id 易与基础主键混淆；机器测试不能替代资金红线人工复核 | SQL mutation gate + 脱敏 control DB 人工 evidence，未伪造完成状态 | 是 |
| Round3 registry factory 仍可注入 `options.bindings`，result expected 仍由 mapper 路径派生 | Round4 改为 module-private binding literal + caller replacement 禁止 + owned inventory snapshot，并新增独立 full-result KAT | 可注入 authority 与 candidate/expected 共源都无法证明 fail-closed 与 lineage 独立性 | public TaskPolicy/IPC/DB/action enablement 不变；测试与 validator 改走真实 Node/SQLite 路径 | 是 |
| Round4 的 source constant、fixture KAT 与生成报告仍可能被协调同步 | Round5 新增独立、non-generated `recovery-contract-authority.v1.json`，并加入 source-binding 与 branch-mapper 两类 coordinated mutants | 任一单点 digest gate只能发现意外漂移，不能阻断有意同步修改所有派生证据 | v1 public authority value 变化必须显式 bump revision +1 并重新人工 redline；report/checksum 不能替代 anchor | 是 |
| Round4 Main gate只证明 symbol/call 存在，initializer 仍接受 prototype host并可读取 hostile message | Round5 将 production host收窄为 frozen exact plain own API，错误固定化，并以 espree AST + fail-fast spies 执行真实顺序测试 | 继续依赖 regex/includes 或读取 caller error details | 不改变 TaskPolicy public API、DB/IPC 或 action enablement；仅收紧 startup invariant 与错误边界 | 是 |
| Round5 把 current authority 自身当 change-control 比较基准 | Round6 增加 `--authority-mode repo|genesis` 与 `--previous-authority PATH`，并以外部手写 rev1 snapshot 验证 revision 演进 | 继续宣称 in-memory no-bump mutant 已证明 authority 信任链 | 首次引入明确 genesis/PENDING；相同 revision 协调 semantic swap FAIL，rev2 缺人工批准仍 PENDING/非 merge-ready | 是 |
| Round5 Main gate只按全 AST node source range 排序 | Round6 production seam 真正拥有 DB/IPC continuations，Main 直接消费；AST 锁 Program.body parent、exact args、唯一 retained/run use | 把 initializer 移到 `if(false)`/function/try 后同步 source SHA | 不改 DB/IPC 业务行为；只把既有 `initializeApplication`/`registerAllIpcHandlers` 作为 frozen continuation 注入 | 是 |
| Round6 authority transition 把 `genesis` 排除在受控 payload，且同时保留未实现的 contractVersion +1 承诺 | Round7 将 `genesis` 纳入受控 payload，允许 post-merge 完整不变继续保持 v1/rev1/genesis，并把演进模型收敛为 v1 revision 精确 +1 | same-revision genesis flip 会错误通过；强制 post-merge genesis=false 又会让完整不变失败；顺带设计 v2 会扩大公共治理接口 | 四个 full-authority KAT 锁定 unchanged/flip/change/bump；未来 v2 必须另建 authority 与人工 redline | 是 |
| Round6 production Main 留两行注释兼容旧 substring 测试 | Round7 删除注释，并在 root 精确授权的两份 legacy startup test 中解析真实 Espree AST seam | 注释可以在真实调用缺失时误报绿色，不能证明 require、continuation ownership 或 app.whenReady 可达性 | source 与 validator 共用真实结构事实；31 个 tracked 路径为本轮最终 scope | 是 |
| Round6 validator 依赖 Node 的 ambient `require('espree')`，external previous 解析/审计分次访问路径 | Round7 锁定 Espree 10.4.0 的显式绝对模块路径；external previous 只 open/read 一次并由同一 bytes 解析、计 size 与 SHA-256 | staged-only copy 可因 NODE_PATH 环境不同失败；路径替换可让解析内容与审计身份分裂 | clean-copy 未设置 NODE_PATH 仍可用显式路径运行；报告记录真实 mode/path/digest/command | 是 |
| Round7 AST 只锁 imported/run identifiers，explicit genesis 直接跳过 previous 解析 | Round8 将 Main import 前 CommonJS loader/cache/module 设为不可变 bootstrap region并执行真实 import statement；Git explicit genesis 复用 repo merge-base解析，detached 单独标为 non-merge-evidence | reviewer 可先重绑 `require` 后保持 exact import 形状；post-merge 可显式声明 genesis 绕过 same-revision change-control | 18 个 startup mutants、18 个 hostile API cases与6个 genesis CLI/Git KAT；production Main/API/DB/IPC不变，authority仍 genesis/PENDING | 是 |
| Round8 loader proof 只执行第 71 行抽出的 import，Git 调用继承 process environment | Round9 把 exact require 移为 Main 首个 non-directive statement并执行 byte-0完整前缀；所有 Git 调用统一 clean env并与物理 repo identity相互校验 | 单条 statement proof不覆盖前置执行；cwd 不能阻止 ambient Git repository/object/config重定向 | startup mutants 18→21、Git/genesis KAT 6→12；新增 linked-worktree positive KAT，普通与 linked report均保留 exact gitDir/commonDir；产品 API/IPC/DB 不变 | 是 |
| Round9 no-write 只核 authority disposition/部分 source trust，且 external positive 直接复用 published repo report | Round10 把 selected report target 的完整 provenance、canonical generation command、reportVersion 与 input hashes 设为 exact gate；published report 只允许 repo/default 写入 | identical authority bytes 仍可能来自不同 mode/base/head/physical repo/path，部分 projection 无法证明报告属于本次 invocation | 原始 external-identical 29/29 旁路改为预期 FAIL；external/detached positive 改用包外临时 report，report v16→v17，不改变 authority payload或产品合同 | 是 |

## Evidence

| 证据 | 结果 | 覆盖的行为/风险 |
| --- | --- | --- |
| Python AST、27 个 JSON、Bash runner 语法检查 | PASS | 校验器与清单可解析，runner 无语法错误 |
| 锁定 `jsonschema==4.26.0` 的完整 Validator 预跑 | `24/24 PASS`、0 error、60 hashed inputs；23 messages、5 valid/24 invalid sequences、20 recovery-control mutations | Schema/fixture、双向 seq、PreparedIntent enum、Recovery result/Control mutation、跨文档合同、输入哈希覆盖 |
| Markdown fence、document path、package hygiene | PASS | 文档结构、81 个本地引用、无 pycache/pyc |
| 原包与修订包 `diff -qr` / whitespace scan | 仅 target 合同包发生审查性变化；源包、旧草案与业务源码未改；无 trailing whitespace | 范围控制与冻结源保护 |
| blindspot-pass + reconciliation-blindspot-pass | 无未关闭的文档级 P0；新增关闭 event mapping、Batch 原子兼容写、manual source、Provider transient/idempotency 与审计血缘 | 入口旁路、边界、失败状态、兼容性、资损审计 |
| 最终 report、PACKAGE-SHA256SUMS 与目录包完整性检查 | Validator 默认模式生成报告后，以目录内 61 条 checksum 覆盖除 checksum 文件自身外的 61 个文件；本 PR 未创建、未测试、也不提交 target ZIP | 证明 62 文件自包含目录包可复现，并避免把未执行 ZIP test 写成证据 |
| E00-G read-only preflight | baseline checksum 61/61 OK；定位 Batch exact union、event DDL、源码 legacy Task/Batch identity 与 validator gap | 证明两个问题属于公共合同 BLOCK，且 C1 未落地所以无需历史 backfill |
| E00-G validator（隔离 `jsonschema==4.26.0`） | `--no-write-report` 与默认模式均 `24/24 PASS`、0 error、61 hashed inputs；recovery-control 27/27 mutations rejected、7/7 canonical request hash self-tests PASS | Batch exact identity、request_hash DDL、完整 request 字段敏感性、writer domain separation 与跨文档合同 |
| E00-G package final | 自包含目录 63 文件；checksum 62/62 PASS；全量 JSON、Bash、Python AST、trailing whitespace、pycache/pyc、`git diff --check` 与 scope guard 均 PASS | validator report、evidence 与 checksum 生成链自洽；无 `src`/rules/旧 top-level changes diff |
| Reviewer round1 production probes | `task-policy-registry.js` SHA-256 `ee5299df...a08de`；122 个 reserve/no-file TaskPolicy 且 taskKey===channel；`archive_batches` PK=`id`，Task/Batch identity columns 来自真实 DDL | 关闭 machine binding 与 physical SQL 两个 BLOCK，不手抄不存在的 legacy key |
| Round1 targeted validator | 隔离 `jsonschema==4.26.0` 的 `--no-write-report` 与默认报告：均 `27/27 PASS`、0 error；52 actions、122 TaskPolicy、60 pairs、16 transition、4 observation、1 restart replay scenario | 新 schema/JCS/binding/result/owner/SQL gates 与报告输入 hash 已闭合 |
| Round1 blindspot + reconciliation redline | exact replay、任一 leaf conflict、binding mismatch、archive_batches.id CAS、final lineage 纳入 `P0-recovery-control-redline-human-review-checklist.md`，状态 `PENDING_HUMAN_REVIEW` | 机器门禁不替代资金/审计血缘人工复核；无伪造 approval |
| Round2 final validator/package | 隔离 `jsonschema==4.26.0` 的 default + `--no-write-report` 均 `27/27 PASS`、0 error；binding digest/provenance、20 requestKey（含 Hold source-pair stable key）、五项 DDL equality、43 doc mutants、122 schema assertions、321 required deletions、291 leaf、15 runtime/7 raw JCS vectors均通过；69 文件包的 68/68 checksum、JSON/Node/Bash/Python syntax、whitespace/pycache/diff/scope 均 PASS | 关闭 Reviewer round2 P1/P2/P3 机器合同 finding；`validation-report.json` 与 checksum 生成链自洽，人工红线仍 PENDING |
| Round3 targeted contract/source gates | 锁定 `jsonschema==4.26.0` 的 `--no-write-report` 为 `27/27 PASS`、0 error、67 inputs；production registry unit `5/5 PASS`，background-execution unit 目录 `230/230 PASS`，source ESLint PASS | source authority 52/122/60/52/70 与 digest、durable observation ordinal、20×20 result projection、400 field/60 owner mutations、正负 unsafe raw、Hold collision 与结构化 validator failure |
| Round3 project gates | 独立 lint、smoke、full unit `5832/5833 PASS`（0 fail/1 skip）与 integration 49 scripts / `2419/2419 PASS`；`check:vars` 精确命中 1 个 Important-skeleton `TaskPolicyRegistry`，无 Critical/Runtime-state/Risk-sensitive/Minor 命中 | Main startup fail-fast 的全仓回归、TaskPolicy 关联 review 与自动 integration inventory；人工资金红线仍 PENDING |
| Round3 `release-check` | 前两次在既有 `archive-storage-root-migration` symlink precedence 用例失败（期望 `ARCHIVE_STORAGE_SYMLINK_REJECTED`、实际 `ARCHIVE_PATH_SYMLINK_REJECTED`）；单文件 `46/46 PASS`、独立 full unit 两次 0 fail，第三次完整 release-check 最终 PASS：lint + smoke + unit `5832/5833`（0 fail/1 skip）+ integration `2419/2419` | 未修改授权范围外的 archive 源码/测试；最终发布链通过，同时保留曾观察到的组合时序不稳定 PROBE，不用一次绿色结果抹去失败证据 |
| Round4 targeted authority/result gates | production registry targeted unit `11/11 PASS`；隔离 `jsonschema==4.26.0` 的 `--no-write-report` 为 `27/27 PASS`、0 error、67 inputs；真实 Node hostile case 11/11，unbound 由 owned inventory/bound Set 真差集计算；独立 result KAT 20 branch/20 fields/digest gate，SQLite baseline 20 + mutations 460 全部通过，mapper mutant 由 SQLite query projection 产出而非查询后 object overwrite | caller replacement、descriptor drift、完整 inventory substitution、mapper self-comparison 与 wrong-owner lineage 均由独立 authority 阻断；人工红线仍 PENDING |
| Round4 project/full gates | background-execution unit `236/236 PASS`、targeted/full ESLint、smoke、独立 full unit `5838/5839 PASS`（0 fail/1 skip）、独立 integration 49 scripts / `2419/2419 PASS`；最终 `release-check` 再次完整 PASS（lint + smoke + unit `5838/5839` + integration `2419/2419`） | Main startup、完整 TaskPolicy inventory、barrel 与全项目生命周期零回归；Round3 archive symlink race 本轮未复现但仍保留外部 PROBE |
| Round4 `check:vars` | command 以预期 code 2 提醒 1 个 Important-skeleton `TaskPolicyRegistry`，无 Critical/Runtime-state/Risk-sensitive/Minor 命中 | 已按清单复核 policy inventory、Main wrapper、恢复幂等与 archive/integration，并执行 smoke、background/full unit/integration/release-check；不改变 TaskPolicy public shape |
| Round5 targeted authority/startup gates（Round6 trust-model 勘误） | 当时 production registry targeted unit `13/13 PASS`；隔离 `jsonschema==4.26.0` 的 default + `--no-write-report` 均 `28/28 PASS`、0 error、68 inputs；真实 Node hostile/order 14/14 | Round5 的 in-memory “authority no-bump mutant”以 current anchor 作比较，不能证明 external revision change-control；Round6 已取代该结论。历史 green 只保留为当时证据，不再宣称机器批准 authority |
| Round5 project/full gates | background-execution unit `238/238 PASS`、targeted/full ESLint、smoke、独立 full unit `5840/5841 PASS`（0 fail/1 skip）、独立 integration 49 scripts / `2419/2419 PASS`；最终 `release-check` 完整 PASS（lint + smoke + unit `5840/5841` + integration `2419/2419`） | production startup 与完整生命周期零回归；Round3 archive symlink race 和 RSS 异常本轮均未复现，仍保留为授权范围外 PROBE |
| Round5 `check:vars` | command 以预期 code 2 提醒 1 个 Important-skeleton `TaskPolicyRegistry`，无 Critical/Runtime-state/Risk-sensitive/Minor 命中 | 已复核 frozen host、完整 inventory、startup fail-fast、barrel export与恢复幂等；smoke、background/full unit、integration和最终 release-check 均已执行，不改变 TaskPolicy public shape |
| Round5 package/hygiene | 自包含目录 70 files、`PACKAGE-SHA256SUMS.txt` 69/69 PASS；32 个 JSON、Node/Bash/Python syntax、trailing-whitespace/no-pycache、`git diff --check` 与 scope guard 全部 PASS | 23 个合同包文件（含独立 authority）与5个精确授权包外文件自洽；其余用户未跟踪文件未触碰，报告/checksum 生成链可复现 |
| Round6 authority/machine-input/startup gates | 隔离 `jsonschema==4.26.0` 的 repo default、repo `--no-write-report`、explicit genesis `--no-write-report` 均 `29/29 PASS`、0 error、68 inputs；production registry targeted unit `15/15 PASS`、background-execution unit `240/240 PASS`、真实 Node hostile/reachability 17/17 | recursive duplicate rejection、strict positive integer metadata、external previous/repo/genesis trust model、Program.body direct startup seam、DB/IPC fail-fast 与 non-string `assertPair` 零 coercion 已由 machine/Node/AST gate 共同闭合；技术 PASS 不改变 genesis human review PENDING |
| Round6 project/full gates | targeted/full ESLint、smoke、独立 full unit `5842/5843 PASS`（0 fail/1 skip）、独立 integration 49 scripts / `2419/2419 PASS`；最终第二次完整 `release-check` PASS（lint + smoke + unit `5842/5843` + integration `2419/2419`） | Main startup real seam、完整 TaskPolicy inventory、barrel 与全项目生命周期零回归；Round3 archive symlink precedence 本轮未复现 |
| Round6 external RSS probe | 第一次 `release-check` 仅 `toolbox-large-split-multi-sheet` 为 `30/31`：tier2 RSS 中位增量 145MB 超过当次 effective 143MB；紧接单脚本复验 `31/31 PASS`（135MB/146MB），第二次完整 `release-check` 同脚本 `31/31 PASS` | 不修改授权范围外的压力测试/阈值；保留组合环境 RSS 波动证据，不以最终绿色结果抹除 transient failure，也不把它误归因于 recovery contract |
| Round6 `check:vars` | command 以预期 code 2 提醒 1 个 Important-skeleton `TaskPolicyRegistry`，无 Critical/Runtime-state/Risk-sensitive/Minor 命中 | 已复核单次 owned inventory、Main frozen exact wrapper、startup fail-fast、稳定身份/恢复幂等与 archive warning；source targeted/background/full unit、smoke、integration/release-check 均已覆盖，未改变 TaskPolicy public shape |
| Round7 authority/external/Espree gates | 隔离 `jsonschema==4.26.0` 的 repo no-write、explicit genesis no-write、external previous + expected digest no-write 均 `29/29 PASS`、0 error、68 inputs；external 首轮因已不存在的旧临时 fixture 稳定失败为 `VALIDATION_INPUT_CONTRACT_ERROR`，新建包外手写 rev1 snapshot并绑定 SHA-256 `6f370041…b049` 后复跑 PASS | 四个 full-authority KAT、genesis controlled payload、v1 revision-only、external same-bytes path/size/hash provenance 与 actual command 已闭合；保留首轮前置 fixture 失败，不把它误记为合同断言失败 |
| Round7 staged-only dependency probe | `git ls-files` 构造的 tracked-only 临时副本，在完全未设置 `NODE_PATH` 时以显式绝对 Espree 10.4.0 path 完成 genesis no-write `29/29 PASS`；不存在的 parser path 在 wrapper 与 direct validator 分别输出稳定 dependency/input contract error | validator 不依赖偶然全仓 module resolution；production binding probe 前置失败即结构化终止，不再以空 bindings 继续并产生次生 KeyError |
| Round7 startup/focused gates | production registry targeted `15/15 PASS`、background-execution 目录 `240/240 PASS`、`main-startup-contract` `6/6 PASS`、获扩展授权的 archive startup contract `26/26 PASS`、三份相关测试 ESLint PASS | exact production require/no shadow/no reassign、唯一 app.whenReady awaited seam、11 类 import/initializer/run mutant与真实 DB/IPC fail-fast 均由 AST/Node路径证明；删除两行 production 注释不再破坏旧测试 |
| Round7 project/full gates | smoke PASS；独立 full unit 首轮 `5841/5843`（1 fail/1 skip），唯一失败是 archive raw-regex，获 root 精确授权改为 AST 后复跑 `5842/5843 PASS`（0 fail/1 skip）；独立 integration 49 scripts / `2419/2419 PASS`；最终 `release-check` 完整 PASS（lint + smoke + unit `5842/5843` + integration `2419/2419`） | 记录失败、授权与复跑完整链，不用最终绿色覆盖中间证据；本轮 archive symlink 与 RSS probe 均未复现，rules 仅由 runner机械刷新 |
| Round7 blindspot pass | 复核 authority previous 来源/TOCTOU、report invocation provenance、genesis same-revision 演进、Main conditional/swallowed/early-return/fake-import/shadow 旁路、Espree ambient resolution、HR 状态与历史 probe | 无新增 P0/P1/P2 BLOCK；HR1-HR6 全部仍未勾选，技术 PASS 不改变 `PENDING_HUMAN_REVIEW`/non-production；Windows durability、archive/RSS 仍保持既有 PROBE |
| Round7 `check:vars` | command 以预期 code 2 提醒 1 个 Important-skeleton `TaskPolicyRegistry`，无 Critical/Runtime-state/Risk-sensitive/Minor 命中 | 关联 review 覆盖 122-key 单次 owned inventory、frozen exact host、TaskPolicy→binding→DB→IPC fail-fast、稳定恢复身份与 archive warning；不改变 TaskPolicy `get/list/require` shape、IPC、DB 或 production enablement |
| Round7 package/hygiene | 自包含目录 70 files、`PACKAGE-SHA256SUMS.txt` 69/69 PASS；32 个 JSON、Node/Bash/Python syntax、trailing-whitespace/no-pycache、`git diff --check` 与 31 tracked-file scope guard 全部 PASS | 最终 report v14、authority/manifest/docs/notes/checklist 与 checksum 生成链自洽；用户未跟踪文件未触碰，HR1-HR6 仍 PENDING |
| Round8 targeted loader/genesis gates | production registry targeted `15/15 PASS`；隔离 `jsonschema==4.26.0` 的 repo no-write 与 explicit genesis no-write 均 `29/29 PASS`、0 error、68 inputs | 18 类 startup AST mutant（含 reviewer `rebound-commonjs-require`、destructuring、`arguments[1]`/alias、target cache/alias、`module.constructor._load`）、真实 loader/export identity、6 类 genesis CLI/Git KAT 均 fail closed；HR1-HR6 仍 PENDING |
| Round8 validator authority matrix | repo default、repo no-write、Git explicit genesis no-write、包外手写 external previous + expected SHA-256 `776e3a3a…759f` no-write 均 `29/29 PASS`、0 error、68 inputs；完全无 `.git`/未设置 `NODE_PATH` 的 tracked-only副本先拒绝所携 repo merge-evidence report，再以 genesis default 重建 non-merge report并由 no-write复验 `29/29 PASS` | repo 报告 v15 记录 merge-base verified previous absent；detached 报告实际记录 `detached-genesis-non-merge-evidence`、`mergeEvidence=false`、真实 genesis/base-ref command，不能冒充 merge evidence；Git lookup/resolver error 不解释为 absent |
| Round8 默认报告同步失败与修复 | 第一次生成 v15 为 `27/29`，只报 owner/result projection KAT 的 authority ref 漏投影 `gitGenesisRequiresVerifiedPreviousAbsence`；补齐 Schema、Action Manifest、result KAT fixture 与两个 validator authority-ref 后复跑 `29/29 PASS` | 保留机器 gate 抓到跨层漏同步的证据；最终绿色没有覆盖首次失败，三项 public digest 与 result KAT 内容均未改变 |
| Round8 focused/project gates | targeted registry `15/15`、Main startup `6/6`、archive startup `26/26`、三份测试 + 三份 production 文件 targeted ESLint、全 src lint、smoke、full unit `5842/5843 PASS`（0 fail/1 skip） | 首次误用 `node --test <directory>` 得到 `MODULE_NOT_FOUND`，改用仓库实际 `*.test.js` 入口后 background-execution `240/240 PASS`；这是命令入口错误，不是产品失败 |
| Round8 integration/release gates | 独立 integration 49 scripts / `2419/2419 PASS`（369693ms）；首轮 `release-check` PASS（integration 282772ms）；blindspot 修订后的最终快照 `release-check` 再次完整 PASS：lint + smoke + unit `5842/5843`（0 fail/1 skip）+ integration `2419/2419`（370173ms） | 三轮 toolbox large-file `50/50`、multi-sheet `31/31` 均 PASS，Round3 archive 与 Round6 RSS 外部 probe 本轮未复现；integration runner 仅机械刷新 rules §七，不用最后一次绿色覆盖历史 PROBE |
| Round8 reviewer mutant 与 authority blindspot | 在 tracked-only 临时副本精确复放 reviewer 单行 `originalBindingRequire; require = ...; exact require` 形态，targeted unit 稳定为预期负例 `14/15`，只命中 `CommonJS loader/cache/module identity immutable before production import`；validator 立即结构化拒绝为 `PRODUCTION_BINDING_AUTHORITY_PROBE_FAILED`。blindspot-pass/reconciliation-blindspot-pass 另发现 detached report trust 可冒充与 Git lookup 非零误作 absent 两个 fail-open | 报告 trust projection 纳入 detached evidenceClass/mergeEvidence/previousAbsenceVerified；只有成功 exact `git ls-tree` 空 entry 才证明 absent，resolver/lookup/read error fail closed；新增两项 KAT 后 genesis CLI/Git mutation 为 6/6 |
| Round8 `check:vars` | 最终 command 输出 1 个 Important-skeleton `TaskPolicyRegistry` 提醒，无 Critical/Runtime-state/Risk-sensitive/Minor 命中 | 已复核完整 122-key owned inventory、Main exact loader→binding→DB→IPC fail-fast、TaskLifecycle policy/IPC inventory、lineage/related/recovery 与 archive integration；不改变 TaskPolicy `get/list/require` shape、IPC、DB 或 production enablement |
| Round9 startup/Git targeted gates | production registry `15/15 PASS`、background-execution `240/240 PASS`、Main/archive startup 合计 `33/33 PASS`、targeted/full ESLint PASS；隔离 `jsonschema==4.26.0` 的 repo、显式 genesis、external previous 三种 no-write 均 `29/29 PASS`、0 error、68 inputs | 21 个 startup mutants 与完整 byte-0 prefix/fresh export proof通过；12 个 Git/genesis KAT 覆盖 clean env、physical identity、ambient repository/object/config、nested root与 linked worktree distinct gitDir/commonDir；HR1-HR6 仍 PENDING |
| Round9 staged-only 与 Reviewer 原证据复放 | 完全 tracked-only、无 `.git`、unset `NODE_PATH` 副本先按预期拒绝携带的 repo merge-evidence 报告为 `28/29`；genesis default 重建 detached non-merge v16 report后 default/no-write 均 `29/29 PASS`。随后把 production import恢复到Reviewer发现的约第71行形态，startup unit稳定 `4/7`、validator `28/29`，命中 source hash/full-prefix/reachability gate | clean copy 不依赖 ambient parser/repo；旧真实 finding不能因同步 report 或 exact import文本仍存在而误绿。该临时负例不修改当前 workspace或用户未跟踪文件 |
| Round9 协调漂移失败与修复 | 首次 source/validator 落盘而 manifest 尚未同步时 repo no-write 如预期为 `27/29`；补 Main SHA 与 startup/Git count后 `29/29`。追加 linked-worktree KAT后 manifest 11 对 runtime 12 又如预期为 `28/29`，同步 hard count后恢复 `29/29` | 保留两次机器 coordination gate 真实抓漂移证据；没有把中间 FAIL 写成产品回归，也没有更改三项 public authority digest/revision |
| Round9 project/full gates | 独立 lint、smoke、full unit `5843/5844 PASS`（0 fail/1 skip）、integration 49 scripts / `2419/2419 PASS`（282941ms）；最终 `release-check` 完整 PASS：lint + smoke + unit `5843/5844` + integration `2419/2419`（372557ms） | 两轮 large-file 均 `50/50 PASS`，multi-sheet 均 `31/31 PASS`；Round3 archive symlink 与 Round6 RSS 外部 probe均未复现，runner仅机械刷新授权 rules §七 |
| Round9 blindspot/reconciliation pass | 复核 Main 首语句边界、helper/side-effect wrapper、fresh module cache、Git ambient controls、filesystem marker/ref、symbolic HEAD capture、ordinary/linked/nested/detached分域、report identity、HR状态与恢复幂等/审计血缘 | 无新增技术 BLOCK；未改变 request/result/SQL/资金语义，HR1-HR6、Windows directory fsync、archive/RSS仍保持既有 PENDING/PROBE，不以自动门禁伪造人工结论 |
| Round9 `check:vars` | command 以预期 code 2 提醒 1 个 Important-skeleton `TaskPolicyRegistry`，无 Critical/Runtime-state/Risk-sensitive/Minor 命中 | 已复核 policy inventory/Main wrapper/裸IPC与renderer/preload入口、TaskLifecycle lineage/recovery/archive告警；targeted/background/full unit、smoke、integration与release-check覆盖，未改变 TaskPolicy `get/list/require` shape |
| Round9 package/hygiene | 自包含目录 70 files、`PACKAGE-SHA256SUMS.txt` 69/69；最终 report v16、32 个 JSON、Node/Bash/Python syntax、trailing-whitespace/no-pycache、`git diff --check` 与 31 tracked-file scope guard全量复验 | authority仍 v1/rev1/genesis/PENDING，用户未跟踪文件未触碰；report/checksum只记录证据，不反向授权 anchor |
| Round10 原始 P1 复现与对称拒绝 | 修复前用包外 identical authority（SHA-256 `776e3a3a…759f`）复用 published repo report，external no-write 错误得到 `29/29 PASS`；修复后同命令与 external-report→repo 均稳定 `28/29 FAIL`，只报 complete authority provenance/canonical generation command drift | 证明 finding 是实际旁路而非理论风险；5 个 machine KAT 同时固定 same-repo PASS、repo→external、external→repo、different base/merge-base、different HEAD/physical identity FAIL。非 repo 覆盖 package report 稳定报 `VALIDATION_CANONICAL_REPORT_PROVENANCE_REQUIRED`，包内 custom target 稳定报 `VALIDATION_REPORT_TARGET_SCOPE_INVALID` |
| Round10 authority/report 模式矩阵 | repo published report v17 default/no-write、包外 external report default→same-provenance no-write、包外 Git explicit-genesis report default→same-provenance no-write均 `29/29 PASS`、0 error、68 inputs；external report精确保留 path/size/actual+expected SHA-256，repo report保留 base/merge-base/HEAD/toplevel/gitDir/commonDir | published report 始终是 repo/default canonical command；不同 provenance 的预期 FAIL 不列作绿门禁，custom report 不进入 package/checksum |
| Round10 tracked-only detached probe | `git ls-files` 构造无 `.git`、不含用户 untracked 的临时副本；完全 unset `NODE_PATH`、显式 Espree 10.4.0 path，包外 detached report default/no-write 均 `29/29 PASS`，且 validator bytes 与 workspace相等 | report v17 精确记录 `detached-genesis-non-merge-evidence`、`mergeEvidence=false`、`previousAbsenceVerified=false`、null Git identity 与真实 custom report command；没有改写或冒充 package published merge evidence |
| Round10 targeted/project/full gates | production registry `15/15 PASS`、background-execution `240/240 PASS`、Main/archive startup `33/33 PASS`、targeted/full lint与smoke PASS；独立 unit `5843/5844 PASS`（0 fail/1 skip），独立 integration 49 scripts / `2419/2419 PASS`（289402ms）；完整 `release-check` PASS：lint、smoke、unit `5843/5844`、integration `2419/2419`（374902ms） | 两轮 large-file 均 `50/50`，multi-sheet均 `31/31`；Round3 archive symlink与Round6 RSS外部 probe均未复现，但继续保留历史 PROBE。integration runner只机械刷新授权 rules §七 |
| Round10 blindspot/reconciliation pass | 复核 report target/command/input-hash绑定、absolute-path normalization、mode/base/head/physical identity、external expected digest、custom report package污染、duplicate-safe loader与 fail-closed顺序；资金合同 request/result/SQL/binding内容均未改变 | 发现并关闭“custom report可写入包内其他路径”盲区；无新增技术 BLOCK。HR1-HR6仍全部未勾选，Windows fsync、archive/RSS保持既有 PENDING/PROBE，不以机器结果伪造人工批准 |
| Round10 `check:vars` | command 以预期 code 2提醒1个 Important-skeleton `TaskPolicyRegistry`，无 Critical/Runtime-state/Risk-sensitive/Minor命中 | 本轮未改3个 production source；继续复核既有122-key owned inventory、Main binding→DB→IPC fail-fast、TaskLifecycle/IPC/renderer-preload边界，并由targeted/background/full项目门禁覆盖；TaskPolicy `get/list/require` shape不变 |

## Remaining Unknowns

| 未知 | 处理 | 负责人/下一步 | 合并影响 |
| --- | --- | --- | --- |
| Windows packaged directory fsync durable primitive | PROBE | E02-C2 在 packaged Windows 环境验证；unsupported/failure 走 DURABILITY_BARRIER_UNAVAILABLE hold | 不阻塞公共平台编码；阻塞所有 target-post-image 资金 action production enablement |
| Recovery red-line checklist 的实现级 evidence | PROBE | 同一 Reviewer 在实现 PR 的脱敏 control DB 上完成六组 checklist 并签名 | 不阻塞本合同勘误落盘；阻塞对应 recovery implementation merge/production enablement |
| 全量组合曾出现 archive symlink precedence 不稳定 | PROBE（授权范围外） | repository owner 后续调查为何 Round3 两次先返回 `ARCHIVE_PATH_SYMLINK_REJECTED`；Round4 与 Round5 独立/full unit 及最终完整 release-check 均未复现 | 不阻塞本轮；保留历史观察记录，避免未来把同类竞态误归于 recovery contract |
| 全量组合的工具箱 RSS guard 偶发越界 | PROBE（授权范围外） | repository owner 后续调查为何 Round6 第一次 release-check 的 tier2 median 为 145MB/预算143MB，而相邻独立与第二次完整运行均 `31/31 PASS` | 不阻塞本轮；保留三次成对证据，禁止通过放宽阈值掩盖环境波动 |
