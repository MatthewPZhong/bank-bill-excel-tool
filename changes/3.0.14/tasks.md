# 3.0.14 Tasks

> 状态：功能实施与自动验证完成，待人工资金复核和发版
> 原则：四个可独立 review 的切片；前一切片的契约稳定后再接下一层。

## PR 1：命中行异常说明口径

- [x] 多对多/异常说明检测输入缩小到实际改值行。
- [x] `命中场景` 只按实际改值判定，不允许 note-only 提升。
- [x] 保持 dispatcher 锁定和行数守恒。
- [x] 补改值+异常、no-op+异常候选、锁定 no-op 回归测试。

## PR 2：临时 MPT 网关账单

- [x] 定义 INBOUND/OUTBOUND 33 字段 schema 和规范字段适配器。
- [x] 实现 `.txt` / `.gz` 流式解析、首行识别、强校验和错误定位。
- [x] 实现 OUTBOUND 金额/币种三级成对 fallback。
- [x] 扩展 run-data side DB 模块、DDL 和批次 repository。
- [x] 实现 hash 幂等、新序号原子替换、旧序号拒绝。
- [x] 实现批次列表、删除和全部清空服务。
- [x] 补解析、原子性、跨重启和 side DB parity 测试。

## PR 3：前置资金对账引擎与导出

- [x] 实现标准银行行校验、方向/name/cardNo/金额派生。
- [x] 实现网关 10 字段指纹和完全重复折叠。
- [x] 实现临时优先的联合候选池和稳定 1:1 消费。
- [x] 结果和统计落 run-data side DB，保证行数守恒。
- [x] 更新模板为 5-sheet 固定契约。
- [x] 实现按渠道流式导出和临时文件原子完成。
- [x] 补匹配、重复、空 ID、0 不平、跨渠道和 Excel 契约测试。

## PR 4：模块接线、兼容和端到端

- [x] 注册 `前置资金对账` 模块和功能设置项，已有用户默认关闭。
- [x] 接入页面、按钮、唯一场景下拉框、进度和状态。
- [x] 接入 preload / main IPC、文件选择、共享锁和结果失效。
- [x] 实现临时链接表管理弹窗。
- [x] 临时链接表删除框与既有日期范围删除框同构，并按批次账单日期闭区间删除。
- [x] 将 INBOUND/OUTBOUND 展示为两个逻辑表库，并按 `sourceType` 独立汇总和删除。
- [x] 删除重复 MPT 导入入口，并保持状态框尺寸与资金对账数据处理一致。
- [x] 按资金对账数据处理五槽位重排前置模块，并将主面板入口改名为 `链接表管理`。
- [x] 收紧前置资金对账平账条件为对账 ID、渠道、金额、币种四项全部相同。
- [x] C4 读取器兼容旧 4-sheet 与新 5-sheet。
- [x] 增加完整集成与 smoke 覆盖。
- [x] 执行 `/check-vars` 并记录关联功能 review。
- [x] 逻辑表库隔离完成后重新执行完整 `release-check`（3445 unit + 39 integration scripts / 1817 assertions）。
- [ ] 完成人工资金红线复核。
- [ ] 验收通过后再更新版本号和三份发布文档。
