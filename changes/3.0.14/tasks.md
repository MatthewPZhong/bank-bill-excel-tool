# 3.0.14 Tasks

> 状态：五个切片、自动验证、技术 Review、人工资金复核、PR #87 合并和最终归档均完成
> 原则：保留原四个已完成切片的历史证据；第 5 个契约偏差切片以最终 `release-check` 和人工资金复核为放行依据。

## PR 1：命中行异常说明口径

- [x] 多对多/异常说明检测输入缩小到实际改值行。
- [x] `命中场景` 只按实际改值判定，不允许 note-only 提升。
- [x] 保持 dispatcher 锁定和行数守恒。
- [x] 补改值+异常、no-op+异常候选、锁定 no-op 回归测试。

## PR 2：临时 MPT 网关账单

- [x] 定义 INBOUND/OUTBOUND 33 字段 schema 和规范字段适配器。
- [x] 实现 `.txt` / `.gz` 流式解析、首行识别、强校验和错误定位。
- [x] 实现 OUTBOUND 金额/币种三级成对 fallback。
- [x] 扩展 run-data side DB 模块、DDL 和批次 repository。
- [x] 实现 hash 幂等、新序号原子替换、旧序号拒绝。
- [x] 实现批次列表、删除和全部清空服务。
- [x] 补解析、原子性、跨重启和 side DB parity 测试。

## PR 3：前置资金对账引擎与导出

- [x] 实现标准银行行校验、方向/name/cardNo/金额派生。
- [x] 实现网关 10 字段指纹和完全重复折叠。
- [x] 实现临时优先的联合候选池和稳定 1:1 消费。
- [x] 结果和统计落 run-data side DB，保证行数守恒。
- [x] 完成原 5-sheet 基线模板与导出契约（后续由 PR 5 扩展为按渠道动态 5/6-sheet，前 5 个 sheet 保持不变）。
- [x] 实现按渠道流式导出和临时文件原子完成。
- [x] 补匹配、重复、空 ID、0 不平、跨渠道和 Excel 契约测试。

## PR 4：模块接线、兼容和端到端

- [x] 注册 `前置资金对账` 模块和功能设置项，已有用户默认关闭。
- [x] 接入页面、按钮、唯一场景下拉框、进度和状态。
- [x] 接入 preload / main IPC、文件选择、共享锁和结果失效。
- [x] 实现临时链接表管理弹窗。
- [x] 临时链接表删除框与既有日期范围删除框同构，并按批次账单日期闭区间删除。
- [x] 将 INBOUND/OUTBOUND 展示为两个逻辑表库，并按 `sourceType` 独立汇总和删除。
- [x] 删除重复 MPT 导入入口，并保持状态框尺寸与资金对账数据处理一致。
- [x] 按资金对账数据处理五槽位重排前置模块，并将主面板入口改名为 `链接表管理`。
- [x] 收紧前置资金对账平账条件为对账 ID、渠道、金额、币种四项全部相同。
- [x] C4 读取器完成旧 4-sheet 与原新 5-sheet 基线兼容（6-sheet 兼容列入 PR 5）。
- [x] 增加完整集成与 smoke 覆盖。
- [x] 执行 `/check-vars` 并记录关联功能 review。
- [x] 逻辑表库隔离及 Review 修复完成后重新执行原 5-sheet 基线 `release-check`（3452 unit + 39 integration scripts / 1817 assertions）。
- [x] 更新版本号、`CHANGELOG.md` / `VERSION_FEATURE_HISTORY.md` / `USER_GUIDE.md` 和 3.0.14 预览图。

## PR 5：重复网关账单审计（PR #87 用户确认后的契约偏差）

- [x] PM/spec 反向同步：动态 5/6-sheet、固定 22 列、双方原始 JSON 分片、渠道并集、C4 4/5/6 和 `sourceType` 删除隔离。
- [x] 匹配/结果侧为每个完全重复折叠组保存统一 `折叠记录ID`、保留记录和全部被折叠记录；折叠仍只产生一个候选，不改变四字段 1:1。
- [x] 结果 side DB 保存双方数据来源、数据位置、13 个展示字段、重复指纹、折叠原因和原始 JSON，生命周期覆盖到导出完成。
- [x] writer 按渠道独立判断：无重复保持原 5 sheet；有重复时末尾追加固定 22 列 `重复网关账单`。
- [x] 原始 JSON 按单片最多 30000 字符多行输出，保留/被折叠每个对象都可按分片序号逐字符无损重组，禁止截断或只写摘要。
- [x] 导出渠道改为银行渠道与重复记录渠道并集；重复专属渠道仍生成 6-sheet 文件，所有文件严格按渠道隔离。
- [x] C4 读取器扩为 4/5/6-sheet 兼容并安全忽略末尾重复审计 sheet，不放宽其它结构校验。
- [x] 回归锁定 `sourceType + sourceDate` 预统计/删除隔离：删 INBOUND 不影响 OUTBOUND，反之亦然，共享月库仍非空时不回收。
- [x] 按 `test-spec.md` 补 unit/integration/smoke：多重复对象、22 列、30000 边界、无损重组、分渠道 5/6、重复专属渠道、C4 4/5/6。
- [x] 重跑针对性测试和 `npm run release-check`，最终为 unit 3483/3483、integration 1842/1842（39 脚本），并回填发布文档。
- [x] 完成人工资金红线复核：指纹与保留对象、双方原始 JSON、渠道并集/不串、C4 兼容、`sourceType` 删除隔离。
