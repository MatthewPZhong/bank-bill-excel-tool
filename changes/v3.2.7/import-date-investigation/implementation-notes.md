# 业务 OP 导入完成但未见新表排查

## Task Brief / Baseline

- Goal：解释用户提供的两份 OP 文件导入后，数据管理没有对应新表的原因。
- Context：当前分支 `codex/v327-enable-bizop-production`，排查起点 `e682bec0`；用户指明存档批次 `2026-09-07-006`、`2026-09-07-007`。
- Constraints：只读检查原文件、实际 SQLite 和既有提交记录；不重导入、不改账期、不修改业务数据，不将存档成功直接视为新增数据成功。
- Done when：按文件、批次、提交收据、当前有效输入和列表筛选建立完整对应关系，说明查看入口及提示不足。

## Unknowns Register

| 未知 | 分类 | 验证及结论 |
| --- | --- | --- |
| 实际运行目录、模块是否启用 | PROBE 已确认 | Electron 使用本仓库；真实数据库模块模式为 ACTIVE |
| 文件名日期是否为实际账期 | PROBE 已确认 | 流式读取两份 XLSX 全部数据行的 Billdate，实际为 2026-05-11、2026-05-12 |
| 是否解析失败或漏写 | PROBE 已确认 | 两个 Task 均 succeeded，IMPORT 收据明确复用已有 OP v1；007 扫描及接受 333432 行，行/文件错误均为 0 |
| 新文件与已有输入是否相同 | PROBE 已确认 | 当前文件、原来源文件与存档 blob 的 SHA-256 逐一相同 |
| 数据管理为何不列新文件名 | PROBE 已确认 | 列表取当前 ACTIVE dataset 的来源；相同指纹复用旧版本，保留旧来源和 activated_at |

## Evidence

| 当前样本 | 全量 Billdate | 数据行数 | 已有来源 |
| --- | --- | --- | --- |
| 业务OP账单-260401.xlsx | 2026-05-11 | 166666 | 业务OP账单 (21).xlsx |
| 业务OP账单-260402.xlsx | 2026-05-12 | 166766 | 业务OP账单 (20).xlsx |

- 两份文件均 23 列，各行日期完全一致，无缺失 Billdate。
- 260401 文件 SHA-256：`bc4ded70ca81f8802b7b332a399fab3aefaa57fbbc9acb99071b6cc461ec7107`。
- 260402 文件 SHA-256：`62f1d332acf13afaae9154de4d4359ae5f4cac4bd453a974635320866ddfc631`。
- 批次 006：新增 FLOW / 2026-04-02 v1，55529 行；OP / 2026-05-11 和 2026-05-12 均 `reused=true`。
- 批次 007：两份 OP 均 `reused=true`，没有新增版本；原件正常存档。
- 对应导入 Task：006 为 `8bb9405b-f317-43f1-a430-f4775f43af75`；007 为 `5744caf1-5ca6-4c45-bfcb-08e4ea581e65`。
- 数据读取使用 SQLite URI `mode=ro`；工作簿使用只读加载及 ZIP/XML 流式遍历。实际当前文件与旧来源文件哈希也与存档 blob 匹配。
- 代码依据：`import-adapter.js:createImportAdapter` 使用第一列 Billdate；`candidate-router.js` 输入指纹不含文件名；`catalog.js:commitImport` 相同账期/指纹复用；`metadata.js:list` 按 activated_at 的操作月份筛选 ACTIVE 数据并读取其来源。
- 当前查看条件：操作月份 2026-09、输入类型 OP；校验表及校验原表查看 2026-05-11 和 2026-05-12，原表来源仍显示旧文件名。
- 结束前只读复查发现用户后续批次 `2026-09-07-009` 已 succeeded：来源为 `Copy of 业务OP账单-260401.xlsx` 和 `Copy of 业务OP账单-260402.xlsx`，已新增 ACTIVE OP / 2026-04-01 v1（166666 行）与 OP / 2026-04-02 v1（166766 行），提交时间 2026-09-07 07:22:28 北京时间。该操作由用户执行，本次排查没有触发；上述 006/007 的结论仍成立。

## Decisions / Deviations

- 诊断结论：本次没有发现导入漏写；实际账期与文件名不同，且文件内容与已有输入完全一致，触发设计内的版本复用。
- 不更改日期解析、幂等规则或来源归属，不修改真实文件与业务数据；无生产代码变更，也无需为只读诊断运行 release-check。
- 当前提示不足：导入协调器把复用结果放在 receipt.datasets，IPC compact 未将其传给页面，状态提示未说明本次新增与复用情况。改善该提示属于后续实现，本次没有修改公共返回合同。

## 对账盲区复核 / Remaining Unknowns

- 当前两个批次的接受行数、实际日期、复用版本、旧来源和存档原件可对应；没有证据表明记录丢失、金额变化或错误归属。
- 不得按文件名静默改业务日期；用户后续自行导入的 Copy 副本已形成四月份当前输入，本次未修改或另外解析这些副本，只核实其成功收据关联的数据库元数据。
- 本次未执行重新导入或计算；排查期间用户仍在操作应用，结论限定于已核实的 006/007 及两份已校验文件。
