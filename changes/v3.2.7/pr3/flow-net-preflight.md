# 合计流水发生额口径预检（2026-09-07）

用户已回复“对的”，确认允许第 13 列按入减出变号，第 14 列改减法且保持原数值，核对差额和结论不变；下述 BLOCK 为确认前的发现记录，已解除。实施合同及当前状态见 spec.md、implementation-notes.md。

## Task Brief

- Goal：用户要求合计流水发生额改为入账合计减出账合计，同时确保改前改后输出结果不变。
- Context：PR237，当前提交 1479fc93；现有 19 列结果保存的是出减入的反推流水。
- Constraints：真实业务数据只读；正负冲正、十进制精度、账户/币种、区间、重复流水和历史封存数据不变；未确认口径前不改金额代码及既有验收 oracle。
- Done when：明确允许变化的列及历史兼容要求后，完成公式/说明/版本调整及独立前后输出对比。

## Facts / Unknowns Register

| 未知 | 分类 | 证据和处置 |
| --- | --- | --- |
| 当前入出账是否已自动带方向符号 | PROBE 已确认 | import-adapter.amountText 保留原符号；compute-group.observe 按方向原值累加。C01 为入 200、出 100；C12 有负数入账冲正，不能取绝对值或自动反号 |
| 输出不变是否允许合计列改变 | BLOCK | compute-group.finishGroup 当前第 13 列是出−入；17 个批准样例中 16 个合计值按新公式会变号。需用户明确是否仅要求核对差额和结论不变 |
| 同步反推是否保持原核对结果 | PROBE 已确认 | 第 14 列由终止余额＋旧合计改为终止余额−新合计，可令全部 17 例第 14/15 列保持；缺失/null 及阈值不必改变 |
| 同输入重跑与历史结果是否受到影响 | PROBE 已确认 | compute-main 按输入指纹复用旧 run，指纹含共享导入 RULE_VERSION。直接改公式会复用旧结果；直接 bump 导入版本会拒绝现有输入，不能当作兼容方案 |

## Decisions / Proposed Plan

1. 先确认“输出结果不变”是否指核对差额/结论不变，允许第 13 列按新公式变号，以及第 14 列标题改为相应减法。
2. 确认后再确定独立计算规则版本及历史导出分流；不通过改导入版本迫使用户重导输入，不回写历史封存结果。
3. 核对 17 例、负数出账/混合冲正、零金额、超大/高精度十进制、容差，以及真实临时导入→计算→全量/差异导出与旧输入/历史结果兼容。

## Evidence

- 现有基线：`node --test tests/unit/main-process/biz-op-v327-compute.test.js`，19 PASS / 0 FAIL。
- 独立数学对比：17 例反推余额与差额一致；16 例合计值变化。探针记录 `/tmp/bizop-flow-net-probe-20260907.json`。这只证明候选公式的代数等价，不是新实现或人工业务验收。
- 独立只读复核确认：RESULT_FULL/RESULT_DIFF 从封存行取值，导出 expected/actual 不重新执行金额公式；须保护独立业务 oracle。RUN_META 当前说明及第 14 列加号表头也需与新语义同步。

## Reconciliation Blindspot Pass

### [Important] 合计金额方向及输出合同变化

- 场景/证据：C01 的 -100 会变为 100；第 14 列当前标题仍为“终止期末＋合计流水”。
- 影响：若只改第 13 列公式而不改反推，差额和结论会改变；若只改内部变量再取负输出，则导出列并没有实现用户要求的新公式。
- 处置：BLOCK。⚠️ 资金红线，请人工复核；由用户明确“输出结果不变”的范围。未修改任何金额实现。

### [Important] 旧结果复用和历史兼容

- 场景/证据：input_fingerprint 复用 PUBLISHED run，现有输入分片严格校验共享 RULE_VERSION，export-source 对结果 schema 也严格匹配。
- 影响：只改单行公式可能继续复用旧结果，粗暴改共享版本会拒绝已导入文件，统一改表头可能误解释旧值。
- 处置：待上述口径确认后，设计并验证独立计算版本及历史兼容；不修改旧输入、旧行或原件。

## Remaining

用户口径已确认，两个 BLOCK 均已解决。17 例既有验收值原样留作旧版证据，独立新 oracle、真实旧版到新版回放及相关回归全部通过，详见 flow-net-validation.md。真实业务、Windows 和目标规模验收仍未执行。
