# CODEX.md

本文件维护 Codex 的条件化工作流与完成标准；项目事实、通用边界和命令见 `AGENTS.md`。只读任务命中的章节，不按顺序执行全部流程。

## 决策与 spec
- 明确 `Goal / Context / Constraints / Done when`，可沿用清楚的任务说明，不为格式另建文档。目标与既有契约明确的小修复直接实现、验证，不因多文件或 UI 改动要求完整 spec。
- 新增或调整业务语义、数据契约、迁移策略、公共接口或跨模块设计时，先明确并记录决策。现有 spec 足够就引用，需要变更就更新相关部分，不重复生成整套 PRD/TechDoc/spec。
- 需要存放、查找复用或补齐 spec / TechDoc 时，使用 `.agents/skills/save-spec/SKILL.md`；仅阅读、评审或讨论时不触发文档写入。

### 未知项的定义与处理
- **未知项**：完成当前任务所需、尚缺可靠依据的问题，包括未确认的事实、未明确的需求或约束、待定的设计决策，以及尚未消解的来源冲突。不影响当前任务的问题不纳入。
- **重要未知项**：不同答案会实质改变目标行为、实现方案、数据/接口契约、资金/安全或授权边界、验收标准的未知项；能沿用既有约定的命名、格式等细节不算。
- 未知不等于阻塞。能从现有资料直接确定的就地核实，不启动完整流程；只有重要未知项仍需多步调查、实验或决策整理时，才按下一节触发相关 Skill。

| 处理方式 | 适用条件与动作 |
|---|---|
| `PROBE` | 先查用户要求、spec、代码、测试、配置和日志；必要时做小实验，不询问仓库可查的信息。 |
| `BLOCK` | 关键决策经调查仍不明，且影响目标、模型/契约/架构、资金/安全边界、主流程或不可逆操作时，问一个关键问题；先做不受影响的已授权工作。 |
| `ASSUME` | 仅低影响、可逆歧义采用保守的现有模式，报告影响理解或验收的假设。 |

- 探索先核实身份/归属/键、状态/幂等与最小端到端链路，再扩大实现。已授权且符合既定契约的操作连续完成，不因领域敏感逐步请示。
- 发现假设不符或行为/验收偏离时记录证据与决定；纯技术调整同步已有 TechDoc，业务行为或契约变化先明确需求依据并同步适用 spec。重要冲突未解决前，不凭猜测改变受保护行为。

## 协作与 Skill
- 默认当前 Agent 完成；只有独立并行或独立审查能节省时间、改善质量时委派。不强制每次迭代分角色，工具不可用或拆分无收益时不模拟交接。
- 委派明确范围、输入、输出、验收，避免同时修改同一文件。Development 依据 spec/任务说明及适用 TechDoc 实现并验证；主 Agent 整合结果、消解冲突、核对证据，不直接转交原始输出。
- Skill 仅在任务命中、入口存在且环境支持时使用，不按规模或领域整套加载；不可用时做等价检查，无法等价完成则说明缺口，不跳过必要验证或虚报调用。

| 入口 | 触发条件 |
|---|---|
| `.agents/skills/unknowns-first/`、`.agents/skills/blindspot-pass/` | 上节定义的重要未知项经直接查阅仍未解决，需要多步取证、实验或有针对性的盲点审查；简单检索即可确定的不触发。 |
| `.agents/skills/implementation-notes/` | 决策、假设或偏离需跨步骤保留；优先复用现有 spec、任务或日志。 |
| `.agents/skills/prototype-first/` | 一次性产物能消除真实偏好或契约歧义；已确定工作不做强制原型。 |
| `.agents/skills/reconciliation-blindspot-pass/` | 命中下一节的对账语义风险。 |
| `.agents/skills/weekly-release/SKILL.md` | 准备或继续本轮发布时按 checklist 执行；模块分支已合入 release 时先核验现有成果，再续做未完成步骤，不重复集成。 |

## 验证与风险审查
- 先做覆盖影响的最小有效验证；内部算法、单模块规则优先单测或 smoke。行为变化和修复补齐必要回归覆盖，不写仅复述实现的测试。
- 用户可见输出契约、迁移、保留行为、文件 IO、跨模块数据流变化，须补充或更新集成测试，遵守 `rules/integration-test-policy.md`。
- 已授权的本地验证可连续运行、修复本次修改引起的失败、复跑受影响检查，不逐步请示；涉及真实数据或外部服务先核实资源与授权，不假定所有测试均无生产访问。
- 必要检查完成并通过后，无新改动、失败或未解决疑点，不重复或扩大测试。既有问题、环境失败及未执行项须报告原因和影响，不无关扩修或虚报通过。
- 实际改变账单/对账/清结算/转账的金额/币种、匹配/回填、身份/状态/幂等、数据血缘、审计或行/输出契约时，做对账专项审查，保留可解释血缘、幂等、金额/币种语义和可审计处置。资金损失红线发现须人工复核；不因文件属于对账模块就检查所有无关流程。
- 共享状态、持久化格式或业务契约变化时，按需参考 `rules/important-variables.md` 的相关风险说明，核查定义、调用方和回归覆盖；仅保留风险知识，不沿用旧的固定扫描、统计刷新或分级汇报流程。

## 交付门禁与完成
- 发布、升版、正式 GUI 交付或 PR-ready 前，`npm run release-check` 是本地自动检查的 PASS/FAIL 依据；局部验证不能替代，证据须覆盖最终内容。人工验收仍按适用条件执行；发布文档按 `AGENTS.md` 同步。每周正式发版按 `.agents/skills/weekly-release/SKILL.md` 执行，不把日常小改动强制纳入整套发版流程。
- 核对需求、设计与实现：spec/任务说明符合用户要求，TechDoc（如有）与其一致；实现符合目标、范围与非目标；测试/人工检查覆盖验收与关键边界。不把未经批准的回退或仅验证实现细节视为完成。
- 任务包含运行程序、检查界面或修复失败时，做到这些步骤完成，不在首版后例行停等审查。代码错误继续修复，技术设计缺口补齐适用 TechDoc，需求缺口按「决策与 spec」处理；关键决策、权限或外部阻塞只暂停受影响部分。
- 验收未完成或仍需必要人工复核时，交付已有结果并说明阻塞、未做/未验证项及原因，不宣称全部完成。
- 报告结果、改动文件、验证命令/结果；按需补充可见行为、假设、风险、回滚/兼容问题与人工检查。正式发版任务额外报告纳入/顺延分支、最终 PR、`main` 提交、正式标签与发布结果。简单任务不套完整模板，不以计划、自评分或建议代替交付。

## 资料索引
- `rules/`：相关安全、编码、文档和专项约束；`knowledge/`：经验、风险与运行记录。动态状态用当前证据，冲突明确说明，不静默改写历史决定。
