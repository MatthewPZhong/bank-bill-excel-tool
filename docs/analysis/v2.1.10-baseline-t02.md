# v2.1.10 β Phase 0 T02 — 起点基线报告

| 字段 | 值 |
|---|---|
| 报告版本 | v0.1（Phase 0 起点 — Dev T02）|
| 生成时间 | 2026-05-28 |
| 关联 PRD | `docs/iterations/v2.1.10/PRD-v2.1.10.md` v0.2 |
| 关联 spec | `docs/iterations/v2.1.10/spec.md` v0.2 §九 |
| 关联 tasks | `docs/iterations/v2.1.10/tasks.md` v0.2 §三 T02 |
| 当前分支 | `v2.1.10` |
| package version | `2.1.10-beta.1` |
| 当前 src 改动 | 0（未启动 Phase 1）|
| 基线工具版本 | `scripts/scan-vars.js` / `scripts/check-vars.js`（无升级）|

---

## 一、目的

按 `tasks.md` T02 + `CLAUDE.md` 重要变量 check：
1. 跑 `npm run scan:vars` 重新生成 `docs/analysis/var-reference-stats.{md,json}`，建立 v2.1.10 起点变量统计基线
2. 跑 `npm run check:vars` 输出当前 src/ 改动命中情况（v2.1.10 尚未开 src/ 改 → 应 0 命中）
3. 对照 `rules/important-variables.md` 5 层，评估：
   - v2.1.9 SR-FIX-1 新增符号是否需升格
   - scan-vars 新增的 A-share（跨 ≥ 3 文件）是否未升格

**T02 仅观察 + 报告**；不修改 `rules/important-variables.md`（升格评估留给后续 / PM 审批）。

---

## 二、scan:vars 关键数据

### 2.1 顶层统计（v2.1.10-beta.1 baseline）

| 维度 | v2.1.7（v9 元数据基线）| v2.1.8（v11 元数据基线）| v2.1.10-beta.1（本次）| 差值 |
|---|---:|---:|---:|---|
| 扫描 JS 文件数 | — | 85 | **90** | +5 |
| 顶层声明总数 | 853 | — | **938** | +85 |
| ≥2 次引用 | — | — | **832** | — |
| 跨 ≥3 文件（A-share）| 146 | — | **158** | +12（v2.1.7 起算）|
| 跨 2 文件（A-pair）| 247 | — | **269** | +22 |
| 单文件（A-local）| 359 | — | **405** | +46 |
| 跨文件合计（B）| 393 | — | **427** | +34 |

**观察**：v2.1.7 → v2.1.10-beta.1 增长 ~12% A-share + 5 个新 JS 文件；属正常迭代规模（v2.1.8 N1/N2/N3/N4 + v2.1.9 N5/N6/N7/G1-cont 累加 →）。

### 2.2 A-share top 10（跨度 ≥ 3 + 总次数最高）

| 名字 | 跨度 | 总次数 | 备注 |
|---|---:|---:|---|
| `path` / `fs` / `XLSX` | 27-33 | 60-106 | 技术性 require（已明确排除升格）|
| `normalizeCell` | 14 | 95 | 已是 Important-skeleton |
| `FileValidationError` | 13 | 63 | 已是 Critical |
| `pad` | 10 | 66 | 已在 Minor 提示性 |
| `appendModuleLog` | 10 | 39 | v2.1.9 SR-log-1 引入；属日志门面；建议人工评估是否升格 Important-skeleton |
| `applyWatermark` | 11 | 27 | 工具，未达资金/契约门槛 |
| `PENDING_COLUMNS` | 9 | 33 | 已是项目内 column anchor 范式 |
| `FLOW_HEADERS` | 8 | 25 | 已是 Important-skeleton |

### 2.3 v2.1.9 SR-FIX-1 新增符号在 scan-vars 中的现状

| 符号 | 跨度 | 总次数 | 当前层级 | 评估 |
|---|---:|---:|---|---|
| `ensureScenariosNameUniqueByChannelId` | 2 | 6 | 未入表 | A-pair（database.js facade + migrations.js 实现）— 不过 A-share；不过 A-pair 高频门槛（≥15）— **不升格** |
| `applyScenarioBundleImport` | 2 | 3 | 未入表 | A-pair（scenarios-bundle-import.js + main.js）— 不过 A-share；不过 A-pair 高频 — **不升格** |
| `findByChannelAndName` | 2 | 3 | 未入表 | A-pair（scenarios-repository.js + database.js）— **不升格** |
| `runChannelBatch` | 1 | 4 | 未入表 | scenario-dispatcher.js 单文件内部 helper — 单文件高位门槛 ≥ 60 未过 — **不升格** |

**结论**：v2.1.9 SR-FIX-1 新增的 4 个符号**均未达数据门槛**（A-share ≥ 3 / A-pair ≥ 15 / 单文件 ≥ 60）。即使语义门槛（runChannelBatch 是 scenario-dispatcher 内部 facade）可能命中 Important-skeleton 的"跨层入口"语义，但本表"双门槛"约束（CLAUDE.md `rules/important-variables.md` § 升格标准）要求数据门槛 + 语义门槛同时满足才升格。**T02 阶段不升格**；建议留在自动报告侧观察，下次版本 bump 时（v2.1.10 stable / v2.1.11 起算）再评估跨度演化。

### 2.4 v2.1.10 spec §九 预测的"新增变量"现状

spec §九 列出本版预计升格 / 新增的关键变量（Critical / Important-skeleton / Runtime-state / Risk-sensitive）：

| spec §九 预测项 | 当前是否存在 | 现状 |
|---|---|---|
| `acquiring_bill_currency_diff_rows` FK schema（Critical — N4-cont-2 新增 CASCADE）| 是（schema 存在；未加 CASCADE）| 待 Phase 5 实施时升格 |
| `runAllScenarios` / scenario-dispatcher（Critical — 已存）| 是 | 已是 Critical；A3 worker 化范畴扩待 Phase 1 完成后回写 review 要点 |
| `IDLE_CLEANUP_MS` + `lastUserActivityTs` + `setupIdleCleanupTimer`（Important-skeleton）| 是（已是 Important-skeleton + Runtime-state）| A3 改造 idle timer 协调待 Phase 2 实施完成后回写 |
| `runCheckWorkerPool` / `workerInstance`（Important-skeleton — 新建）| **否**（待 Phase 1 T07 新建）| Phase 1 完成时一并升格入表 |
| `serializeError` / `deserializeError`（Important-skeleton — 新建）| **否**（待 Phase 1 T08 新建）| Phase 1 完成时一并升格 |
| `bill_imports.raw_json`（Risk-sensitive — v0.2 新增"对账成功老行可被 NULL 清空"语义）| 是（已是 Risk-sensitive；v2.1.8 N4 升格）| Phase 4 完成后 review 要点回写 v0.2 raw_json NULL 语义 |
| `n4_cont_2_diff_rows_cascade_migrated` settings（Runtime-state — 新建）| **否**（Phase 5 migration 完成时一并入表）| 待 Phase 5 |
| `acquiring_bill_raw_json_retention_days` settings（v0.2 单 key — Runtime-state — 新建）| **否**（Phase 4 T22 完成时一并入表）| 待 Phase 4 |
| `clearStaleSuccessfulRawJson`（Runtime-state — 新建）| **否**（Phase 4 T23 新建）| 待 Phase 4 |

**结论**：spec §九 列出的"新增"变量均尚未存在（v2.1.10 src/ 改动 = 0）；与"待 Phase 1-5 实施时一并升格入表"预测一致。

---

## 三、check:vars 输出

### 3.1 执行结果

```
$ npm run check:vars

> bank-bill-excel-tool@2.1.10-beta.1 check:vars
> node scripts/check-vars.js

[check-vars] src/ 下无改动（HEAD + working tree）。跳过。
```

**符合预期**：v2.1.10 分支启用以来未触碰 src/（仅 docs/ + package.json bump），故 0 Critical 命中。

### 3.2 v2.1.10 起点命中基线

| 层级 | 命中数 |
|---|---:|
| Critical | 0 |
| Important-skeleton | 0 |
| Runtime-state | 0 |
| Risk-sensitive | 0 |
| Minor | 0 |

---

## 四、升格建议（T02 仅观察 — 不修改 rules/）

### 4.1 不建议升格（v2.1.9 SR-FIX-1 4 符号）

- `ensureScenariosNameUniqueByChannelId` / `applyScenarioBundleImport` / `findByChannelAndName` / `runChannelBatch`：数据门槛未过（A-pair / 单文件 helper）。
- 建议：下次版本 bump 时（v2.1.10 stable / v2.1.11）再次评估跨度演化；当前留在自动报告。

### 4.2 建议人工评估（待用户审批）

- `appendModuleLog`：A-share 跨 10 文件 + 总次数 39 + v2.1.9 SR-log-1 范式化日志门面 — 语义命中 Important-skeleton "跨层门面 / 入口"；**建议**：等用户审批是否升格 Important-skeleton（避免 agent 自行修改 rules/）。
- 备注：本条不在 T02 强制范围；仅供 PM/team-lead 知会。

### 4.3 当前不修改 rules/important-variables.md

按指令"不要修改 rules/important-variables.md（升格评估留给后续步骤评估）"。

---

## 五、附录：当前 v2.1.10 git 状态

```
当前分支：v2.1.10
package.json.version：2.1.10-beta.1
最近提交（v2.1.10 内）：
  912481a [v2.1.10] docs(reverse-sync): D25/D26/D27 拍板 + N4-cont-1 重大方案变更（v0.1 → v0.2）
  b599aea [v2.1.10] docs(pm): 四件套 v0.1 + bump 2.1.10-beta.1
src/ 改动数：0（git diff main..v2.1.10 -- src/ 为空）
```

---

## 六、产出文件清单

| 文件 | 用途 |
|---|---|
| `docs/analysis/var-reference-stats.md` | 自动生成 — 158 A-share / 269 A-pair / 405 A-local / 427 B-合计 |
| `docs/analysis/var-reference-stats.json` | 自动生成 — 同上 JSON 版 |
| `docs/analysis/v2.1.10-baseline-t02.md` | 本报告 |

---

## 七、下一步（T03 / T04）

- T03：worker_threads POC 脚本 `scripts/poc/v2.1.10-a3-worker-threads.js`
- T04：utilityProcess POC 脚本 `scripts/poc/v2.1.10-a3-utility-process.js`
- T05：POC 报告汇总 + D23 拍板建议（由主线程处理）

T02 完成；进入 T03。
