# PR #26（待 merge）

- **URL**：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/26
- **分支**：`v2.0.0` → `main`
- **版本**：`2.0.0-beta.1` → `2.0.0-beta.2`
- **状态**：待 merge
- **integrated**：false

---

## Summary

新增 **页面风格切换**（`Clear` / `General` 二选一），默认 Clear。设计稿来自 `Clear/` 38 份 HTML（Claude Design）。

整个迭代分 6 阶段实施：
1. **数据底座 + 版本号 bump** — SQLite `app_settings.ui_style` 字段 + IPC + preload API
2. **HTML 结构对齐 Clear** — `index.html` 基线整体重写
3. **双风格 CSS 切换机制（D15 简化方案）** — 三 link 切换 + 5 类条件渲染节点退化 + D16 风格-背景色联动
4. **UI 切换器 + 提醒框** — 调色板「切换页面风格」下拉 + 二次确认 + 即时切换
5. **dialog factory 双套适配** — alert/confirm/manager/account-mapping/big-account/bill-split/Pending 系列等全套对话框 Clear 视觉重写
6. **preview 双风格适配** — 4 个 render 脚本支持 `APP_PREVIEW_STYLE` env + 双风格 35×2 截图全量产出

49 commits，+12502 / -868（136 files）。

---

## ⚠️ 关联功能 review（check-vars 自动生成）

本次改动触及以下重要变量，请针对性验证：

- **Runtime-state**: `state` (新增 `state.uiStyle`), `elements` (新增 palette style select 引用)
  - review: 验证风格切换 → state 更新 → DOM 引用一致；pendingStatusBox 子节点写法保留 SVG status-spark
- **Risk-sensitive**: `ensureUiStyleDefault` (新增 schema migration)
  - review: 升级路径（无 ui_style key → 自动写 'Clear'）+ 重启加载持久化值

**必跑**：
- [x] `npm run smoke`（多次 pass，含双风格各跑一次）
- [x] P0-2 beta.1 升级到 beta.2（ui_style 自动写 Clear）
- [x] P0-4 / P0-5 风格双向切换 + DB 持久化
- [x] P0-8 关闭重启风格保持
- [x] P0-10 猫猫 GIF 跨风格

---

## 关键决策

- **D15**（reverse sync）：CSS 隔离机制简化 — 放弃 PRD 原方案的"全 selector 加 `body[data-style]` 前缀"双保险，改为三 link `.disabled` 切换实现 cascade 完全隔离，工作量减少 ~80% 安全等价
- **D16**（reverse sync）：风格-背景色联动 — 仅当当前色是"另一风格默认色"（魔法值场景）才同步，不覆盖用户自定义颜色
- **D8**：猫猫 GIF 跨两风格保留
- **D9**：风格切换走二次确认（弹 confirm dialog）
- **D10**：取消时下拉值回滚到原风格

---

## 文档同步

- `CHANGELOG.md` — beta.2 6 阶段 + 实施记录文件
- `docs/VERSION_FEATURE_HISTORY.md` — 新增/变更/移除三段
- `docs/USER_GUIDE.md` §二「页面风格」— Clear/General 切换 + 持久化 + 背景色联动说明
- `docs/iterations/v2.0.0-beta.2/PRD-v2.0.0-beta.2.md` §十六 — 6 个阶段 commit 引用 + 关键决策落点 + check-vars 验收摘要

## Test plan

- [x] `npm run smoke`（双风格）
- [x] `npm run preview:all` Clear 35 张
- [x] `APP_PREVIEW_STYLE=general npm run preview:all` General 35 张
- [x] PRD §13.1 P0-1 ~ P0-10 手动场景
- [ ] 待 reviewer 在 app 里走一遍真实业务流程（导入文件 / 模板管理 / 账户映射 / Pending 月度对账）确认双风格视觉与原版无功能差异
