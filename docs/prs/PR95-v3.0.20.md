---
pr: 95
version: v3.0.20
merged: 2026-07-20 (1142f7e)
integrated: true
released: false
prd: docs/iterations/v3.0.20/PRD-v3.0.20.md
---

# [v3.0.20] 修复主页面同类要素垂直对齐

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/95 ｜ 分支 `codex/v3.0.20-main-panel-alignment` → `main`。

## 一、交付内容

1. 10 个主模块状态框增加统一内容层，星形图标与单行或多行文字按内容中心对齐。
2. 星形图标固定为 14x14px 并改为块级 SVG，消除行内基线偏移。
3. “模式 / BU / 对账场景 / 账单类别 / 场景”五组标签与下拉框统一使用 48px 垂直轨道。
4. 资金对账状态框保持原 176px 跨行高度，24 行长文案可完整滚动且不撑高面板。
5. 新开账户状态框、现有水平位置、按钮间距、业务流程、IPC、数据库和 Excel 输出保持不变。
6. 新增可复跑 Electron 几何验收，并接入 Windows PR 与 Release workflow。

## 二、验证与 Review

- `npm run release-check`：lint、smoke、unit 3702/3702、integration 1955/1955 全部通过。
- `npm run verify:main-panel-alignment`：双尺寸 x DPR `1 / 1.25 / 1.5`，6/6 PASS。
- 单行/三行状态中心差最大约 `0.004 CSS px`，5 组标签/控件顶部、高度和中心差均为 `0`。
- 10 个目标模块 preview 已生成并逐张检查；重复入金默认文案由运行时脚本再次断言。
- `npm run scan:vars` 与 `npm run check:vars -- --include-minor` 完成，无重要变量命中。
- GitHub Windows workflow run `29733232198` 通过，review threads 为 0。
- 第一轮 review 的 P2/P3/P4 均已处理或按既定 merge 后归档顺序收口；最终 PM 与 Development 复审均为 P0-P4 Finding 0。

## 三、合并与发布

- PR #95 于 2026-07-20 以 merge commit `1142f7e` 合入 `main`。
- 远程和本地开发分支已删除，用户原有无关未跟踪文件保持不动。
- `v3.0.20` tag 与 GitHub Release 尚未创建；发布 workflow、在线资产和匿名 feed 证据将在发布完成后补录。
- 合并归档后的最终 `main` 已重新通过 `npm ci`、完整 release-check、六组合几何门禁和变量门禁。
- Windows 安装版 100% / 125% / 150% 最终字形观感与升级数据保留仍需发布后人工 canary。
