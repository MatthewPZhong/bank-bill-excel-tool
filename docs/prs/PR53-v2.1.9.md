---
integrated: true
pr_number: 53
pr_url: https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/53
version: 2.1.9
created_at: 2026-05-27
---

# v2.1.9 α PR #53（已提交）

## Summary

v2.1.8 之后 1 轮迭代（α 范围），**9 项主题**：N5（银行渠道区分场景 — 🔴 资金红线 + DB schema 破坏性 migration）+ N6（状态框换行修复）+ N7（场景模板按渠道导入/导出 — 新 bundle 类型）+ SR-backup-1（sqlite VACUUM INTO 备份基建）+ G1-cont（单元测试 37 文件全量铺）+ SR-policy-1（integration-runner 清单自动同步）+ N1-settings（idle 30min 阈值 settings 化）+ N4 重构（migration 备份切到新 API）+ SR-log-1（全局告警统一日志化 + JSON Lines）。

⚠️ **1 个🔴破坏性变更**（Sheet 3 主输出撤除 → 独立报表）+ **3 个资金红线护栏**（N5 双维 dispatcher + Sheet 3 输出契约 + DB schema migration）。

## 主题完成度

| 主题 | 状态 | 关键产出 |
|---|---|---|
| **N5** 银行渠道区分场景 | ✅ | channels 表 + scenarios.channel_id FK + 双维 dispatcher + 渠道 CRUD + 转移/批量 + Sheet 3 拆出独立报表 |
| **N6** 状态框换行修复 | ✅ | renderer.js 2 行删 `\n`（D18=a 仅银行对账单）；其他 5 模块零外溢 |
| **N7** 场景模板按渠道导入/导出 | ✅ | scenarioBundleVersion=1 独立 bundle + 二阶段确认 + 缺失渠道自动创建 + 同名跳过 |
| **SR-backup-1** sqlite 安全备份 | ✅ | VACUUM INTO（POC 后 spec 反向同步）+ label 白名单 + tmp atomic rename |
| **G1-cont** 单元测试全量铺 | ✅ | 37+ 文件 / **1099 case / 277 suites**（+976 from baseline 123） |
| **SR-policy-1** integration-runner 自动同步 | ✅ | in-place 编辑 policy.md §七 + 时间戳东八区 + stdout |
| **N1-settings** idle 阈值 settings 化（D21=c 修订） | ✅ | settings 表 + 范围 5-180min + 启动期读取 + getter 兜底；**不做 UI**（自扩展的 ⚙️ 按钮已回退，用户用 sqlite3 改 + 重启） |
| **N4 重构（顺带）** | ✅ | ensureBillRawJsonV2Slim 切到 createBackupFn + 备份路径前缀不变 |
| **SR-log-1** 全局告警日志化 | ✅ | preload reportLog + wrapper hijack + 49+ 处 console 改造 + 新日志结构 `logs/{YYYY-MM}/{MM-DD}/{level}.log` JSON Lines + 双写 |

## 测试覆盖（数据待定终态）

| 测试类型 | v2.1.8 基线 | v2.1.9 α 终态 | 增量 |
|---|---|---|---|
| `npm run test:unit` | 123 / 28 suites | **1099 / 277 suites / 0 fail** | **+976 case / +249 suites** |
| `npm run test:integration` | 6 脚本 / 1276 断言 | **7 脚本 / 359 断言**（含 SR-log-1 v2.1.9-sr-log-1.js 35 case） | +1 脚本 / +35 case（注：v2.1.8 计数为整套累计；本版按 release-check 输出实际 PASS） |
| `npm run smoke` | 全绿 | **全绿 0 regression** | 0 退步 |

## 资金红线护栏

| 风险 | 等级 | 缓解 |
|---|---|---|
| Sheet 3 输出契约二次变更（v2.1.8 加 → v2.1.9 拆） | 🔴 资金红线 | USER_GUIDE / CHANGELOG 显著警告；用户后处理脚本需迁移 |
| DB schema 破坏性 migration（channels + FK + backfill） | 🔴 不可逆 | SR-backup-1 前置 VACUUM INTO + 事务回滚 + 标志位幂等 |
| dispatcher 调度模型双维改造 | 🔴 对账契约 | spec §2.4 first-match-wins 不变量保留 + 5 矩阵 unit case 全覆盖 + smoke 0 regression |

## ⚠️ 升级提示

1. **首次启动自动备份 + N5 migration**：`<userData>/backups/tool-data-bak-pre-N5-<timestamp>.sqlite`；现有 scenarios 全部归到「通用」渠道（id=1）
2. **主输出 Sheet 3 撤除**（v2.1.8→v2.1.9 二次变更）：迁移到 `error-reports/{date}/命中场景行-{原文件 basename}-{timestamp}.xlsx`
3. **N7 场景配置导出导入**：跨环境分享 + 缺失渠道弹框确认
4. **N1-settings idle 阈值** （D21=c 无 UI）：sqlite3 `UPDATE app_settings SET setting_value=... WHERE setting_key='acquiring_bill_idle_cleanup_minutes';` + 重启生效（默认 30 / 范围 5-180 / 非法值自动回退默认）
5. **SR-log-1 日志位置**：`logs/{YYYY-MM}/{MM-DD}/{level}.log`（JSON Lines）+ 旧 `app_activity_log.txt` 双写

## α/β 拆分（用户拍板）

- **v2.1.9 α**（本 PR）：9 主题
- **v2.1.10 β**（α merge 后立即启动）：A3 worker 跨进程化 + A4 SQL chunked + N4-cont-1 raw_json 治理 + N4-cont-2 FK CASCADE 改造
- **v2.1.11+ 继续延期**：F5-cont（C4 ILP 重写）/ N5-channels-scale（虚拟滚动评估）

## ⚠️ 关联功能 review（check-vars 已跑 — 2026-05-27 实际命中）

`npm run check:vars` 输出（共 23 命中）：

- 🔴 **Critical（3）**：`findBestAmountSubset` / **`runAllScenarios`**（N5 双维改造扩展） / **`unmatchedRows`**（dispatcher 路径 v2.1.9 行 metadata 扩展 + N5 fallback）
- 🟡 **Important-skeleton（5）**：`enabled_modules` / `ipcRenderer` / `saveMappings` / `settingsRepository`（N1-settings 扩展）/ `setupIdleCleanupTimer`（N1-settings settings 化）
- 🟢 **Runtime-state（7）**：`AppDatabase` / `app` / `dialog` / `elements` / `setStatus`（SR-log-1 wrapper hijack）/ `state` / `updateStatusBox`（SR-log-1 wrapper hijack）
- 🟢 **Risk-sensitive（6）**：`cleanup_pending` / `displayIndex`（N3-1 + N5 复用）/ `ensureBillRawJsonV2Slim`（N4 重构签名变更）/ `hasColumn` / `hitScenarios`（N5 metadata 扩展） / `writeBankStatementOutput`
- Minor 2（不强制列）

**review 结论**：所有 Critical / Important-skeleton 命中均在 v2.1.9 spec / PRD §四 N5 + §七-十一 多 SR / G1 章节内有明确变更说明；Runtime-state / Risk-sensitive 命中为合理扩展或派生；无超出 spec 范围的意外触及。

## 完整变量影响（spec 阶段预计）

| 层级 | 变量 | 新增/变更 |
|---|---|---|
| 🔴 Critical | `channels` 表 + `scenarios.channel_id` | 新增（N5） |
| 🔴 Critical | `runAllScenarios` 双维调度 | 扩展 + 5 矩阵 unit case 覆盖（N5） |
| 🔴 Critical | `bill_imports.raw_json` 内容契约 | 不变（N4 重构仅改备份方式） |
| 🟡 Important-skeleton | `_hitChannelKey` / `_matchStatus` / `_matchedChannelId` / `_fallbackChannelId` | 新增 metadata（N5） |
| 🟡 Important-skeleton | `SUPPORTED_SCENARIO_BUNDLE_VERSION = 1` | 新增（N7） |
| 🟡 Important-skeleton | `createBackup` / `createBackupFn` | 新增基建（SR-backup-1） |
| 🟡 Important-skeleton | `desktopApi.reportLog` / `app:report-log` | 新增 IPC（SR-log-1） |
| 🟢 Runtime-state | `appSettingsBtn` / `acquiring_bill_idle_cleanup_minutes` | 新增（N1-settings） |

## 关联文档

- `CHANGELOG.md` v2.1.9 章节
- `docs/VERSION_FEATURE_HISTORY.md` v2.1.9 段
- `docs/USER_GUIDE.md` 七、v2.1.9 新增能力 7.1-7.6
- `docs/iterations/v2.1.9/*.md` 五件套 v0.5-0.8
- `docs/iterations/v2.1.10/backlog.md` v0.1（β 范围）
- `rules/integration-test-policy.md §七` 自动同步（SR-policy-1）
- `rules/important-variables.md` v12 升格（待 Phase 9 T37 check-vars 跑后定稿）

## Test plan

- [ ] 用户拉新分支测验（按 `docs/iterations/v2.1.9/manual-test-checklist.md` v0.4 含 §八.9 SR-FIX-1 8 项手测）
- [ ] 老库升级路径验证（v2.1.8 库 → v2.1.9 N5 migration + scenarios.name UNIQUE migration）
- [ ] N7 跨环境 bundle 互通验证（含跨 channel 同名场景导入路径）
- [ ] SR-log-1 日志写入路径 + JSON Lines 格式验证
- [ ] N6 状态框冒号后单换行（仅银行对账单）+ 其他 5 模块零外溢回归
- [ ] **SR-FIX-1 C3 1v1 红线手测**（spec §16.2 / manual-test-checklist.md §8.9.2）：「工商-上海」渠道下 C3 场景 + 2 行 bank 同金额 + 2 行 gw 同金额 → 独立报表两行 bank 对应不同 gw 行（不共费）
- [ ] **SR-FIX-1 C2 笛卡尔配对**（manual-test-checklist.md §8.9.3）：「工商-上海」渠道下 C2 场景 + 3+ 行 bank 含 leftType/rightType 配对 → 命中数 > 0
- [ ] **SR-FIX-1 跨 channel 同名场景**（manual-test-checklist.md §8.9.4）：通用 + 工商-上海 同时新增「对账场景」名都成功；同 channel 同名拒绝
- [ ] release-check（smoke + unit + integration）全绿

---

## SR-FIX-1 round 2 — Codex 自动 review 修复（合并前补丁）

PR push 后 Codex 自动 review 抓 5 个 finding（2 P1 🔴 + 2 P2 + 1 P3）。**全部合并前修**（spec §16.3.2-16.3.4 reverse sync）：

| # | 等级 | finding | 修复 |
|---|---|---|---|
| **F1** | 🔴 P1 | createScenario 漏写 channel_id → UI 新建场景落 NULL → dispatcher 永远不命中（v2.1.9 N5 核心功能失效） | scenariosRepo.createScenario 加 channelId 入参（缺省 1 + 老 schema 兼容）+ UI 完成新建附 state.activeScenarioChannelId + 5 unit case + 集成 Case E 端到端 |
| **F2** | 🔴 P1 | applyScenarioBundleImport 仍用全表 name 查重 → N7 跨渠道同名 import 错误跳过（违反 D39 + spec §6.3.2） | main.js:419 改用 database.findScenarioByChannelAndName + database.js 加 facade + 集成 Case F |
| **F3** | 🟡 P2 | batchDelete 拒删内置 vs deleteScenario 允许 + USER_GUIDE 说可删 → 不一致 | batchDelete 移除 is_builtin 检查 + unit/IPC case 同步修订 |
| **F4** | 🟡 P2 | package-lock.json 顶部 2.1.8 ≠ package.json 2.1.9 | npm install --package-lock-only 同步 3 处 version |
| **F5** | 🟢 P3 | tasks.md:246 trailing whitespace + dispatcher.test.js:1137 EOF 多余空行 | 直接删 |

### 修复后测试

| 测试 | round 1 | round 2 |
|---|---|---|
| `npm run test:unit` | 1140 | **1145**（+5 F1）|
| `npm run test:integration` | 595 / 10 脚本 | **614 / 10 脚本**（v2.1.9-sr-fix-1-coverage.js 98 → 117，新增 Case E + Case F）|
| `npm run smoke` | 全过 | 全过 0 regression |
| `git diff --check origin/main...HEAD` | 不干净 | working tree 0 warning |

### 复盘 — SR-FIX-1 round 1 为什么漏抓 F1/F2

**F1**：round 1 集成 Case C 1.4 模拟 N7 import 用 `createScenario + transferScenarios` 串联（注释「scenariosRepo.createScenario 不接收 channelId 入参，依生产 IPC 路径用 transferScenarios 显式补」）— 但「UI 新增场景」路径没有 transferScenarios 补救。没写端到端「UI 新建场景 → dispatcher 调度」case。
**F2**：round 1 Case C 测了 bundle parse 防御 + scenariosRepo 层验证，但没测 main.js applyScenarioBundleImport 完整 import 逻辑。

Codex 用最小脚本端到端测到了：DB 原始 `channel_id=NULL` + `listByChannelIdAndCategory(1, ...)` 返 0。

---

## SR-FIX-1 修复（合并前补丁 — 2026-05-27 self-review 闭环）

PR 提交后 self-review 发现 5 个 🔴 Critical + 9 Important + 5 Minor finding（详 `docs/iterations/v2.1.9/spec.md §十六` 完整设计）。Critical 5 项中 #1-#4 涉及资金红线，**合并前**已用 F1 方案修复。

### 修复 finding 清单

| # | 等级 | finding | 修复 |
|---|---|---|---|
| **SR1 #1** | 🔴 资金红线 | C3 dispatcher per-row 单调破坏 v2.1.7 F2 1v1 红线（多 bank 行共费同 gw 行） | dispatcher 改 per-channel batch（新增 `runChannelBatch` helper + 重写 `runDualDimensionDispatch` Step 1-5） |
| **SR1 #2** | 🔴 资金红线 | C2 笛卡尔配对在 per-row 入参下永不命中（leftRows / rightRows 必空） | 同上 per-channel batch 修复，C2 收到完整 unlocked rows 集 |
| **SR1 #3** | 🔴 spec 冲突 | scenarios.name 全表 UNIQUE 与 spec §6.3.2「channel 内同名跳过」冲突 | 新增 `ensureScenariosNameUniqueByChannelId` migration（VACUUM INTO 备份 + 冲突预检 + 事务 swap rebuild + 标志位）；UNIQUE 改 (channel_id, name) 复合；scenarios-repository 新增 `isScenarioNameUniqueError` 兼容新旧错误 + `findByChannelAndName` |
| **SR1 #4** | 🔴 测试盲区 | dispatcher 既有 30+ 双维 case 全用 C1，C2/C3 双维路径 0 覆盖 | dispatcher.test.js 新增 15 case（spec §16.4 case 1-15）+ scenarios-repository.test.js 新增 5 case（R1-R3 + 2 migration 幂等）= 20 case；case 2 严格 `['GW-A','GW-B']` 不共费断言 |
| **SR1 #5** | 🟡 spec 滞后 | spec §5.2「匹配渠道」列描述未同步 D16 (a)→(b) 修订 | spec §5.2 + USER_GUIDE §1.4 同步入文（命中专属→`${name}-${ownerLocation}` / 命中通用→`通用` / 未命中→空） |

### SR2 Important (9) + SR3 Minor (5) — 留 v2.1.10 收尾

不影响 PR #53 合并：所有 SR2/SR3 项为代码风格 / 注释清理 / 边界 UX 改进 / 文档调整等，不涉及资金红线。

### 修复后测试增量

| 测试 | 修复前 | 修复后 | 变化 |
|---|---|---|---|
| `npm run test:unit` | 1099 | **1139 / 0 fail** | +40（dispatcher.test.js +15 - 9 = +6 + scenarios-repository.test.js +5 + migration ×2 + 其他场景适配 ≈ +29） |
| `npm run test:integration` | 9 脚本 | **497 / 497 / 9 脚本** | 0 regression（含 v2.1.9-n5-end-to-end 68 + v2.1.9-n5-migration 67） |
| `npm run smoke` | 全过 | 全过 0 regression | 0 退步 |
| `npm run check:vars` | 23 命中 | **0 新增 Critical** | Critical 1（unmatchedRows）+ Risk-sensitive 3 全部为现有变量正常 hit |

### 修复文件清单（6 个）

- `src/main-process/scenario-dispatcher.js`（重写 runDualDimensionDispatch + 新增 runChannelBatch helper + 删除 dispatchSingleRow / firstMatchWinsForRow）
- `src/backend/database/migrations.js`（新增 `ensureScenariosNameUniqueByChannelId` 函数 + 8 status 状态机 + 调用顺序保证）
- `src/backend/database.js`（facade `ensureScenariosNameUniqueByChannelId()` + 启动期在 N5 后调用）
- `src/backend/database/scenarios-repository.js`（新增 `isScenarioNameUniqueError` + `findByChannelAndName` + createScenario/updateScenario 错误捕获升级）
- `tests/unit/main-process/scenario-dispatcher.test.js`（+15 case = case 1-15 spec §16.4 矩阵；scenarioHitCount 语义回归 legacy）
- `tests/unit/backend/database/scenarios-repository.test.js`（+5 case = R1-R3 + 2 migration 幂等）

### spec / docs 同步（4 个）

- `docs/iterations/v2.1.9/spec.md` v0.9（§1.6 加 D38-D40 + §2.1/§2.4/§3.1/§5.2/§6.3.2 Reverse Sync marker + 新增 §十六 完整修复设计 / §十六→§十七 重号）
- `docs/iterations/v2.1.9/tasks.md` v0.4（Phase 10 加 T41-T44）
- `docs/iterations/v2.1.9/manual-test-checklist.md` v0.4（§八.9 加 SR-FIX-1 8 项手测）
- `CHANGELOG.md` v2.1.9 章节加「SR-FIX-1 修复」段
- `docs/USER_GUIDE.md` §1.4 「匹配渠道」描述同步 D16=b + 加「⚠️ v2.1.9 SR-FIX-1 已知边界」段
- 本文件（PR53-v2.1.9.md）追加 SR-FIX-1 修复段
