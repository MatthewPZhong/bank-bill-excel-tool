---
pr: 35
title: "[v2.1.0-beta.1 PR-A] feat: 单据对账ReconID修复模块骨架（DB + UI + IPC）"
branch: v2.1.0
target: main
status: merged
integrated: true
merge_commit: 6e5ebaf6fb2b058353a1c778732b82597b2d0a88
merged_at: 2026-04-30T04:01:04Z
---

# PR #35 — [v2.1.0-beta.1 PR-A] 单据对账ReconID修复模块骨架

## 范围

v2.1.0-beta.1 迭代第 1 个 PR（共 4 个）。新增第 5 业务模块「单据对账ReconID修复」的**骨架 only**：

- DB schema（scenarios CHECK 约束扩 4 值，含 'recon-id-fix'）
- 主页面模块下拉 + 第 5 模块面板
- 场景管理 CRUD（C4 类）
- 4 个 IPC handlers 占位（import/run/export 返回 not-implemented；session-status 实装）
- 主面板"场景"选择下拉

**不含**对账匹配算法、识读规律算法、文件 IO（PR-B/C 范围）。

## 改动清单

| 类别 | 文件 | 说明 |
|---|---|---|
| **DB 迁移** | `src/backend/database/migrations.js` | `ensureScenariosCategoryReconIdFix`（CHECK 3→4 值，幂等 + RENAME-CREATE-INSERT-DROP-COMMIT） |
| | `src/backend/database/scenarios-repository.js` | VALID_CATEGORIES +1 |
| | `src/backend/database/settings-repository.js` | CURRENT_MODULE_VALID +1 |
| | `src/backend/database.js` | 注册迁移调用 |
| **前端 DOM** | `index.html` | 主页面模块下拉第 5 项 + 主面板 fork（场景下拉 + 4 按钮 + statusBox） |
| | `src/styles-gemini-extra.css` | `.recon-id-fix-board` 样式 |
| **渲染层** | `src/renderer.js` | MODULES +1 / state +5 字段 / elements +6 项 / setCurrentModule 分支 / reloadReconIdFixScenarios + 4 时机刷新 |
| | `src/renderer-dialogs.js` | createScenarioConfigDialogC4（5 行布局 + 互斥 + 联动 + commonId/SubBizType + 识读按钮 disabled 占位）+ 类别选择对话框扩四选一 + 场景管理路由 |
| | `src/renderer-previews.js` | 3 个新 preview 入口 |
| **IPC** | `src/main.js` | 4 个 `recon-id-fix:*` IPC handlers（占位）+ scenarios:* 4 入口资金红线清 reconIdFixResult |
| | `src/preload.js` | `desktopApi.reconIdFix.*` + `BUSINESS_BILL_FIELDS` / `OPPONENT_BILL_FIELDS` 常量 |
| **smoke** | `scripts/smoke/migrations-recon-id-fix.js` | 6 用例（空库 / 老库 builtin 无损 / 三连幂等 / 新 schema no-op / VALID_CATEGORIES / CHECK 拦截非法） |
| | `scripts/smoke/recon-id-fix-scenario-ipc.js` | 11 用例（CRUD + 资金红线分流清缓存 + UNIQUE 拦截 + round 3 P2 4 分流验证 T8/T9/T10/T11） |
| | `scripts/smoke-test.js` / `scripts/smoke/scenarios-repository.js` | 入口注册 |
| **preview** | `docs/previews/recon-id-fix-panel.png` 等 3 个新 + 6 个 M | 第 5 模块视觉验证 |
| **docs** | `docs/iterations/v2.1.0-beta.1/{PRD,spec,tasks,log}.md` | 三件套 + log（4 PR 拆分 + 4 决策回写） |
| **fixture** | `samples/单据对账导出不平.xlsx` / `单据对账导出不平-对平例子.xlsx` | 入库（PR-B/C 算法 fixture） |
| **build** | `package.json` | 3 个新 preview script |

合计：**31 文件改动**（25 + 6 docs/samples），**+3994 / -17** 行。

## 验收证据

- **smoke 181/181 PASS**（新增 18 个：6 migrations + 11 IPC + 1 repository P3-B 拒绝改 category）
- **5 模块切换路径无破坏**（main-page.png / module-switcher-open.png 已 preview 验证）
- **C4 dialog 5 行布局**：preview `scenario-config-c4.png` + `scenario-config-c4-both.png`
- **资金红线第一层防御**：scenarios:create/update/delete/toggle 4 IPC 已追加 `reconIdFixResult = null`（export 端 snapshot 校验在 PR-B 加上）

## 关联 spec 文档

- `docs/iterations/v2.1.0-beta.1/PRD-v2.1.0-beta.1.md`（840 行，§三 模块结构 / §六 前端 / §十 资金红线）
- `docs/iterations/v2.1.0-beta.1/spec.md`（745 行，§一 文件清单 / §二 SQLite 迁移 / §三 IPC / §七 state）
- `docs/iterations/v2.1.0-beta.1/tasks.md`（A1-A9 9 个 task，每条含验收证据）

## ⚠️ 关联功能 review（check-vars 自动生成）

本次改动触及以下重要变量，已逐项自查：

### Important-skeleton 命中

- **`ipcRenderer`** — 新增 4 个 IPC channel `recon-id-fix:{import,run,export,sessionStatus}`；preload 已暴露 `desktopApi.reconIdFix.*`；main 端均已 `ipcMain.handle` 注册 ✅

### Runtime-state 命中

- **`MODULES`** — 模块枚举 4 → 5 项（reconIdFix）；`setCurrentModule` 已加分支；模块面板 hidden 切换逻辑通过；`info.currentModule` 持久化经 `CURRENT_MODULE_VALID` 校验 ✅
- **`state`** — 新增 5 字段（reconIdFixSession / Result / Export / SelectedScenarioId / Scenarios）；UI 渲染依赖 updateReconIdFixUi；scenario CRUD 关闭后调 reloadReconIdFixScenarios 同步 ✅
- **`elements`** — 新增 6 个 DOM 引用（5 主面板 + 1 reconIdFixScenarioSelect）；初始化在 elements 顶层；setCurrentModule 用 elements.reconIdFixModulePanel 控制 hidden ✅
- **`dialog`** — 命中均为 renderer 层 dialog factory（`createScenarioConfigDialogC4`），非 `src/main.js` 的 `require('electron').dialog`；create/edit/view 三态 view 时所有控件 disabled；保存走 createScenarioConfirmDetailDialog ✅

**无 Critical / Risk-sensitive 命中**（迁移函数 ensureScenariosCategoryReconIdFix 已通过 6 用例 smoke 覆盖：含老库 3 builtin 无损迁移 + CHECK 拦截）。

## ⚠️ 风险显式提醒（CLAUDE.md 规则 7）

- **DB 迁移 / CHECK 约束变更**：v2.0.0-beta.3 老库（含 3 builtin scenarios）必须无损迁移；migration 已包事务 + 幂等检查 sqlite_master.sql；smoke 已覆盖
- **资金红线（spec §十）**：scenarios CRUD 4 入口已落第一层防御（清 reconIdFixResult）；export 端 snapshot 校验是 PR-B 范围，本 PR handler 返回 not-implemented 无 stale 数据风险
- **不含算法**：本 PR 仅骨架；对账匹配 / 识读规律 / 文件 IO 在 PR-B/C 实施

## 必跑（用户手测已通过）

- [x] `npm run smoke` — 181/181
- [x] 5 模块切换路径无破坏
- [x] 类别选择对话框四选一展开能选第 4 项
- [x] C4 dialog 5 行所有控件可填、互斥逻辑工作
- [x] 保存后场景管理表显示"单据对账修复"

## 下一步

PR #35 合并后启动 PR-B（对账引擎 7+5 规则 + 1v1/1v多/多v1 三阶匹配）。
