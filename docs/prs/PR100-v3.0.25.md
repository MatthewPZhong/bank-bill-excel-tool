---
pr: 100
version: v3.0.25
merged: 2026-07-23 (047275f)
integrated: true
released: 2026-07-23 (v3.0.25)
prd: docs/iterations/v3.0.25/PRD-v3.0.25.md
---

# [v3.0.25] 设置弹窗、存档策略与平盘文案调整

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/100 ｜ 分支 `codex/v3.0.25-settings-dialog` → `main`。

## 一、交付内容

1. 设置弹窗底部统一为【确认】；进入存档设置后由全局【确认】保存保留期限并关闭，保存失败时保留弹窗与错误反馈。
2. 修复永久保留回显与存档设置异步加载竞态；返回、切页或关闭会失效旧请求，加载完成前禁止确认。
3. 彻底退役网银模板“不存档”：删除 Renderer、Preload、Main IPC 和 Controller 接口，升级启动后将旧排除配置统一归一化为 `[]`。
4. 存档默认保留期改为 60 天，下拉支持 30、60、90、180、365 天和永久；既有合法设置不迁移。
5. 删除指定自动更新与存档说明，统一开关状态字号为 `14px`，并将自动更新内容右边界对齐全局【确认】按钮左缘。
6. 平盘功能第二项显示名称改为“平盘对账数据处理”，内部值 `position-data-info-backfill` 保持不变。

## 二、验证与 Review

- 存档相关定向测试 `36/36 PASS`。
- `npm run release-check`：unit `3818/3818`、42 个 integration 脚本 `1963/1963`，lint 与 smoke 全部通过。
- 设置布局六组合验证 `6/6 PASS`；右边界误差均为 `0px`，开关状态文字为 `14px`。
- `npm run startup:measure`：建窗到可见平均 `98.202ms`，ready-to-show 平均 `167.323ms`。
- `scan:vars` 为 202 个 JS 文件、2320 个顶层声明；ArchiveCenter 四层接口、模板读取依赖和设置读写关联功能已复核。
- GitHub PR #100 workflow run [`29991926713`](https://github.com/MatthewPZhong/bank-bill-excel-tool/actions/runs/29991926713) 通过；最终 self-review 为 P0-P4 Finding 0。

## 三、合并与发布

- PR #100 于 2026-07-23 以 merge commit `047275f` 合入 `main`。
- 无关未跟踪文件保持不动。
- 合并归档后的 `main` 已在干净 `npm ci` 依赖上重新通过 release-check：unit `3818/3818`、42 个 integration 脚本 `1963/1963`，lint 与 smoke 全绿。
- 发布前设置布局和主页面布局均为 `6/6 PASS`；启动建窗到可见平均 `100.985ms`，ready-to-show 平均 `170.123ms`。
- 发布前变量扫描仍为 202 个 JS 文件、2320 个顶层声明，重要变量硬节点因 `src` 无新改动安全跳过。
- 生产依赖仍有既有 7 条 audit 告警（2 moderate、5 high、0 critical）；v3.0.25 未新增生产依赖，该问题继续作为依赖治理 follow-up。
- annotated tag `v3.0.25` 指向 `588eaeb9ab6a209ad810f0d722b1eaee4c01230a`；Windows Release workflow run [`29993821247`](https://github.com/MatthewPZhong/bank-bill-excel-tool/actions/runs/29993821247) 全部通过。
- GitHub Release [`v3.0.25`](https://github.com/MatthewPZhong/bank-bill-excel-tool/releases/tag/v3.0.25) 已成为 latest、非 draft、非 prerelease，包含 Setup、Setup blockmap、portable 和 `latest.yml` 四个资产。
- 资产大小分别为 Setup `99,763,759`、portable `99,266,991`、blockmap `105,442`、`latest.yml` `371` 字节。
- `latest.yml` 公布版本 `3.0.25`、Setup 路径、大小 `99,763,759` 与 SHA-512；其 SHA-256 `942e11810694a809528536eb144899f3d6af8de4268c07c4b16464f6e4b2cb83` 与 GitHub 元数据一致。
- 匿名 Range 回读确认 Setup 与 portable 均返回 HTTP 206，文件头均为 `MZ`。
- 远程与本地开发分支已删除；无关未跟踪文件保持不动。
- Windows 安装版实机字体与缩放复核仍是人工 follow-up；自动几何验证通过不等于实机视觉验收完成。
- 真实 Windows `v3.0.24 → v3.0.25` 生产在线升级 canary、SmartScreen 及 SQLite/设置/导出文件保留仍是公告前人工门禁。
- GitHub `production-release` Environment 当前没有 protection rules，`main` 也未启用 branch protection；服务端发布治理需由仓库管理员按 Runbook 补齐。
