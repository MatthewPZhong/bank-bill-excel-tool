# PR #14 — v1.4.7 后续修复合集

**分支**: v1.4.7
**base**: main
**当前 PR**: #13（已 merged，2026-04-01 10:29 UTC）
**状态**: 部分内容已 merged（通过 #10/#11/#13）；**剩余未提 PR 的内容见下方 ❌ 条目**

> **Merge 状态说明（2026-04-07 核对）**
> 本文件原作为 v1.4.7 后续修复的汇总草稿，实际上大部分条目已通过 #10/#11/#13 merged 到 main。下面每条前的图标含义：
> - ✅ = 已 merged 到 main（通过 PR #10 / #11 / #13）
> - ❌ = **尚未提 PR**，仍只存在于本地 `v1.4.7` 分支，需要真正的 PR #14 推上去

---

## 变动清单

### 日期解析
- ✅ BNI 点号时间格式 `HH.MM.SS`（stripDateTimeSuffix 正则 `:` → `[:.·]`） — merged via #11 (commit `cd6cd2e`)
- ✅ PAB Excel 序列号字符串化（`"46102"` 作为纯数字字符串解析） — merged via #13 (commit `a774c71`)
- ✅ DD-MM/MM-DD 不歧义 fallback（DD-MM 无效时尝试 MM-DD） — merged via #13 (commit `fcb25b8`)
- ✅ YYMMDD 优先于 Excel 序列号（数字字符串检测移到 YYMMDD 之后） — merged via #13 (commit `d7528dc`)

### 空交易拦截
- ✅ 全部账号 0 笔交易时直接报错"导入文件中没有账号存在交易数据" — merged via #13 (commit `7e33ba4`)
- ✅ identifyAccountBlocks 空块 fallback：有 headerBreaks 但全空时返回 `[]`，不创建假块 — merged via #13 (commit `dcf06ea`)

### 大账号对话框
- ✅ remember checkbox 不固定模式下灰显而非隐藏 — merged via #11 (commit `e23c49e`)
- ✅ 搜索关键字切换重置索引 — merged via #11 (commit `6abff40`)
- ✅ 模式切换清空搜索状态 — merged via #11 (commit `adecac7`)
- ✅ 初始化期间禁用交互 — merged via #11 (commit `adecac7`)
- ✅ 报错区分可重试（留在对话框）/ 不可重试（关闭+弹窗） — merged via #13 (commit `90792ed`)
- ✅ applyPrefilledOrder 用 dataset 比较替代 querySelector（避免 CSS 特殊字符报错） — merged via #11 (commit `50c4e48`)

### 新开账户
- ✅ 修复 currencyLabel 未定义（改为 currencySegment） — merged via #13 (commit `2a39e18`)
- ✅ 余额账单日期改为到昨天（系统时间前一天） — merged via #13 (commit `9f7479b`)

### MerchantId 空格
- ✅ 去除账号中间空格（如 `NRA 7101 2023 0223 63` → `NRA71012023022363`） — merged via #13 (commit `78c4a96`)

### dateFormat 后端兼容
- ✅ 保留数据库 date_format 列、配置传递、normalizer dateParseOrder、bundle 导出导入 — merged via #13 (commits `100fd60` / `672b7b0` / `676d943` / `03f84b5`)
- ✅ 删除映射管理对话框的 UI 下拉入口 — merged via #13 (commit `03f84b5`)
- ✅ saveMappings 可选 dateFormat：有值时写入，无值时保留现有 DB 值 — merged via #13 (commit `676d943`)

---

### ❌ CSV 长数字精度丢失（未 merged）
- CSV 改用纯文本解析器（`parseCsvText`），所有值保持字符串，不经过 xlsx 类型推断
- 解决长数字（如 20 位交易流水号）后几位被截断为 0 的问题
- 支持引号包裹、转义引号、CRLF/LF、UTF-8 BOM
- xlsx/xls 文件不受影响，仍用 XLSX 库读取
- **commit**: `4307176` (2026-04-02 15:22 +0800)
- **文件**: `src/backend/file-service/readers.js`（新增 `parseCsvText` 函数 +73 行，`readWorkbookRows` 增加 `.csv` 分流）

### ❌ Currency 映射排除拼接（未 merged）
- Currency 字段不再走 multi-select（新增 `isCurrencyField` 判断，排除"需要拼接字段"选项）
- **commit**: `eb5a09a` (2026-04-03 14:18 +0800)
- **文件**: `src/renderer-dialogs.js`

### ❌ 余额账单所在地取值（未 merged）
- `splitTemplateName` 修改：多个 `-` 时 location 只取第二段（如 `BNI-ID-SG` → `ID`，不再是 `ID-SG`）
- **commit**: `eb5a09a` (2026-04-03 14:18 +0800)
- **文件**: `src/backend/balance-seed-store.js`

### ❌ 映射对话框 dateFormat UI 再次移除（未 merged）
- PR #13 已经移除过一次 dateFormat 下拉，但 v1.4.7 与 main 合并时又从 main 带回来了，再移一次
- 同时移除 `dateFormatSelect` 变量及 `saveMappings` 里的 `dateFormat: dateFormatSelect.value`
- **commit**: `36b24fd` (2026-04-01 19:41 +0800)
- **文件**: `src/renderer-dialogs.js`

> **注**：原草稿里提到的 `505c165` (merge commit) 和 `211dd23` (duplicate `selectionRowsWithEmpty` 声明清理) 经两点 diff 确认已经在 main 里了（通过 PR #13 squash 带走），无需再提。

---

## 涉及文件

**❌ 未 merged 部分实际改动的文件**（`git diff origin/main..v1.4.7 --stat` **两点 diff**）：

```
 src/backend/balance-seed-store.js   |  6 +--
 src/backend/file-service/readers.js | 73 +++++++++++++++++++++++++++++++++++++
 src/renderer-dialogs.js             | 19 ++--------
 3 files changed, 80 insertions(+), 18 deletions(-)
```

> 只有这 3 个文件、净 +62 行改动。之前看到的三点 diff（8 文件 151/52）会把已经被 squash merged 的 commit 全部计入，具有误导性；两点 diff 才是 main 和 v1.4.7 tip 的真实差异。

---

## 测试项

**❌ 未 merged 部分需要测试的项**：

- [ ] 导入含长数字的 CSV → 导出明细 → 数字完整无截断（对应 CSV 精度修复）
- [ ] Currency 映射下拉不包含"需要拼接字段"（对应 Currency 排除）
- [ ] 余额账单所在地：BOC-CN → `CN`，BNI-ID-SG → `ID`（对应 location 取值）
- [ ] 映射管理对话框无日期格式下拉（对应 dateFormat UI 再次移除）

**✅ 已 merged 的测试项**（已通过 #11/#13，这里保留仅作历史参考）：
- [x] BNI-ID 模板导入含点号时间的 xlsx
- [x] PAB-CN 模板导入含 Excel 数字日期的 xlsx
- [x] BOC-CN 导入全空交易 CSV → 提示"没有账号存在交易数据"
- [x] 新开账户余额账单日期到昨天
- [x] 导入含空格账号的 xls → MerchantId 无空格
- [x] 大账号选择对话框：模式切换、搜索、初始化 loading、报错重试

---

## 下一步

1. **review 实际 diff**：`git diff origin/main...v1.4.7` 确认上述 ❌ 条目涵盖全部变动
2. **创建真正的 PR #14**：base `main`, head `v1.4.7`，body 只保留 ❌ 条目
3. **merge 后**：在 main 上打 tag `v1.4.7`，本文件归档到 `docs/iterations/` 或删除
