---
pr: 110
version: v3.1.3
merged: 2026-08-01 (4bb08b54676c9dd826d48c63ec6f7b4f6acf96f1)
integrated: true
released: false
prd: docs/iterations/v3.1.3/PRD-v3.1.3.md
---

# [v3.1.3] 平盘百万级流式导入

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/110 ｜ 分支
`codex/v3.1.3-pr-e-bank-account-ui-release` → `main`。

## 一、交付内容

1. 银行和五类链接原始表迁移到 utility process 流式解析与增量写库，主进程不再持有
   随输入行数增长的完整 workbook、行、主键、冲突和派生集合。
2. 银行整批原子、普通来源逐文件事务、账户确认替换三类边界保持不变；业务数据、
   revision、input proof、checkpoint 和存档证据使用同一提交事实。
3. 普通来源使用完整规范行 `row_hash/sourceRecordKey` 作为技术身份；完全重复折叠，
   同业务主键不同内容全部保留并独立派生与消费。
4. 银行摘要、来源摘要、删除和 FundTransfer 映射重建改为索引聚合或后台分批作业，
   300 万行管理路径保持有界内存。
5. 导入进度、协作取消、磁盘门禁、worker 崩溃恢复和暂存保护链路完整接入；任何证据
   不一致均失败关闭，不回退主进程旧 reader。

## 二、验证与 Review

- 300 万行银行、网关入账、网关出账三份证据均为 `success`、3/3 input proof、
  `quick_check=ok`；main RSS 增量均低于 9 MiB，worker 峰值均低于 1 GiB。
- 真实五文件 1,339,185 行全部提交；同业务主键不同内容独立保留，完全重复折叠 0 条。
- 最终 self-review 定向 `139/139`、parity `54/54`、fault `50/50`，无 P0-P3 Finding。
- PR #110 合并后重新通过 lint、smoke、unit `4454/4454` 和 44 个 integration 脚本
  `2051/2051`。
- 主页面布局 `6/6 PASS`；启动进程总耗时平均 `715.103ms`，ready-to-show 平均
  `194.527ms`。
- `scan:vars` 为 261 个文件、3283 个顶层名称；发布准备未改 `src`，
  `check:vars -- --include-minor` 安全跳过。

## 三、合并与发布准备

- PR #110 已以 merge commit `4bb08b54676c9dd826d48c63ec6f7b4f6acf96f1`
  合入 `main`；实现 head 为 `3185aa343b2c935dafcde093148ac436da4ef193`。
- GitHub 仓库为 PUBLIC；发布准备时 `v3.1.3` tag 和 Release 均不存在。
- macOS `Asia/Shanghai` 首次本地 unit 的 5 个日期夹具受 SheetJS 1899 历史秒级
  时区偏移影响；Windows workflow 同款 Node 22 + UTC 环境全绿。本次不改变业务日期口径。
- `npm audit --omit=dev` 为 7 high、2 moderate、0 critical；依赖升级留作独立治理任务。
- annotated tag、Windows workflow、四个发布资产及公开回读摘要将在发布证据 PR 中补充。
- 首次 tag 触发的 Windows run `30673001316` 在 `release-check` 暴露测试 teardown 的
  SQLite 文件锁与路径规范化问题；构建和发布步骤未执行，未产生 GitHub Release 或公开资产。
  修复只调整跨平台测试资源释放，不改变本 PR 的业务实现。
- ⚠️ Windows 安装版真实导入/取消/文件锁和真实资金逐笔复核仍未完成；技术发布授权
  不等于人工验收通过。
