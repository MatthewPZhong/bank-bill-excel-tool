# PR #28（已 merge）

- **URL**：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/28
- **分支**：`v2.0.0` → `main`
- **版本**：`2.0.0-beta.2`（本次不 bump）
- **状态**：MERGED 2026-04-28（merge commit `52f65f4`）
- **integrated**：true（已回写 PRD §十六 实施记录"附加：PR #28"）

---

## Summary

v2.0.0-beta.3 大迭代（T2 银行对账单处理新模块）启动前的两个独立小改动，按 PR 切分方案 A 抽出来打小 PR：

- **T1 主页面标题改黑**：`index.html` 主标题"网银账单小助手"在 Clear 风格下从 4 色渐变改为纯黑色
- **T3 月度余额账单日期统一月末**：导出表的"账单日期"列统一为目标月末日，不再用 seed 实际记录日

5 src 文件 +12/-14（含 smoke + 回归用例同步），spec 三件套 +215/-0。

---

## ⚠️ 资金红线反转（关键）

T3 反转 v1.5.3 R1 (T1.4) PRD §5.1.3 Q2 决策：

| 时间点 | 决策 | 出处 |
|---|---|---|
| **v1.5.3 R1** | billDate 用 seed 实际记录日（"忠实反映底层数据"） | 旧 `monthly-balance.js:191` 注释明确"Q2 资金红线" |
| **v2.0.0** | billDate 统一为月末日（`targetLastDay`） | 用户业务要求：月度账单日期必须落在月末 |

**新行为**：
- 导出表"账单日期"列**全部显示月末日**（如 2026-02-28）
- 但**余额值**仍取 seed 里"≤ 月末的最近交易日"那条的实际值（如 2026-02-25 那天的余额）
- `pickReason` 仍区分 `'exact'` / `'fallback'` 用于 stats 报告（外观无变化，内部留痕）

---

## 改动文件

| 文件 | 改动 |
|---|---|
| `src/styles-gemini.css` | T1：`.gemini-gradient` 删 4 色渐变，改为 `color: #000` |
| `src/main-process/monthly-balance.js` | T3：行 197 `billDate: chosen.billDate` → `billDate: targetLastDay`；同步更新行 1-12 头注释 + 191 行注释 |
| `scripts/smoke/scenarios.js` | smoke 用例反转：场景 3 (P0-5) + 场景 7 (P0-11) 期望值更新 |
| `scripts/test-v1.5.3-regression.js` | Codex F1：P0-5 fallback 期望值同步反转 `2026-02-28` → `2026-03-31` |
| `docs/previews/main-page.png` + `account-mapping.png` | 重渲（标题已显示黑色） |
| `changes/v2.0.0-pre-beta3-quickwin/` | spec 三件套（spec.md / tasks.md / log.md） |

---

## 关键决策

- **D1**：T1 单点改 `.gemini-gradient { color: #000 }`，删掉渐变三件套；不在 `index.html` 加 inline style，保持 CSS cascade 优势
- **D2**：T3 单点改 `monthly-balance.js:197`，不在 `toBalanceRows` 或 `pickLatestSeedForAccount` 内改（职责分离）
- **D3**：保留 `pickReason` 不变（'exact' / 'fallback' 仍用于 stats 报告）
- **D4**：smoke 场景 3/7 + v1.5.3 回归脚本 P0-5 期望值同步反转，确保完整测试套与新行为对齐

---

## check-vars 评估

`npm run check:vars` 输出"未命中任何重要变量"。手工补判：

- **资金红线**：`monthly-balance.js` 改动是 v1.5.3 R1 资金红线决策的反转，虽然不直接命中表中具体条目（该文件本身未单独登记），但行 1 注释明确称该文件为"资金红线集中点"。已在 spec § 7 详细论证 + PR body 高亮 + smoke / regression 测试覆盖
- **不新增条目**：本次不触发新的 important-variables 升格候选

---

## 文档同步

按 `workflow_docs_update`：发版前才更新文档三件套，中间小功能不更新。本次**不**更新 CHANGELOG / VERSION_FEATURE_HISTORY / USER_GUIDE，留到 v2.0.0 系列发版前统一收口。

---

## Test plan

- [x] `npm run smoke`（含场景 3/7 资金红线回归）
- [x] `node scripts/test-v1.5.3-regression.js`（P0 31/31 通过）
- [x] `npm run preview` + `npm run preview:account`
- [x] T3 后端单元验证（fallback / exact 两种 pickReason 路径）
- [x] T1 视觉验证（preview 截图标题已黑）
- [x] Codex review 处理：F1 (P2) + F2 (P3) 已修复
