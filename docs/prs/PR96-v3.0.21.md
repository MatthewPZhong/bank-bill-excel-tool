---
pr: 96
version: v3.0.21
merged: 2026-07-20 (3c44420e)
integrated: true
released: false
prd: docs/iterations/v3.0.21/PRD-v3.0.21.md
---

# [v3.0.21] 修复 Ach Return 与 DBS-Charge 校验

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/96 ｜ 分支 `codex/v3.0.21-ach-return-dbs-charge` → `main`。

## 一、交付内容

1. Ach Return 退款过滤改为复用 R1 的具体配对，仅过滤配对网关 TradeType 严格等于 `AchReturn` 的银行对象。
2. 同 reconciliationId 的其他银行行不再被这道过滤扩散；`Inbound-VA` 可继续进入退款精准匹配。
3. DBS-Charge 步骤2增加固定 12 类网关 TradeType 白名单，非白名单候选不参与判断。
4. DBS-Charge 在金额币种判断前增加银行 `Credit Amount` 方向守卫，非零方向保持进入步骤2前的 FundType 并输出 warning。
5. R1/R4、DBS 步骤1、IPC、数据库、前端和 Excel 契约保持不变。

## 二、验证与 Review

- 定向单元测试 `220/220 PASS`。
- 隔离工作区 `npm run release-check`：lint、smoke、unit `3716/3716`、integration `1955/1955` 全部通过。
- 受控本地问题样本只读回放确认 `Inbound-VA` 不再阻断后续“精准命中”；业务标识未进入仓库。
- `npm run scan:vars`：纯 3.0.21 树为 `195` 个 JS 文件、`2202` 个顶层名称。
- `check-vars` 只命中 `runRound5RefundOrderBackfill` 和 `STEP2_GW_TRADE_TYPE_WHITELIST` 两个预期 Risk-sensitive 变量，关联 smoke 与资金回归已完成。
- GitHub PR workflow run `29747527965` 通过；最终 self-review 为 P0-P4 Finding 0。

## 三、合并与发布

- PR #96 于 2026-07-20 以 merge commit `3c44420e` 合入 `main`。
- 远程与本地 3.0.21 开发分支已删除，主工作区 3.0.22 未提交改动未被触碰。
- `v3.0.21` tag 与 GitHub Release 尚未创建；发布 workflow、在线资产和匿名 feed 证据将在完成后补录。
- 合并归档后的最终 `main` 已在干净 `npm ci` 依赖上重新通过完整 release-check、六组合 Electron 几何门禁和变量门禁。
- 真实脱敏 DBS 白名单、金额币种、方向 warning 和改值去向的人工逐笔复核仍是发布后 follow-up，不得宣称人工验收通过。
