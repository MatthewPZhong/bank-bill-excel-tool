---
pr: 99
version: v3.0.24
merged: 2026-07-22 (a965c48)
integrated: true
released: 2026-07-22 (v3.0.24)
prd: docs/iterations/v3.0.24/PRD-v3.0.24.md
---

# [v3.0.24] 平盘对账前端与 Payment 多大账号支持

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/99 ｜ 分支 `codex/v3.0.24-position-reconciliation` → `main`。

## 一、交付内容

1. 新增默认位于闲置功能的「平盘对账数据处理」主模块；本版只交付三项功能枚举、五个占位按钮和状态框，不接业务 IPC、数据库或 Excel。
2. 页面沿用对账单修复的两行结构；仅「开始运行」为蓝色主按钮，其他按钮为白色次按钮，「开始运行」与「导出文件」宽度固定为 `140px`。
3. Payment 线下调拨订单回填的 `bigAccount` 扩展为严格中文顿号分隔列表，继续使用原字符串 schema，不新增迁移。
4. R1/R2 按账号与周分桶，R3 兜底同样要求银行大账号与订单收款账号相同；三轮继续共享全局 1:1 消费集合，禁止跨账号回填。
5. 非法历史配置安全 no-op，并通过 `payment-offline-invalid-big-account-config` 进入可见错误报告。

## 二、验证与 Review

- Payment 定向测试 `89/89 PASS`；模块与设置定向测试 `68/68 PASS`。
- `npm run release-check`：unit `3813/3813`、42 个 integration 脚本 `1963/1963`，lint 与 smoke 全部通过。
- 主页面几何门禁 `6/6 PASS`；`scan:vars` 为 202 个 JS 文件、2324 个顶层声明。
- `check-vars -- --include-minor` 命中 Runtime-state `MODULES/elements`，模块全集、默认列表和占位按钮旁路均已复核；Payment 多账号按 Risk-sensitive 资金口径完成代码与自动回归。
- GitHub PR #99 smoke workflow 通过；最终 self-review 为 P0-P4 Finding 0。

## 三、合并与发布

- PR #99 于 2026-07-22 以 merge commit `a965c48` 合入 `main`。
- 远程开发分支已删除；无关未跟踪文件保持不动。
- 合并归档后的 `main` 已在干净 `npm ci` 依赖上重新通过 release-check：unit `3813/3813`、42 个 integration 脚本 `1963/1963`，lint 与 smoke 全绿。
- 主页面几何门禁 `6/6 PASS`；启动建窗到可见平均 `99.058ms`；变量扫描仍为 202 个 JS 文件、2324 个顶层声明，重要变量硬节点安全通过。
- 生产依赖仍有既有 7 条 audit 告警（2 moderate、5 high）；v3.0.24 未新增生产依赖，该问题继续作为依赖治理 follow-up。
- annotated tag `v3.0.24` 指向 `f516a9fd288b5be1a553ddc8e9935ca7a4670d8d`；Windows Release workflow run [`29938081301`](https://github.com/MatthewPZhong/bank-bill-excel-tool/actions/runs/29938081301) 全部通过。
- GitHub Release [`v3.0.24`](https://github.com/MatthewPZhong/bank-bill-excel-tool/releases/tag/v3.0.24) 已成为 latest、非 draft、非 prerelease，包含 Setup、Setup blockmap、portable 和 `latest.yml` 四个资产。
- 资产大小分别为 Setup `99,766,021`、portable `99,269,256`、blockmap `105,329`、`latest.yml` `371` 字节；匿名 Range 回读确认两个 EXE 文件头均为 `MZ`。
- `latest.yml` 公布版本 `3.0.24`、Setup 路径、大小 `99,766,021` 与 SHA-512；其 SHA-256 `3c162df0d4dead45bf3805f5999e4375e610e6f6afa6f03a44d60f07ba3e9130` 与 GitHub 元数据一致。
- 真实或脱敏双大账号逐笔资金复核仍是人工 follow-up；自动测试通过不等于人工资金验收完成。
