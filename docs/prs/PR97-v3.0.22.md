---
pr: 97
version: v3.0.22
merged: 2026-07-21 (116eee1)
integrated: true
released: 2026-07-21 (v3.0.22)
prd: docs/iterations/v3.0.22/PRD-v3.0.22.md
---

# [v3.0.22] 设置页内存档中心

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/97 ｜ 分支 `codex/v3.0.22-archive-center` -> `main`。

## 一、交付内容

1. 设置页增加“自动更新 / 存档中心”双导航，默认仍进入自动更新；存档中心不新增主模块。
2. 对 11 个主模块保存当前启动周期内可证明来源的业务输入和第一次成功结果。
3. 使用 SQLite 轻量索引与 SHA-256 内容寻址 Blob，支持全局去重、保留期、锁定、删除、失败重试、只读打开和另存为。
4. 链接表与临时 MPT 使用独立输入批次；运行批次按模块、批次类型和 run key 隔离。
5. 存档失败不改变原业务返回；正常退出和在线升级重启会等待存档队列完整排空。

## 二、Self-review 修复

- 显式历史 run key 未命中时不再回退到最新批次。
- 临时 MPT 修复按源文件与结果下标绑定，避免同名文件串绑。
- 业务成功后立即固化文件身份，排队期间文件变化时拒绝把新内容冒充原输入或结果。
- 月度余额不再覆盖普通网银账单的活动批次。
- 同批文件在首个复制动作前全部登记，正常退出不丢弃尚未开始的存档任务。
- 另存目标同时检查词法路径和真实目录，阻止目录链接绕过存档根保护。

## 三、验证与 Review

- 存档专项测试 `64/64 PASS`。
- `npm run release-check`：unit `3780/3780`、integration 42 个脚本 `1955/1955`、Smoke 全通过。
- ESLint、语法检查、差异检查、设置页预览和启动性能检查通过。
- `scan:vars`：201 个 JS 文件、2313 个顶层名称。
- `check-vars` 命中预期的存档仓储、IPC、UI 状态和退出生命周期；未修改既有资金金额、币种、匹配或回填口径。
- GitHub PR workflow 通过；最终 self-review 为 P0-P4 Finding 0。

## 四、合并与发布

- PR #97 于 2026-07-21 以 merge commit `116eee1` 合入 `main`。
- 远程与本地开发分支已删除，主工作区无关未跟踪文件未被触碰。
- 合并归档后的最终 `main` 已在干净 `npm ci` 依赖上重新通过完整 release-check、六组合主页面几何门禁、变量扫描和重要变量复核，可进入受控 tag 发布。
- 首次发布 workflow run `29814335578` 在打包前因两条 Windows 测试可移植性假设失败，未创建 Release 或发布资产；路径规范化和 CRLF/LF 断言修复后，定向测试 `34/34` 与完整 release-check 再次通过。
- 既有生产依赖仍有 7 条 audit 告警（2 moderate、5 high）；3.0.22 未改变生产依赖，该问题继续作为依赖治理 follow-up。
- 真实文件 SHA-256 与模块血缘抽查、Windows Excel/WPS 只读表现仍为人工 follow-up，不得宣称人工验收通过。
- annotated tag `v3.0.22` 指向 `9e40a298f2f85fa82ce10b5eed941a0b4716a48a`；Windows Release workflow run [`29816044492`](https://github.com/MatthewPZhong/bank-bill-excel-tool/actions/runs/29816044492) 全部通过。
- GitHub Release [`v3.0.22`](https://github.com/MatthewPZhong/bank-bill-excel-tool/releases/tag/v3.0.22) 已发布为 latest、非 draft、非 prerelease，包含 Setup、Setup blockmap、portable 和 `latest.yml` 四个资产。
- 资产大小分别为 Setup `99,758,793`、portable `99,262,027`、blockmap `105,297`、`latest.yml` `371` 字节；匿名 Range 回读确认两个 EXE 文件头均为 `MZ`。
- `latest.yml` 公布版本 `3.0.22`、Setup 路径、大小 `99,758,793` 与 SHA-512；完整 Setup SHA-512 及更新资产契约由 workflow 验证。
