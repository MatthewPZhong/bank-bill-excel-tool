---
pr: 98
version: v3.0.23
merged: 2026-07-21 (0171b2b)
integrated: true
released: false
prd: docs/iterations/v3.0.23/PRD-v3.0.23.md
---

# [v3.0.23] C3 渠道预筛与 R4 资金性质校验收紧

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/98 ｜ 分支 `codex/v3.0.23-c3-channel-prefilter` → `main`。

## 一、交付内容

1. C3 网关 Channel 候选预筛改为双方 trim 后忽略英文大小写；R1、DBS-Charge、R4、R5 继续使用原大小写敏感候选池。
2. Ach Return、Wire Return、HX-out、HX-in 改为 ReconID、MerchantId、Currency、固定 TradeType、金额、signed Extra Fee 和借贷方向完整一致后才匹配。
3. 四类 R4 共享严格 1:1 消费状态，按网关链接表原序与银行 Excel 原序稳定处理冲突，并对多候选、条件不符和方向异常提供告警。
4. R4 将包括同值 no-op 在内的具体匹配关系传给 R5；R5 只排除实际配对的 Ach Return 银行对象，不按 ReconID 或行号扩散。
5. R1、DBS-Charge、C3 内部条件、IPC、数据库 schema、Excel 结构和高亮契约保持不变。

## 二、验证与 Review

- R4、R5 与编排器定向测试 `205/205 PASS`；双候选池及 R4→R5 完整链路回放 `23/23 PASS`。
- `npm run release-check`：lint、smoke、unit `3791/3791`、42 个 integration 脚本 `1963/1963` 全部通过。
- `npm run scan:vars`：201 个 JS 文件、2323 个顶层声明。
- `check-vars` 按设计命中 `runAllScenarios`、`runReconciliation` 等 2 个 Critical 和 4 个 Risk-sensitive 变量，关联功能 review 与资金回归已完成。
- GitHub PR #98 Windows workflow 通过；最终 self-review 为 P0-P4 Finding 0。

## 三、合并与发布

- PR #98 于 2026-07-21 以 merge commit `0171b2b` 合入 `main`。
- 远程与本地开发分支已删除，主工作区无关未跟踪文件未被触碰。
- `v3.0.23` tag 与 GitHub Release 尚未创建；发布 workflow、在线资产和匿名 feed 证据将在完成后补录。
- 合并归档后的最终 `main` 已在干净 `npm ci` 依赖上重新通过完整 release-check、主页面几何门禁 `6/6`、启动性能、变量扫描和重要变量硬节点复核，可进入受控 tag 发布。
- 既有生产依赖仍有 7 条 audit 告警（2 moderate、5 high）；v3.0.23 未改变生产依赖，该问题继续作为依赖治理 follow-up。
- 真实 Ach Return、Wire Return、HX 及重复 ReconID 冲突样本的逐笔复核仍是人工 follow-up，不得宣称资金人工验收通过。
