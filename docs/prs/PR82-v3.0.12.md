---
pr: 82
version: v3.0.12
merged: 2026-06-29 (eb9b83c)
integrated: false  # 无独立 in-repo PRD（v3.0.12 由 ~/.claude/plans 已批准 plan 驱动，changes/ 无 3.0.12 spec）；改动清单见本文件
---

# [v3.0.12] 资金对账：异常-人工判断 sheet + 账户映射管理 + C3 网关 currency 重启修复

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/82 ｜ 分支 `v3.0.12`（基于 main）→ main。

「资金对账数据处理」模块 2 个新能力 + 并入 1 个 C3 重启 bug 修复，均围绕 `bank-statement:run`（5 轮编排）主链。team-lead 拆批委托 dev 实施 + team-lead 审 diff / release-check / 接缝核查 / preview 兜底。用户手动测试通过后提 PR，经 codex 3 轮 review + team-lead self-review 收敛。

## 一、改动清单

| # | 项 | 性质 | 核心改动 |
|---|----|------|---------|
| 功能1 | 异常-人工判断 sheet | 🔴 资金红线·纯只读 | 银行↔网关/调拨「多对多」时，现有引擎只发 `multi-bank-match-backfill` 警告就静默取 `cand[0]` 回填、人工无从感知。新增 `many-to-many-detector.js` 在 R5 全跑完后**只读**检测（分组键=账号+币种+金额分+日期±1含同日；连通分量银行≥2 且 对手≥2 → 命中；银行行不限 FundType、对手=网关+调拨都检测），命中银行行汇总进结果文件新 sheet「异常-人工判断」（列=`[异常说明,...46银行列]`，空命中→不加 sheet）。**保留现有回填不变、不改任何 bankRow 字段/modifications、行数守恒**。性能：银行优先门控（无碰撞整段跳过对手池扫描）+ detector 前后 `yieldTick`（消「开始运行」卡顿）+ 大组 O(nb×nc) 封顶过报 |
| 功能2 | 账户映射管理 | 🔴 资金红线 | 链接表管理左下角新增「账户映射管理」嵌套弹窗：中台调拨单账户号 → 清结算系统银行账号。派生调拨对账单时把 `big_account` 换成清结算账号再对账（缺失原样保留=现状零变化）。新全局表 `fund_transfer_account_mappings`(UNIQUE mid_account_id) + 仓储(整表删+插) + facade + IPC(list/save) + preload + dialog。映射在派生处**单点注入**（`rebuildFundTransferReconDerivation` 唯一生产调用处，run/导入两链共用）→ R5s2-recon + DBS-Charge R3.5 两轮一致单一真值源 |
| 修复C | C3 网关 currency 字段重启被清空 | 🔴 资金红线 | 启动迁移 `ensureC3GwFieldCurrencyCaseFix` 每次开机把 C3 `reconFields[].gwField` 小写 `currency` 改成大写 `Currency`，但 v2.1.15 W1 + v2.1.16-beta.2 T1 后 UI 下拉源与引擎取数源已统一小写 → 重启后大写匹配不到落空、currency 维度静默不比对。**反转**该迁移（`...CaseRevert`：大写→小写）+ `app_settings` marker `c3_gw_field_currency_revert_done` 幂等只跑一次（0 改动也写 marker，杜绝重蹈每次开机覆盖用户改动） |

## 二、改动文件

- **代码（功能1）**：`scenario-engines/many-to-many-detector.js`（新）/ `reconciliation-orchestrator.js`（接线+容差+yield）/ `bank-statement-io.js`（第8形参透传）/ `exceljs-writer.js`（`appendManyToManyReviewSheet`）/ `main.js`（run/export 两跳）
- **代码（功能2）**：`database/fund-transfer-account-mapping-repository.js`（新）/ `database/migrations.js`（新表 + C3 反转）/ `backend/database.js`（facade）/ `main.js`（IPC + 校验 + op-lock）/ `preload.js` / `renderer-dialogs.js`（嵌套弹窗）/ `renderer-previews.js` / `fund-transfer-recon-builder.js`（映射应用）/ `linked-derive-rebuild.js`（映射注入）/ `renderer.js`
- **测试**：`many-to-many-detector.test.js`（新）/ `fund-transfer-account-mapping-repository.test.js`（新）/ `migrations-c3-gw-field-currency-revert.test.js`（新）/ `renderer-dialogs-fund-transfer-account-mapping-load-guard.test.js`（新，P2-3）/ `fund-transfer-recon-builder.test.js` / `reconciliation-orchestrator.test.js` / `bank-statement-op-lock.test.js`（P2-2）/ 集成 `bank-statement-many-to-many-review-sheet.js`（新）/ `fund-transfer-recon-account-mapping.js`（新）
- **文档**：`CHANGELOG.md` / `docs/VERSION_FEATURE_HISTORY.md` / `docs/USER_GUIDE.md` / `rules/important-variables.md`（detector + 映射表登记）/ `rules/integration-test-policy.md` / `docs/analysis/var-reference-stats.{md,json}`（scan:vars 刷新）/ `docs/previews/{linked-table-manager,fund-transfer-account-mapping}.png`（preview 重渲）/ `knowledge/backlog.md`（B25）

## 三、验证

- `npm run release-check` 全绿：**unit 3283/3283 · integration 1775/1775（38 脚本）· smoke pass**。
- `check:vars`：Critical `unmatchedRows` + Risk-sensitive `detectFundTransferManyToMany`/`writeBankStatementOutput` + Runtime-state `processingResult` 等命中，自查见 PR body「⚠️ 关联功能 review」（writer 第8形参不串位、detector 只读输出不变、processingResult 失效已修、行数守恒 integration 1775 + 只读 byte-parity 锁定）。
- `scan:vars` 已刷新；前端 preview 无回归（字节不变）；用户手动测试通过（多对多 sheet、账户映射、C3 重启 currency 仍选中）。

## 四、codex review + self-review（3 轮收敛，2026-06-28~29）

`codex exec review --base main` 逐轮 + team-lead self-review，修齐 3 个 P2 + 1 Minor：

- **轮1**：① P2-1（🔴）detector 日期容差未对齐引擎——硬编码回退 1，容差≠1 时复核表漏报/过报。**修**：detector 改用 `r5s2Bucket[0].config.dateToleranceDays`（网关侧 `runRound5FundTransferBackfill` 与调拨侧 `runRound5FundTransferReconBackfill` 同源此字段、`useMidReconTable` 互斥择一 → 单值即可），默认 1 路径字节不变。② P2-2（🔴）账户映射 save 未纳 `bankStatementOperationLock`——run/export 让出窗口内并发保存清 `processingResult` 后被旧映射结果覆盖 → 导出 stale。**修**：纳入 op-lock（3→6 handler 共用），争用即拒、finally 释放（仿 linked-table import/delete）。
- **轮2**：确认 P2-1/P2-2 消除；新发现 **P2-3（🔴 数据丢失）** 账户映射弹窗加载竞态——`list()` 异步回填前 / 失败时 Done 仍可点 → 读空表 `save([])` → 仓储整表删+插清掉用户已配映射。**修**：`loaded` 标志 + Done 初始禁用、`list()` 成功才启用（含合法空[]）、失败给错误态保持禁用 + handler 内 `if(!loaded)return` 兜底（门控「还没 load 就存」而非「不让存空」）。
- **轮3**：`No actionable correctness issues found` —— 干净收敛。
- **Minor（self-review）**：IPC 校验去重 `String().trim()` → `normalizeCellValue`，与仓储 `saveMappings`/`getMappingMap` 同口径。

修复后 `release-check` 复跑全绿（unit 3283 含新增 detector 容差 / op-lock / 弹窗 load-guard 用例）；改动仅控制流 / UI 闸 / 容差入参，主产物 golden 字节不变。

## 五、已知非阻塞（已记 backlog）

- **B25（P2）**：源账单 BillDate/ValueDate 存为真 Excel 日期单元格（数值序列号）时被读成数字 → 输出三表显示序列号。经 3 复现 + 两份真实文件核验确认为**既有行为·非本次回归**（命中场景主表同样如此），且对账匹配正确（引擎内部 `normalizeDateExportValue` 按序列号正常比对），仅输出可读性问题。修法（写出层 `fmtDate` / 归一化层）待后续。
