---
pr: 101
version: v3.0.26
merged: 2026-07-25 (fa416aa)
integrated: true
released: false
prd: docs/iterations/v3.0.26/PRD-v3.0.26.md
---

# [v3.0.26] 前置资金导出与 R5 手续费迭代

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/101 ｜ 分支 `codex/v3.0.26` → `main`。

## 一、交付内容

1. 平盘模块“对账表管理”改为“对账数据管理”；资金链接表删除框标题固定为“删除数据”，实际删除目标与成功提示仍按所选表执行。
2. 前置资金对账“不平结果”在第 6 列增加银行原始 `FundType`，模板由 20 列升级为 21 列。
3. C4 严格兼容旧 19 列“对账结果”、旧 20 列“不平结果”和新 21 列“不平结果”，继续投影为既有 19 列内部数据。
4. R5 两种来源和调拨多对多审计统一使用 `abs(Credit-Debit) + signed Extra Fee`；非法手续费退出候选并进入主错误报告。
5. DBS-Charge 两个步骤显式保留不含 `Extra Fee` 的旧金额口径。

## 二、验证与 Review

- `npm run release-check`：unit `3855/3855`、43 个 integration 脚本 `1978/1978`，lint 与 smoke 全部通过。
- 前置资金输出契约集成 `15/15 PASS`；多对多输出集成 `33/33 PASS`。
- 主页面几何门禁 `6/6 PASS`；`scan:vars` 为 202 个 JS 文件、2329 个顶层声明。
- GitHub PR #101 workflow run [`30155798779`](https://github.com/MatthewPZhong/bank-bill-excel-tool/actions/runs/30155798779) 通过；最终 self-review 为 P0-P4 Finding 0。
- ⚠️ `bankAmountWithExtraFee` 关联的 R5、DBS 隔离、多对多只读、1:1 消费和异常可观测性均已完成重要变量复核。

## 三、合并与发布

- PR #101 于 2026-07-25 以 merge commit `fa416aa` 合入 `main`。
- 远程与本地开发分支已删除；无关未跟踪文件保持不动。
- 合并后的 `main` 已在干净 `npm ci` 依赖上重新通过 release-check：unit `3855/3855`、integration `1978/1978`，lint 与 smoke 全绿。
- 主页面布局 `6/6 PASS`；启动建窗到可见平均 `102.107ms`，ready-to-show 平均 `173.357ms`。
- 发布前变量扫描仍为 202/2329；重要变量硬节点因 `src` 无新改动安全跳过。
- `npm audit --omit=dev` 报告 9 条生产依赖 advisory（2 moderate、7 high、0 critical）；相对 v3.0.25 未改变依赖图，继续作为安全治理 follow-up。
- 当前已达到 tag 条件；annotated tag `v3.0.26`、Windows Release workflow 和公开更新资产验证尚待创建。
- 真实资金样本、Windows Excel/WPS 21 列模板显示及 `v3.0.25 → v3.0.26` 在线升级 canary 仍是公告前人工门禁。
