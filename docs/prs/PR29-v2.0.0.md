# PR #29（已 merge）

- **URL**：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/29
- **分支**：`v2.0.0` → `main`
- **版本**：`2.0.0-beta.2`（v2.0.0-beta.3 系列阶段 1，发版前不 bump）
- **状态**：MERGED 2026-04-28（merge commit `2c5fba5`）
- **integrated**：true（已回写 PRD-v2.0.0-beta.3 §十六 实施记录）

---

## Summary

v2.0.0-beta.3 主体迭代「**银行对账单处理模块**」启动，**阶段 1 = 数据底座**（4 个 PR 中第 1 个，按方案 B 切分）。

实现：`scenarios` 表 + 迁移 + seed 3 内置场景 + repository CRUD + 6 个 IPC channel + preload API。**不含 UI、算法、文件 IO**（这些归后续 3 个 PR）。

9 文件 +1377/-0（含 749 行 PRD-v2.0.0-beta.3 主文档）。

---

## ⚠️ 关联功能 review（check-vars）

- **Important-skeleton**: `ipcRenderer`
  - review: 新增 6 个 channel `scenarios:list / get / create / update / delete / toggle-enabled` 已在 main 端 `ipcMain.handle` 注册，preload 已暴露 `desktopApi.scenarios`，对齐
- Critical / Risk-sensitive 命中：0 处（仅持久化层）

---

## 关键决策（D6 + D14 + F1 修复）

- **D6**：单表 `scenarios` + JSON blob（不开多表）
- **D14**：`is_builtin` 仅记录用，不阻止删除（任意场景可删）
- **F1 修复（资金红线相关）**：
  - 旧逻辑（COUNT(*) === 0 即 seed）违反 D14 删除终态语义
  - 改用 `app_settings.scenarios_seeded` marker
  - 删完所有内置场景后重启 → marker 已存在 → 不复活（避免误处理账单）
  - 5 个边界单测全 PASS（A1-E1：全新库 / 删完后重启 / 部分删 / 老库迁移 / marker+空表）

---

## 改动文件

| 文件 | 改动 |
|---|---|
| `src/backend/database/migrations.js` | +123 — `ensureScenariosSupport` + `BUILTIN_SCENARIOS` + marker 机制 |
| `src/backend/database/scenarios-repository.js` | **新文件** — list / get / create / update / delete / toggleEnabled + 校验 |
| `src/backend/database.js` | +32 — facade 暴露 6 method + init 链补 `ensureScenariosSupport` |
| `src/main.js` | +51 — 6 个 `scenarios:*` IPC handler |
| `src/preload.js` | +9 — `desktopApi.scenarios` |
| `docs/iterations/v2.0.0-beta.3/PRD-v2.0.0-beta.3.md` | **新文件** 749 行 — 完整 PRD |
| `changes/v2.0.0-beta3-stage1-data/` | spec 三件套 |

---

## 测试

- ✅ 单测 14/14 PASS（list/get/create/update/delete/toggleEnabled + seed 幂等）
- ✅ F1 修复后追加 5 个边界单测（A1-E1）全 PASS
- ✅ `npm run smoke` 通过
- ✅ `npm run check:vars` 命中 ipcRenderer 已自查
- ✅ 老库迁移路径验证（无 marker 但有数据 → 仅写 marker 不重复 seed）

---

## Codex review 处理

- **F1 [P2]**：seed 时机违反 D14（删除终态）→ 改用 marker 机制，5 个边界单测覆盖
- **F2 [P3]**：tasks.md 全标 todo → 全部改 done
- **F3 [P3]**：PRD 行 428 trailing whitespace + 末尾多余空行 → 清理

---

## 文档同步

按 `workflow_docs_update`：v2.0.0-beta.3 系列发版（阶段 7+8 = PR #32）才统一更新 CHANGELOG / VERSION_FEATURE_HISTORY / USER_GUIDE。本阶段**不**更新。

---

## 下一步（v2.0.0-beta.3 进度）

```
✅ 阶段 1 (PR #29): 数据底座          ← 已 merge
⏳ 阶段 2+3 (PR #30): 模块入口 + 场景管理弹窗（1.5 天）
⏳ 阶段 4+5+6 (PR #31): 3 类配置弹窗 + 算法（6 天）
⏳ 阶段 7+8 (PR #32): 调度 + 标黄 + E2E + 文档 + bump beta.3（3-4 天）
```

## Test plan

- [x] 单测 14/14 + F1 边界 5/5
- [x] `npm run smoke`
- [x] `npm run check:vars`
- [x] Codex review F1 (P2) + F2/F3 (P3) 已修复
- [ ] 等 reviewer 在自己机器跑：删 `tool-data.sqlite` → 启动 → sqlite3 查表应有 3 内置 + marker；再删完 3 内置 → 重启 → 应仍为空
