---
pr: 77
merged: 2026-06-17
integrated: true
---

# [v3.0.8] 资金对账迭代 + 用户验收反馈 UI/输出/文档调整

v3.0.8 合入 main。内容 = **原 7 项需求**（已落 `docs/iterations/v3.0.8/PRD.md` + `TECHDOC.md`）+ **本轮 9 项用户验收反馈**（UI/输出/文档）+ **测试/工程加固**。68 commits。

## 一、原 7 项需求（详见 PRD/TECHDOC）

| 需求 | 摘要 | 红线 |
|---|---|---|
| 需求1 | 工具箱🧰：脱离主流程的 Excel 合表/拆表小工具 | — |
| 需求2 | 场景管理：退役自带场景 C3（前端隐藏，后端可回滚）+ 资金性质校验/中台订单两组三角折叠 | — |
| 需求3 | 资金对账运行不阻塞：主进程 async + 轮次进度事件，消除「未响应」 | 🔴 只改控制流，golden 字节不变 |
| 需求4 | 银行未命中 sheet：A1 提醒 + 数据右移到 B 列 | 🔴 输出口径 |
| 需求5 | BOC 调拨修复行 Type 2→1 | 🔴 资金红线 |
| 需求6 | 运行内存尖峰修复：bank-deposit 消费方门控 + gateway 按 Channel 过滤读 | 🔴 资金红线 |
| 需求7 | R5s3 两级 fallback（ReconId 主 + ChannelOrderNo 兜底）+ FundType 子串判定 | 🔴 资金红线 |

## 二、本轮 9 项用户验收反馈

**工具箱 + 场景管理弹窗 UI（commit f5e4fa1）**
- #1 去掉拆表「导出文件」按钮 → 选字段「完成」即一气呵成过滤另存为
- #2/#3 两个「导入文件」按钮同尺寸 + 中线与左侧文本对齐
- #4 工具箱弹框宽度 460→230px
- #5 拆表值多选框：原生 `<select multiple>` → 「按钮+浮动勾选面板」控件（复用 `new-account-currency-dropdown-*`，对齐场景管理「适用银行渠道」下拉）
- #6 中台调拨订单回填管理页「对账数据来源」+「Payment线下调拨」两勾选框并排一行
- #8 工具箱所有反馈 `window.alert` → 应用内 `createAlertDialog`（有前端页面/可预览，escapeHtml+`<br>`，成功 skipLogReport）

**未命中 sheet 输出（commit 1b2055c，🔴 输出口径）**
- #9 银行行数据整体上移一行：表头第2行→第1行（与 A1 提醒同行），数据第3行→第2行，填掉顶部空行。仅行位置变，数据/排序/右移列/标黄不变。

**使用手册（commit c5a9270）**
- #7 导出文件名 `使用手册.html` → `使用手册-v${app.getVersion()}.html`（= `使用手册-v3.0.8.html`）；版本头 v3.0.5→v3.0.8；§1.11.5 去内部代号 C1~C4 改用类别选择框实际显示名 + 标注 C3 退役；control-row/scenarios.name 等正文 jargon 去除；历史 changelog 保留（用户拍板「仅正文」）。

## 三、测试/工程加固
- **ESLint `no-undef` 静态门进 release-check**（commit 7c2d682）——根治「跨作用域 not-defined」类事故（CommonJS 层覆盖，renderer 浏览器脚本四件套因共享全局排除）。探针验证能抓到原事故。
- **run handler 接缝 + 进度跨进程契约护栏单测**（commit 0e3e7db）——钉死 main.js handler 体的资金红线门控谓词跨文件同源 + not-defined 回归 + 编排器↔renderer↔preload 进度通道契约。

## 四、验证证据
- **`npm run release-check` 全绿**：lint + smoke + unit **2977/2977** + integration **33 脚本 / 1469 例**。
- preview 回归：`toolbox` / `toolbox-split-field-picker` / `builtin-fixed-channel-manage(-payment)` 截图已更新并目检。
- ⚠️ 待手测（IPC/GUI 自动化盲区）：#5 拆字段弹框值下拉**点开浮动面板**勾选（闭合态已截图；开态逻辑镜像已验证的渠道下拉）；#8 合表/拆表跑通看应用内弹框。

## ⚠️ 关联功能 review（check-vars 自动生成）
- **Critical**: `unmatchedRows`
  - review：#9 改未命中 sheet 行布局（表头第1行/数据第2行）。仅行位置变，未命中行的筛选/数据/FundType 排序/W2 右移/A1 提醒/标黄全不变。golden `exceljs-writer-dual-sheet` 8/8 + smoke `F8-W2/W3` 已同步钉死新行位置。
  - 必跑：`npm run smoke` ✓（已过）
- **Runtime-state**: `app`, `dialog`
  - review：#7 在 `app:save-user-guide` handler 用 `app.getVersion()` 拼版本化文件名（只读，无副作用）；`dialog.showSaveDialog` 入参不变。
