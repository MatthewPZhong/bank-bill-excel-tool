---
pr: 70
version: v3.0.3
merged: 2026-06-10
integrated: true
---

## 概览

v3.0.3 迭代：四个块 / 10 个 PR。收单单据模块导入/对账性能批次（块 A）+ 资金对账数据处理状态框「渠道:场景」明细（块 B）+ USER_GUIDE 补全（块 C）+ 🔴 通用大表导入引擎与收单首迁（块 D，W4 达成）。

Spec：`changes/v3.0.3/spec.md`（迭代入口）/ `changes/acquiring-import-recon-perf/spec.md`（块 A）/ `changes/big-table-import-engine/spec.md`（块 D）｜ PRD：`docs/iterations/v3.0.3/PRD-v3.0.3.md`（含实施记录与已知限制）｜ Bench 留档：`docs/iterations/v3.0.3/BENCH-v3.0.3.md`

## 交付与实测数字

| 块 | 内容 | 关键数字 |
|----|------|---------|
| A | flow raw_json 停写 + 索引瘦身 v2 covering + PRAGMA temp_store + W2 checkpoint + 多 worker 阈值 30w + OneDrive 提示 + 解析列白名单 | flow 导入段 **6.36x**；对账统计段 **5.2x**；解析段 1.20x 收口（债务转块 D） |
| B | hitScenarios +channelId/channelName、双维去重键、状态框按渠道分组换行 + fallback + 滚动护栏 | dispatcher 既有 21+ 测试 0 回归 |
| C | USER_GUIDE 6 新章节 + §1.4 口语化 | — |
| D | 引擎（单遍字节状态机 / 多文件并行按文件序单写 / 白名单三层防护）+ 收单首迁 + 单行回退开关 | 解析段 **2.32x**；4-worker **3.06x**；端到端 50w 11.2s→7.3s；**W4：导入全程主进程零阻塞** |

## 验收

- `npm run release-check` **exit 0**：unit **2179/2179** ｜ integration **22 脚本 1086/1086** ｜ smoke 全模块 PASS
- 🔴 资金红线放行闸：`acquiring-engine-migration.js` **34 断言**——新旧两路六场景（成功/UNIQUE 冲突/跨月/坏表头/overwrite/bill raw_json）逐行含 rowid + 报错文案逐字符 byte-for-byte
- 回退预案：`USE_BIG_TABLE_IMPORT_ENGINE`（session.js:54）单行拨 false 即回 v3.0.2 路径；reader.js / reader-handrolled.js / import-repository.js 一字未改

## ⚠️ 关联功能 review（check-vars）

本次改动触及以下重要变量（全部命中项的必跑清单已在各 PR 验收时执行完毕）：

- **Critical**: `USE_BIG_TABLE_IMPORT_ENGINE`（新升格条目，PR-H 本体）、`settle_amount_abs`/`parseAmountAbs`、`settle_currency_norm`/`normalizeCurrency`、`insertDiffRowsByJoin`（covering 索引 chunked 子查询）、`runAllScenarios`/scenario-dispatcher（hitScenarios 结构）、`bill_imports.raw_json`（contract-bill 平移副本）
  - review 焦点：**金额/币种解析函数在 contract-flow/bill 与 import-repository 为双副本，改任一侧必须同步**（集成脚本 + important-variables.md 已锁）
- **Important-skeleton**: `serializeError`/`deserializeError`（引擎 worker 链）、`TEMPLATE_BILL_HEADERS`（只读引用）、`settingsRepository`（新增 OneDrive 键）、收单 `FLOW_HEADERS`（只读引用）
- **Runtime-state**: `processingResult`（renderer 读侧 fallback 兼容，写侧未动）

**必跑**（已全绿）：
- [x] `npm run release-check`
- [x] `acquiring-engine-migration.js`（34）+ `big-table-import-engine.js`（19）+ `acquiring-index-slim-v2.js`（22）
- [x] smoke acquiring-bill-currency 203 + progress 34 + pragma 27
- [x] `npm run preview`（块 B 前端回归）

## 建议合并后手测（自动化覆盖不了）

1. W4 体验：多个大文件导入期间主界面不卡（可对比开关拨 false）
2. 多文件并行：4+ 文件同时导入总耗时
3. 覆盖导入：同月重导 → 确认弹窗 → 数据正确
4. 🔴 真实数据对账闭环：真实 flow+bill 导入→对账→导出差异表，与 v3.0.2 结果核对一致
