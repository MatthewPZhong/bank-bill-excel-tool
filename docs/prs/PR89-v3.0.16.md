---
pr: 89
version: v3.0.16
merged: 2026-07-16 (e09a18c)
integrated: true
prd: docs/iterations/v3.0.16/PRD-v3.0.16.md
---

# [v3.0.16] 前置资金对账规则与错误行重跑

PR：https://github.com/MatthewPZhong/bank-bill-excel-tool/pull/89 ｜ 分支 `codex/v3.0.16-pre-fund-rules` → `main`。

## 一、交付内容

1. 银行匹配金额改为方向金额与有符号 `Extra Fee` 的精确十进制和，原始导出金额字段保持不变。
2. 固化附件中的 14 类 `FundType + 借贷方向 + gateway tradeType` 资格规则，再按对账 ID、渠道、金额、币种执行严格 1:1 消费。
3. 规则未配置、方向不符或无网关类型的银行行进入不平结果，并输出原因和分类统计。
4. 临时 MPT 明细错误支持导出错误数据，以及不修改源文件的逻辑删除错误行后原子重跑。
5. 错误工作簿保留来源、物理行号、规范字段与原始行分片；文件变化、行数超限和发布失败均 fail closed。
6. `MPT_CHANNEL_OTHERS`、临时银行对账单和缺渠道账单明确不做。

## 二、验证与 Review

- `npm run release-check`：lint、smoke、unit 3585/3585、integration 1877/1877（40 个脚本）全部通过。
- `npm run scan:vars` 与 `npm run check:vars -- --include-minor` 完成；金额规则、错误 schema、IPC、状态生命周期和发布恢复已逐项复核。
- 页面预览覆盖主页面、前置资金对账完成态、临时链接表管理与导入失败三按钮页。
- GitHub Actions `smoke-test` 通过；P0/P1/P2/P3/P4 Finding 均为 0。

## 三、合并与发布门禁

- PR #89 于 2026-07-16 以 merge commit `e09a18c` 合入 `main`，远程开发分支删除。
- 真实脱敏样本中的 Extra Fee 正负号、规则组合、1:1 消费和逻辑删除批次仍需业务人员逐笔复核。
- 人工资金复核阻塞发布，但不回退已完成的代码合并状态。
- 本次不创建 `v3.0.16` tag，不发布 GitHub Release。
