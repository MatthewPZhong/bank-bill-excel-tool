# v2.1.16 迭代 PRD（索引 / 导读 · 🚧 在研）

> 状态：🚧 **在研**｜当前 `2.1.16-beta.1`（阶段一·地基层已合入 main，PR #61）｜分支：`v2.1.16`
> 阶段二（资金性质校验 + 5 轮对账编排 + 网关对账单 reader + 期权表）待续 → `beta.2` → 转正 `2.1.16`。
> 本文件为**在研索引**：当前规格以 PR #61 草稿承载（[`docs/prs/PR61-v2.1.16-beta.1.md`](../../prs/PR61-v2.1.16-beta.1.md)），阶段二 spec 待补。

---

## 一、阶段一·地基层（beta.1，已合入）

为「资金对账数据处理」模块补齐地基能力，**纯新增 + bugfix，不破坏现有功能**，main 仍可发布：

| # | 内容 | 关键文件 |
|---|---|---|
| 1 | 批量导入：「导入对账单」改多选入口 + 按表头自动识别表类型（L1 精确 + L2 模糊两层）；多份银行对账单**合并对账**（追加不覆盖）| `table-type-detector.js`(新)、`bank-statement-merge.js`(新) |
| 2 | 链接表管理持久化：网关对账单 / 中台调拨订单 / 外汇交割 3 表库**混合存储**（join 键列 + 日期列索引 + `raw_json` 整行）；弹窗渲染真实日期范围/更新日期；期权表占位 | `linked_*` 表 |
| 3 | 自带写死场景「管理」弹窗新增「优先级」输入框 | `renderer-dialogs.js` |
| 4 | 修复/规范：调拨订单数据日期范围改用「交易时间」；列名 `business_date → transaction_date`（新表零存量）；assets 模板库规范（新增 `外汇期权订单.xlsx` / `外汇交割表.xls`、更新加款单剔除模板）| `linked_mid_allocation` 等 |

> 🔴 **资金/并发红线**：多文件合并后 `_rowId` 全局重编号保证跨文件唯一，dispatcher `rowLockSet` 锁定不变量保持（PR 称已验证 9888 行无漏对/误锁）。

---

## 二、阶段二（待续）

资金性质校验 + 5 轮对账编排 + 网关对账单 reader + 期权表接入；将从 main 切新分支续 `beta.2` → 转正 `2.1.16`。**规格待补。**

---

## 三、源文档

- PR 草稿：[`docs/prs/PR61-v2.1.16-beta.1.md`](../../prs/PR61-v2.1.16-beta.1.md)
- 分支：`v2.1.16`
- 阶段二 spec：⏳ 待补

---

## 四、变更记录

| 日期 | 变更内容 |
|------|---------|
| 2026-06-07 | 补建在研索引 PRD（阶段一已合入；阶段二 spec 待补）|
