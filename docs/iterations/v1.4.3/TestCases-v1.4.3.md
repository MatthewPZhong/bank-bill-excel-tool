# 测试用例文档 - 网银账单小助手 v1.4.3

| 项目 | 内容 |
|------|------|
| 版本 | v1.4.3 |
| 日期 | 2026-03-23 |
| 状态 | 待评审 |
| 作者 | Tester |
| 关联 PRD | docs/iterations/v1.4.3/PRD-v1.4.3.md |
| 关联技术文档 | docs/iterations/v1.4.3/TechDoc-v1.4.3.md |

---

## 一、测试范围总览

| 需求 | 优先级 | 测试类型 | 用例数 |
|------|--------|----------|--------|
| 需求 1：映射字段控件改回下拉框 | P1 | 功能 + 兼容性 | 8 |
| 需求 2a：拼接面板点击空白收起 | P1 | 功能 + 交互 | 8 |
| 需求 2b：拼接顺序标签显示 | P1 | 功能 + UI | 4 |
| 需求 2c：取消顺序确认弹窗 | P1 | 功能 + 数据完整性 + 代码清理 | 6 |
| 需求 3：大账号币种布局调整 | P2 | UI + 布局 | 5 |
| 需求 4：映射关系间距回滚 | P1 | UI + 回归 | 4 |
| 需求 5：多币种按钮修复与改造 | P0 | Bug回归 + 功能 + 交互 | 14 |
| 需求 6：新增按钮位置修正 | P2 | UI + 功能 + 回归 | 6 |
| 回归测试 | - | 回归 | 8 |
| **合计** | | | **63** |

---

## 二、测试用例

### 需求 1：映射字段控件改回下拉框

#### TC-1.1 下拉框控件类型验证

| 项目 | 内容 |
|------|------|
| 前置条件 | 已导入源文件，打开映射关系管理对话框 |
| 测试步骤 | 1. 观察所有非 Balance 映射字段的控件类型 |
| 预期结果 | 所有映射字段（含 Balance）均为单行下拉框（`<select>`），无 `multiple` 属性，无 `size` 属性 |
| 验证要点 | DevTools 检查 select 元素不含 `mapping-multi-select` class |

#### TC-1.2 下拉框外观一致性

| 项目 | 内容 |
|------|------|
| 前置条件 | 已导入源文件，打开映射关系管理对话框 |
| 测试步骤 | 1. 对比非 Balance 字段的下拉框与 Balance 字段的下拉框外观 |
| 预期结果 | 所有下拉框高度一致（44px）、圆角一致（14px）、最小宽度一致（260px），外观统一 |

#### TC-1.3 下拉框选项内容

| 项目 | 内容 |
|------|------|
| 前置条件 | 已导入源文件，打开映射关系管理对话框 |
| 测试步骤 | 1. 点击任意非 Balance 映射字段的下拉框<br>2. 检查下拉选项列表 |
| 预期结果 | 选项包含：空选项、"需要拼接字段"选项、源文件表头字段列表。选择一个选项后下拉框收起，显示选中值 |

#### TC-1.4 拼接字段选择器联动

| 项目 | 内容 |
|------|------|
| 前置条件 | 已导入源文件，打开映射关系管理对话框 |
| 测试步骤 | 1. 在任意支持拼接的字段下拉框中选择"需要拼接字段"<br>2. 观察拼接字段选择器是否出现<br>3. 将下拉框改选为其他普通字段<br>4. 观察拼接字段选择器是否隐藏 |
| 预期结果 | 选择"需要拼接字段"后拼接字段选择器正常显示；改选其他选项后选择器隐藏 |

#### TC-1.5 已保存映射关系回显（普通映射）

| 项目 | 内容 |
|------|------|
| 前置条件 | 已有保存的映射关系配置（普通单选映射） |
| 测试步骤 | 1. 打开映射关系管理对话框<br>2. 检查各字段下拉框的选中值 |
| 预期结果 | 已保存的映射字段值正确回显在对应下拉框中 |

#### TC-1.6 已保存映射关系回显（拼接映射）

| 项目 | 内容 |
|------|------|
| 前置条件 | 已有保存的拼接字段映射关系 |
| 测试步骤 | 1. 打开映射关系管理对话框<br>2. 检查拼接映射字段的下拉框选中值<br>3. 检查拼接字段选择器的勾选状态和预览文本 |
| 预期结果 | 下拉框显示"需要拼接字段"，拼接字段选择器显示且已勾选字段和顺序正确回显 |

#### TC-1.7 v1.4.2 旧数据兼容性

| 项目 | 内容 |
|------|------|
| 前置条件 | 使用 v1.4.2 版本保存的映射配置（含多选非拼接映射，即 `mappedFields` 中有多个值） |
| 测试步骤 | 1. 在 v1.4.3 中打开该映射配置<br>2. 观察是否有报错<br>3. 检查下拉框回显值 |
| 预期结果 | 不报错。下拉框以 `mappedField`（首选值）回显，多选中的其余值不再显示（预期行为） |

#### TC-1.8 Balance 和 MerchantId 字段不受影响

| 项目 | 内容 |
|------|------|
| 前置条件 | 已导入源文件，打开映射关系管理对话框 |
| 测试步骤 | 1. 检查 Balance 字段的下拉框行为（选项含 BALANCE_DISABLED_OPTION）<br>2. 检查 MerchantId 字段的下拉框行为<br>3. 分别选择值后保存，重新打开验证回显 |
| 预期结果 | Balance 和 MerchantId 字段的现有逻辑不受影响，选项列表、选择行为、保存回显均正常 |

---

### 需求 2a：拼接面板点击空白处收起

#### TC-2a.1 点击面板外部关闭

| 项目 | 内容 |
|------|------|
| 前置条件 | 映射关系管理对话框已打开，某字段已选择"需要拼接字段"，拼接面板已展开 |
| 测试步骤 | 1. 点击拼接面板外部的对话框空白区域 |
| 预期结果 | 拼接面板收起（hidden = true） |

#### TC-2a.2 点击面板内部不关闭

| 项目 | 内容 |
|------|------|
| 前置条件 | 拼接面板已展开 |
| 测试步骤 | 1. 点击面板内的 checkbox 勾选字段<br>2. 点击面板内的文字区域<br>3. 如有滚动条，拖动滚动条 |
| 预期结果 | 面板保持展开状态，不收起 |

#### TC-2a.3 Trigger 按钮切换功能正常

| 项目 | 内容 |
|------|------|
| 前置条件 | 某字段已选择"需要拼接字段"，面板当前关闭 |
| 测试步骤 | 1. 点击"选择字段"按钮，面板展开<br>2. 再次点击"选择字段"按钮 |
| 预期结果 | 第一次点击展开面板，第二次点击关闭面板。Trigger 按钮的切换功能不受外部点击关闭逻辑影响 |

#### TC-2a.4 面板收起后勾选状态保留

| 项目 | 内容 |
|------|------|
| 前置条件 | 拼接面板已展开，已勾选若干字段 |
| 测试步骤 | 1. 记录当前勾选的字段和预览文本<br>2. 点击面板外部区域关闭面板<br>3. 重新点击"选择字段"按钮展开面板 |
| 预期结果 | 面板收起后重新展开，之前勾选的字段状态完整保留，预览文本不变 |

#### TC-2a.5 多面板同时展开后全部关闭

| 项目 | 内容 |
|------|------|
| 前置条件 | 两个或以上映射字段均选择了"需要拼接字段" |
| 测试步骤 | 1. 展开字段 A 的拼接面板<br>2. 展开字段 B 的拼接面板（此时两个面板均可见）<br>3. 点击对话框内非面板区域 |
| 预期结果 | 所有已展开的拼接面板全部关闭 |

#### TC-2a.6 点击其他映射行区域关闭面板

| 项目 | 内容 |
|------|------|
| 前置条件 | 某字段的拼接面板已展开 |
| 测试步骤 | 1. 点击另一个映射字段的下拉框 |
| 预期结果 | 已展开的拼接面板关闭 |

#### TC-2a.7 面板关闭与单选下拉框联动

| 项目 | 内容 |
|------|------|
| 前置条件 | 某字段已选择"需要拼接字段"，拼接面板已展开 |
| 测试步骤 | 1. 点击同一行的下拉框（select）<br>2. 将选项从"需要拼接字段"切换为其他普通字段 |
| 预期结果 | 拼接面板关闭（由 `syncEditorState` 控制 `concatFieldPicker.hidden = !isConcatMode`），下拉框 change 事件正常触发，外部点击关闭逻辑不干扰 select 的切换操作 |

#### TC-2a.8 对话框关闭后无残留事件

| 项目 | 内容 |
|------|------|
| 前置条件 | 映射关系管理对话框已打开，拼接面板已展开 |
| 测试步骤 | 1. 关闭映射关系管理对话框<br>2. 在主界面进行其他操作（点击各区域）<br>3. 检查控制台无报错 |
| 预期结果 | 对话框关闭后，面板外部点击事件监听器已清理，无内存泄漏，无控制台报错 |

---

### 需求 2b：拼接顺序标签显示

#### TC-2b.1 标签文本显示

| 项目 | 内容 |
|------|------|
| 前置条件 | 映射关系管理对话框已打开 |
| 测试步骤 | 1. 在某字段下拉框中选择"需要拼接字段"<br>2. 观察拼接预览区域 |
| 预期结果 | 预览文本正上方显示"当前拼接顺序："标签文本，与预览文本左对齐 |

#### TC-2b.2 标签文本样式

| 项目 | 内容 |
|------|------|
| 前置条件 | "当前拼接顺序："标签已显示 |
| 测试步骤 | 1. 通过 DevTools 检查标签样式 |
| 预期结果 | 颜色为灰色（var(--muted)）、字号约 12px、与整体 UI 协调 |

#### TC-2b.3 非拼接模式下标签不显示

| 项目 | 内容 |
|------|------|
| 前置条件 | 映射关系管理对话框已打开 |
| 测试步骤 | 1. 检查选择了普通映射字段（非拼接）的行<br>2. 检查未选择任何值（空选项）的行 |
| 预期结果 | 非拼接模式下，"当前拼接顺序："标签和拼接预览均不显示 |

#### TC-2b.4 标签与预览布局关系

| 项目 | 内容 |
|------|------|
| 前置条件 | 已选择"需要拼接字段"并勾选了多个拼接字段 |
| 测试步骤 | 1. 观察"当前拼接顺序："标签与拼接预览文本的位置关系 |
| 预期结果 | 标签在预览文本正上方，两者左对齐，呈纵向排列（由 `.concat-preview-wrapper` flex-direction: column 实现） |

---

### 需求 2c：取消顺序确认弹窗

#### TC-2c.1 完成按钮直接保存

| 项目 | 内容 |
|------|------|
| 前置条件 | 映射关系管理对话框已打开，已设置含拼接字段的映射 |
| 测试步骤 | 1. 为某字段选择"需要拼接字段"并勾选多个字段<br>2. 点击"完成"按钮 |
| 预期结果 | 直接保存映射结果，不弹出"多选字段顺序确认"对话框 |

#### TC-2c.2 拼接字段顺序正确保存

| 项目 | 内容 |
|------|------|
| 前置条件 | 映射关系管理对话框已打开 |
| 测试步骤 | 1. 为某字段选择"需要拼接字段"<br>2. 依次勾选字段 A、字段 B、字段 C<br>3. 点击"完成"保存<br>4. 重新打开映射关系管理对话框 |
| 预期结果 | 保存的 `mappedFields` 顺序为 [A, B, C]，回显时拼接预览文本顺序与保存时一致 |

#### TC-2c.3 修改拼接顺序后保存

| 项目 | 内容 |
|------|------|
| 前置条件 | 已有保存的拼接映射（顺序为 A, B, C） |
| 测试步骤 | 1. 打开映射关系管理对话框<br>2. 取消勾选字段 B，再重新勾选字段 B（顺序变为 A, C, B）<br>3. 点击"完成"保存<br>4. 重新打开验证 |
| 预期结果 | 新顺序 [A, C, B] 正确保存并回显 |

#### TC-2c.4 多个拼接映射同时保存

| 项目 | 内容 |
|------|------|
| 前置条件 | 映射关系管理对话框已打开 |
| 测试步骤 | 1. 字段 X 选择拼接，勾选 A, B<br>2. 字段 Y 选择拼接，勾选 C, D, E<br>3. 点击"完成"保存<br>4. 重新打开验证 |
| 预期结果 | 两个拼接映射各自的 `mappedFields` 顺序独立正确保存和回显 |

#### TC-2c.5 导出 Excel 拼接结果正确

| 项目 | 内容 |
|------|------|
| 前置条件 | 已保存含拼接字段的映射，已导入源文件 |
| 测试步骤 | 1. 执行导出明细功能<br>2. 打开生成的 Excel 文件<br>3. 检查拼接字段对应列的数据 |
| 预期结果 | 拼接结果按用户设定的字段顺序正确拼接，与拼接预览一致 |

#### TC-2c.6 代码清理验证

| 项目 | 内容 |
|------|------|
| 前置条件 | v1.4.3 代码已部署 |
| 测试步骤 | 1. 在 `src/renderer-dialogs.js` 中搜索 `createMappingOrderDialog`<br>2. 在 `src/styles.css` 中搜索 `mapping-order-` |
| 预期结果 | `createMappingOrderDialog` 函数已完全删除，`.mapping-order-*` 相关 CSS 规则已全部删除，无残留代码 |

---

### 需求 3：维护大账号 -- 币种区域布局调整

#### TC-3.1 默认窗口下同行显示

| 项目 | 内容 |
|------|------|
| 前置条件 | 窗口宽度为默认尺寸（约 1200px），打开维护大账号对话框 |
| 测试步骤 | 1. 观察币种编辑区域的布局 |
| 预期结果 | 币种输入框（或下拉框按钮）和"多币种"勾选框、文本在同一行水平排列，不换行 |

#### TC-3.2 较小窗口下不换行

| 项目 | 内容 |
|------|------|
| 前置条件 | 将窗口宽度调整至约 900px |
| 测试步骤 | 1. 打开维护大账号对话框<br>2. 观察币种编辑区域的布局 |
| 预期结果 | 元素仍在同一行，不换行。允许适度压缩，但不应出现异常溢出 |

#### TC-3.3 币种输入框功能正常

| 项目 | 内容 |
|------|------|
| 前置条件 | 维护大账号对话框已打开 |
| 测试步骤 | 1. 在币种输入框中输入币种代码（如 USD、CNY、EUR）<br>2. 确认文字完整显示，不被截断 |
| 预期结果 | 输入框宽度缩小后（128px），常见 3-4 字符币种代码仍可完整显示和正常使用 |

#### TC-3.4 多币种勾选功能正常

| 项目 | 内容 |
|------|------|
| 前置条件 | 维护大账号对话框已打开 |
| 测试步骤 | 1. 勾选"多币种"复选框<br>2. 取消勾选<br>3. 确认切换行为正常 |
| 预期结果 | 多币种勾选/取消勾选后，币种区域正确切换显示模式，功能不受布局调整影响 |

#### TC-3.5 大账号增删改操作不受影响

| 项目 | 内容 |
|------|------|
| 前置条件 | 维护大账号对话框已打开 |
| 测试步骤 | 1. 新增一条大账号记录<br>2. 修改币种值<br>3. 删除一条记录 |
| 预期结果 | 增删改操作正常，币种区域布局在操作过程中保持稳定 |

---

### 需求 4：映射关系间距回滚

#### TC-4.1 CSS 规则已删除

| 项目 | 内容 |
|------|------|
| 前置条件 | v1.4.3 代码已部署 |
| 测试步骤 | 1. 在 `src/styles.css` 中搜索 `.mapping-card .data-table td:first-child`<br>2. 搜索 `.mapping-card .data-table th:first-child` |
| 预期结果 | 上述 CSS 规则已不存在（v1.4.2 新增的规则已删除） |

#### TC-4.2 模板字段列宽度恢复

| 项目 | 内容 |
|------|------|
| 前置条件 | 已导入源文件，打开映射关系管理对话框 |
| 测试步骤 | 1. 观察模板字段列（左列）的宽度<br>2. 通过 DevTools 检查 td:first-child 无 `width: 1%` 和 `white-space: nowrap` 约束 |
| 预期结果 | 模板字段列宽度为表格自动分配，不再被强制压缩为最小宽度。两列间距视觉上明显大于 v1.4.2 |

#### TC-4.3 映射关系功能不受影响

| 项目 | 内容 |
|------|------|
| 前置条件 | 映射关系管理对话框已打开 |
| 测试步骤 | 1. 选择映射字段<br>2. 保存映射<br>3. 重新打开验证回显 |
| 预期结果 | 间距回滚不影响映射关系的选择、保存、回显功能 |

#### TC-4.4 其他对话框不受影响

| 项目 | 内容 |
|------|------|
| 前置条件 | v1.4.3 已部署 |
| 测试步骤 | 1. 打开账户映射对话框，检查表格布局<br>2. 打开维护大账号对话框，检查表格布局 |
| 预期结果 | 其他对话框的表格间距和布局未受影响（`.account-card .data-table`、`.manager-card .data-table` 不受此变更影响） |

---

### 需求 5：新开账户多币种按钮修复与改造

#### TC-5.1 Bug 回归：多币种按钮可点击

| 项目 | 内容 |
|------|------|
| 前置条件 | 新开账户表单页面 |
| 测试步骤 | 1. 勾选"多币种账户"复选框<br>2. 点击币种下拉框按钮 |
| 预期结果 | 按钮可正常点击响应，展开多选面板（修复前点击无响应的 Bug 已解决） |

#### TC-5.2 单币种/多币种模式切换

| 项目 | 内容 |
|------|------|
| 前置条件 | 新开账户表单页面 |
| 测试步骤 | 1. 默认状态下确认为文本输入框（单币种模式）<br>2. 勾选"多币种账户"<br>3. 确认切换为下拉框按钮<br>4. 取消勾选"多币种账户"<br>5. 确认恢复为文本输入框 |
| 预期结果 | 模式切换流畅，币种输入框和下拉框按钮正确显隐 |

#### TC-5.3 多选面板展开/关闭

| 项目 | 内容 |
|------|------|
| 前置条件 | 已勾选"多币种账户"，显示下拉框按钮 |
| 测试步骤 | 1. 点击下拉框按钮展开面板<br>2. 再次点击下拉框按钮关闭面板 |
| 预期结果 | 面板正常展开和关闭，按钮状态（`.is-open` class、`aria-expanded`）正确切换 |

#### TC-5.4 币种勾选与序号显示

| 项目 | 内容 |
|------|------|
| 前置条件 | 多选面板已展开 |
| 测试步骤 | 1. 勾选第一个币种（如 USD），观察序号<br>2. 勾选第二个币种（如 CNY），观察序号<br>3. 勾选第三个币种（如 EUR），观察序号 |
| 预期结果 | 每次勾选后，对应选项旁显示序号（1、2、3），序号反映勾选顺序。序号样式复用 `.concat-picker-index`（主色调、粗体） |

#### TC-5.5 取消勾选后序号重排

| 项目 | 内容 |
|------|------|
| 前置条件 | 已勾选 USD(1)、CNY(2)、EUR(3) |
| 测试步骤 | 1. 取消勾选 CNY（序号 2） |
| 预期结果 | USD 序号保持 1，EUR 序号自动变为 2。序号连续无间断 |

#### TC-5.6 勾选状态在重新渲染后保持

| 项目 | 内容 |
|------|------|
| 前置条件 | 多选面板已展开，已勾选若干币种 |
| 测试步骤 | 1. 勾选/取消勾选一个币种（触发 `renderNewAccountCurrencyOptions` 重新渲染）<br>2. 检查其他币种的勾选状态 |
| 预期结果 | 重新渲染后，其他币种的 checkbox 勾选状态正确保持，与 `selectedCurrencies` 数组一致 |

#### TC-5.7 下拉按钮摘要文本

| 项目 | 内容 |
|------|------|
| 前置条件 | 已勾选"多币种账户" |
| 测试步骤 | 1. 未选择任何币种，检查按钮文本<br>2. 选择 1 个币种（USD），检查按钮文本<br>3. 选择 2 个币种（USD, CNY），检查按钮文本<br>4. 选择 3 个及以上币种，检查按钮文本 |
| 预期结果 | 0 个已选：显示空白或占位文本<br>1-2 个已选：显示币种代码（如 "USD" 或 "USD, CNY"）<br>3 个及以上：显示摘要（如 "3个币种"） |

#### TC-5.8 点击面板外部关闭

| 项目 | 内容 |
|------|------|
| 前置条件 | 多选面板已展开 |
| 测试步骤 | 1. 点击面板外部区域 |
| 预期结果 | 面板关闭，已勾选状态保留 |

#### TC-5.9 点击面板内部不关闭

| 项目 | 内容 |
|------|------|
| 前置条件 | 多选面板已展开 |
| 测试步骤 | 1. 点击面板内的 checkbox<br>2. 点击面板内的文字区域 |
| 预期结果 | 面板保持展开状态 |

#### TC-5.10 快速连续点击

| 项目 | 内容 |
|------|------|
| 前置条件 | 已勾选"多币种账户" |
| 测试步骤 | 1. 快速连续点击下拉按钮（展开-关闭-展开）<br>2. 快速连续勾选/取消勾选多个币种 |
| 预期结果 | 面板状态正确，无竞态问题。序号正确显示，无闪烁或错乱 |

#### TC-5.11 取消多币种后数据清理

| 项目 | 内容 |
|------|------|
| 前置条件 | 多币种模式下已选择若干币种 |
| 测试步骤 | 1. 取消勾选"多币种账户"<br>2. 确认切换回文本输入框<br>3. 重新勾选"多币种账户" |
| 预期结果 | 切换回单币种模式后恢复为文本输入框，已选币种被清空（`selectedCurrencies` 置空）。重新勾选多币种时，面板无已选币种，下拉按钮显示空白/占位文本 |

#### TC-5.12 新增行（clone 行）多币种功能

| 项目 | 内容 |
|------|------|
| 前置条件 | 新开账户表单，已有一行数据 |
| 测试步骤 | 1. 点击"新增"添加新行<br>2. 在新行勾选"多币种账户"<br>3. 点击下拉按钮展开面板<br>4. 勾选币种，验证序号显示<br>5. 验证新行与第一行的币种选择互不影响 |
| 预期结果 | 新增行的多币种功能完全正常，各行币种选择独立，互不干扰 |

#### TC-5.13 多币种数据传递至生成流程

| 项目 | 内容 |
|------|------|
| 前置条件 | 多币种模式下已选择币种并填写完整表单 |
| 测试步骤 | 1. 点击生成/导出<br>2. 检查生成的结果 |
| 预期结果 | `selectedCurrencies` 数组正确传递，顺序与用户勾选顺序一致，生成的网银账单币种数据正确 |

#### TC-5.14 面板滚动位置（边界场景）

| 项目 | 内容 |
|------|------|
| 前置条件 | 币种选项较多，面板出现滚动条 |
| 测试步骤 | 1. 滚动到面板底部<br>2. 勾选一个底部的币种选项（触发重新渲染） |
| 预期结果 | 观察面板滚动位置是否合理（如重置到顶部或保持当前位置均可，记录实际行为作为基线） |

---

### 需求 6：新增按钮位置修正

#### TC-6.1 按钮位置正确

| 项目 | 内容 |
|------|------|
| 前置条件 | 新开账户表单页面 |
| 测试步骤 | 1. 观察"银行账号"标签文本和"新增"按钮的位置关系 |
| 预期结果 | "新增"按钮显示在"银行账号"标签文本的右侧，不在输入框右侧。两者在同一行，垂直居中对齐 |

#### TC-6.2 按钮不在输入框旁

| 项目 | 内容 |
|------|------|
| 前置条件 | 新开账户表单页面 |
| 测试步骤 | 1. 通过 DevTools 确认"新增"按钮的 DOM 位置 |
| 预期结果 | 按钮位于 `<span class="new-account-label new-account-label-with-action">` 内部，不在 `.new-account-bank-account-row` 内 |

#### TC-6.3 点击"新增"功能正常

| 项目 | 内容 |
|------|------|
| 前置条件 | 新开账户表单，仅一行 |
| 测试步骤 | 1. 点击"新增"按钮<br>2. 确认新行被正确添加 |
| 预期结果 | 点击"新增"后新行添加成功，且点击"新增"按钮不会导致银行账号输入框意外聚焦（label 行为不受影响） |

#### TC-6.4 仅第一行显示"新增"按钮

| 项目 | 内容 |
|------|------|
| 前置条件 | 新开账户表单，已有多行 |
| 测试步骤 | 1. 检查第一行是否显示"新增"按钮<br>2. 检查第二行及后续行是否隐藏"新增"按钮 |
| 预期结果 | 仅第一行显示"新增"按钮，其余行按钮 hidden |

#### TC-6.5 新增行布局正确

| 项目 | 内容 |
|------|------|
| 前置条件 | 点击"新增"添加了多行 |
| 测试步骤 | 1. 检查每一行的布局<br>2. 确认新增行无多余的可见"新增"按钮<br>3. 确认标签行和输入行布局正确 |
| 预期结果 | 新增行结构正确，标签行布局正常，无多余按钮显示 |

#### TC-6.6 删除行后按钮显隐逻辑

| 项目 | 内容 |
|------|------|
| 前置条件 | 新开账户表单有 3 行 |
| 测试步骤 | 1. 删除第一行<br>2. 检查新的第一行是否显示"新增"按钮 |
| 预期结果 | 删除行后，`syncNewAccountAddButtonVisibility` 正确执行，新的第一行显示"新增"按钮 |

---

### 回归测试

#### TC-R.1 映射关系保存/加载完整流程

| 项目 | 内容 |
|------|------|
| 关联需求 | 需求 1、2 |
| 测试步骤 | 1. 打开映射关系管理<br>2. 设置普通映射、拼接映射各若干<br>3. 保存<br>4. 关闭后重新打开<br>5. 验证所有映射回显正确 |
| 预期结果 | 所有映射数据保存和加载正确 |

#### TC-R.2 导出明细功能

| 项目 | 内容 |
|------|------|
| 关联需求 | 需求 1、2 |
| 测试步骤 | 1. 设置映射关系（含拼接字段）<br>2. 导出明细<br>3. 检查 Excel 文件中映射列的数据 |
| 预期结果 | 导出数据与映射配置一致，拼接字段结果正确 |

#### TC-R.3 模板切换后映射重载

| 项目 | 内容 |
|------|------|
| 关联需求 | 需求 1 |
| 测试步骤 | 1. 在模板 A 设置并保存映射<br>2. 切换到模板 B<br>3. 切换回模板 A<br>4. 打开映射关系管理验证回显 |
| 预期结果 | 模板切换后映射正确重载，各模板映射配置独立 |

#### TC-R.4 新开账户完整流程（单币种）

| 项目 | 内容 |
|------|------|
| 关联需求 | 需求 5、6 |
| 测试步骤 | 1. 在新开账户表单填写完整信息（单币种模式）<br>2. 点击生成<br>3. 验证生成结果 |
| 预期结果 | 单币种模式的完整流程不受需求 5、6 的改动影响 |

#### TC-R.5 新开账户完整流程（多币种）

| 项目 | 内容 |
|------|------|
| 关联需求 | 需求 5 |
| 测试步骤 | 1. 勾选多币种，选择若干币种<br>2. 填写其他必填信息<br>3. 点击生成<br>4. 验证生成结果中币种数据 |
| 预期结果 | 多币种模式完整流程正常，生成的网银账单包含正确的有序币种数据 |

#### TC-R.6 多行表单独立性

| 项目 | 内容 |
|------|------|
| 关联需求 | 需求 5、6 |
| 测试步骤 | 1. 新增多行表单<br>2. 第一行设为单币种，第二行设为多币种<br>3. 分别填写数据<br>4. 验证各行数据独立 |
| 预期结果 | 各行表单数据相互独立，币种模式切换不影响其他行 |

#### TC-R.7 维护大账号增删改完整流程

| 项目 | 内容 |
|------|------|
| 关联需求 | 需求 3 |
| 测试步骤 | 1. 打开维护大账号对话框<br>2. 新增大账号，设置 MerchantId 和币种<br>3. 修改已有大账号的币种<br>4. 切换单币种/多币种模式<br>5. 删除一条记录<br>6. 保存后重新打开验证 |
| 预期结果 | 大账号的增删改查功能正常，布局调整不影响数据操作 |

#### TC-R.8 其他对话框布局无影响

| 项目 | 内容 |
|------|------|
| 关联需求 | 需求 4（CSS 删除） |
| 测试步骤 | 1. 打开账户映射对话框，检查布局<br>2. 打开维护大账号对话框，检查布局<br>3. 打开其他对话框（如有），检查布局 |
| 预期结果 | 所有非映射关系管理对话框的布局均不受需求 4 间距回滚影响 |

---

## 三、测试环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | macOS / Windows（应用支持的平台） |
| 窗口尺寸 | 默认尺寸（约 1200px）+ 最小尺寸（约 900px） |
| 测试数据 | 需准备 v1.4.2 版本保存的旧映射配置数据（含多选非拼接映射） |
| 调试工具 | Chrome DevTools（检查 DOM 结构、CSS 样式、控制台日志） |

---

## 四、测试优先级执行顺序

| 优先级 | 测试范围 | 说明 |
|--------|----------|------|
| 第一轮 | TC-5.1 ~ TC-5.13 | P0 Bug 修复与多币种改造（最高优先级） |
| 第二轮 | TC-1.1 ~ TC-1.8、TC-2c.1 ~ TC-2c.6 | P1 映射字段改单选 + 删除弹窗（功能核心改动） |
| 第三轮 | TC-2a.1 ~ TC-2a.7、TC-2b.1 ~ TC-2b.4、TC-4.1 ~ TC-4.4 | P1 拼接交互优化 + 间距回滚 |
| 第四轮 | TC-3.1 ~ TC-3.5、TC-6.1 ~ TC-6.6 | P2 布局调整 |
| 第五轮 | TC-R.1 ~ TC-R.8 | 回归测试 |
