# 测试用例文档 - v1.4.2

## 文档信息

| 项目 | 内容 |
|------|------|
| 版本号 | v1.4.2 |
| 日期 | 2026-03-23 |
| 基于文档 | PRD-v1.4.2.md、TechDoc-v1.4.2.md |
| 测试用例总数 | 76 |

---

## 一、需求一：账号映射对话框优化

### 1.1 表头文案修正

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC1-01 | 表头显示"大账号ID" | 已打开应用，存在至少一个模板 | 1. 点击"账号映射"按钮<br>2. 观察对话框表头 | 右侧列表头显示"清结算系统大账号ID"，而非"大账户ID" | AC1-1 |

### 1.2 勾选框显示与交互

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC1-02 | 每行显示勾选框 | 打开账号映射对话框，至少有 2 行 | 观察每行"清结算系统大账号ID"输入框右侧 | 每行右侧均有"有账户号无币种"勾选框 | AC1-2 |
| TC1-03 | 默认状态未勾选 | 打开账号映射对话框（新建行或无历史数据） | 观察勾选框默认状态和币种输入框 | 勾选框未勾选，币种输入框不可见 | AC1-3 |
| TC1-04 | 勾选后弹出币种输入框 | 打开账号映射对话框 | 勾选某行的"有账户号无币种" | 勾选框右侧出现币种输入框，可进行文本输入 | AC1-4 |
| TC1-05 | 取消勾选隐藏并清空 | 已勾选"有账户号无币种"，币种输入框中已输入"CNY" | 取消勾选 | 币种输入框隐藏，已输入的"CNY"被清空 | AC1-7 |

### 1.3 币种输入框自动补全

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC1-06 | 自动补全提示 | 已勾选"有账户号无币种"，币种输入框可见 | 在币种输入框中输入"CN" | 出现灰色幽灵文字补全建议"CNY" | AC1-5 |
| TC1-07 | 右方向键接受补全 | 币种输入框中已输入"CN"，出现补全建议"CNY" | 按右方向键 | 输入框值变为"CNY"，补全建议消失 | AC1-6 |
| TC1-08 | 输入不匹配时无补全 | 已勾选"有账户号无币种"，币种输入框可见 | 在币种输入框中输入"ZZ" | 无自动补全建议（或补全建议为空） | AC1-5 |

### 1.4 保存与回显

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC1-09 | 正常保存 | 已填写银行账号ID和清结算大账号ID，勾选"有账户号无币种"并输入"HKD" | 点击"完成" | 保存成功，无报错 | AC1-8 |
| TC1-10 | 回显验证 | TC1-09 完成后 | 重新打开账号映射对话框 | 之前的行：勾选框为已勾选状态，币种输入框可见且值为"HKD" | AC1-9 |
| TC1-11 | 未勾选行不受影响 | 对话框中有两行，第一行勾选并输入币种，第二行未勾选 | 点击"完成"后重新打开 | 第一行正确回显勾选和币种；第二行勾选框未勾选，无币种输入框 | AC1-8, AC1-9 |

### 1.5 新增行

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC1-21 | 新增行也有勾选框 | 打开账号映射对话框 | 点击"新增"添加一行 | 新行也有"有账户号无币种"勾选框，默认未勾选，币种输入框不可见 | AC1-2, AC1-3 |

### 1.6 输入校验

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC1-12 | 勾选但未填币种 | 已勾选"有账户号无币种"，币种输入框为空 | 点击"完成" | 提示"请填写币种"，不保存 | AC1-10 |
| TC1-13 | 格式无效-纯数字 | 已勾选"有账户号无币种"，币种输入框输入"123" | 点击"完成" | 提示"币种代码无效" | AC1-11 |
| TC1-14 | 格式无效-过短 | 已勾选"有账户号无币种"，币种输入框输入"AB" | 点击"完成" | 提示"币种代码无效" | AC1-11 |
| TC1-15 | 格式无效-过长 | 已勾选"有账户号无币种"，币种输入框输入"ABCDEF" | 点击"完成" | 提示"币种代码无效" | AC1-11 |
| TC1-16 | 格式无效-特殊字符 | 已勾选"有账户号无币种"，币种输入框输入"CN!" | 点击"完成" | 提示"币种代码无效" | AC1-11 |
| TC1-17 | 格式有效但非常见币种 | 已勾选"有账户号无币种"，币种输入框输入"XYZ" | 点击"完成" | 保存成功（格式校验通过，不做白名单校验） | AC1-11 |

### 1.7 币种回填（数据处理）

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC1-18 | 币种回填生效 | 1. 账号映射中某银行账号勾选"有账户号无币种"，币种设为"HKD"<br>2. 源文件中该银行账号对应行无币种数据 | 导入文件并导出明细 | 导出 Excel 中该行的 Currency 单元格值为"HKD" | AC1-8 |
| TC1-19 | 源文件有币种时不回填 | 1. 同 TC1-18 的映射配置<br>2. 源文件中该行已有币种值"USD" | 导入文件并导出明细 | 导出 Excel 中该行 Currency 为"USD"（源文件值优先，不被覆盖） | AC1-8 |
| TC1-22 | 未勾选时不回填 | 1. 账号映射中某银行账号未勾选"有账户号无币种"<br>2. 源文件中该行无币种数据 | 导入文件并导出明细 | 导出 Excel 中该行 Currency 为空（不回填） | AC1-8 |
| TC1-23 | 源文件币种为空白字符串时回填 | 1. 账号映射中某银行账号勾选"有账户号无币种"，币种设为"HKD"<br>2. 源文件中该行币种列值为空白字符串（空格） | 导入文件并导出明细 | 导出 Excel 中该行 Currency 为"HKD"（`normalizeCell` 将空白视为空，触发回填） | AC1-8 |

### 1.8 数据迁移兼容

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC1-20 | v1.4.1 旧数据加载 | 使用 v1.4.1 版本保存过账号映射数据 | 升级到 v1.4.2 后打开账号映射对话框 | 所有行的勾选框默认未勾选，币种输入框不可见，功能正常 | NFR-2 |

---

## 二、需求二：导出文件命名规则变更

### 2.1 正常流程

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC2-01 | 明细文件名后四位 | 已导入文件，账户号为"1234567890" | 点击"导出明细" | 导出文件名格式为 `{模板名}-7890-DETAIL-{日期范围}.xlsx` | AC2-1 |
| TC2-02 | 余额文件名后四位 | 已导入文件，账户号为"1234567890" | 点击"导出余额" | 导出文件名格式为 `{模板名}-7890-BALANCE-{日期范围}.xlsx` | AC2-2 |
| TC2-03 | 文件内容不受影响 | 同 TC2-01 | 打开导出的 Excel 文件 | MerchantId 对应的单元格仍显示完整账户号"1234567890" | AC2-5 |

### 2.2 边界条件

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC2-04 | 账户号恰好4位 | 账户号为"7890" | 导出明细 | 文件名中账户号为"7890"（原样输出） | AC2-3 |
| TC2-05 | 账户号不足4位 | 账户号为"89" | 导出明细 | 文件名中账户号为"89"（原样输出） | AC2-3 |
| TC2-06 | 账户号为空 | 账户号为空字符串 | 导出明细 | 文件名中不包含账户号段，格式为 `{模板名}-DETAIL-{日期范围}.xlsx` | AC2-4 |
| TC2-07 | 账户号5位 | 账户号为"12345" | 导出明细 | 文件名中账户号为"2345"（取后四位） | AC2-1 |

---

## 三、需求三：币种输入框简化

### 3.1 新开账户模块

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC3-01 | 下拉按钮已移除 | 打开新开账户模块 | 观察币种输入区域 | 无下拉按钮（三角形图标不存在） | AC3-1 |
| TC3-02 | 下拉面板已移除 | 打开新开账户模块 | 点击币种输入框区域 | 不出现下拉选项面板 | AC3-2 |
| TC3-03 | 自动补全正常 | 打开新开账户模块 | 在币种输入框中输入"US" | 出现灰色幽灵文字补全建议"USD" | AC3-4 |
| TC3-04 | 右方向键接受补全 | 币种输入框中出现补全建议"USD" | 按右方向键 | 输入框值变为"USD" | AC3-5 |

### 3.2 大账号/币种选择对话框

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC3-05 | 选择对话框币种无下拉 | 触发大账号/币种选择对话框（多大账号模板导入文件） | 观察币种输入区域 | 币种列为文本输入框+自动补全，无下拉按钮和面板 | AC3-3 |

### 3.3 大账号管理对话框

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC3-08 | 管理对话框币种无下拉 | 打开映射管理，MerchantId 选择"自己输入" | 点击"维护大账号"按钮，观察币种输入区域 | 币种输入区域为文本输入框+自动补全，无下拉按钮和面板 | AC3-3 |

### 3.5 多币种模式不受影响

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC3-06 | 多币种模式保留 | 新开账户模块 | 勾选多币种复选框 | 多币种模式不受影响，勾选后仍保留现有交互方式（下拉面板选择多个币种） | -- |

### 3.6 布局验证

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC3-07 | 输入框布局正常 | 打开新开账户模块 | 观察币种输入框的宽度和布局 | 去掉下拉框后，输入框宽度和周围元素布局无异常、无错位 | AC3-6 |

---

## 四、需求四：MerchantId 自己输入模式增强

### 4.1 场景 A：多文件导入

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC4-01 | 多文件弹出选择框 | 1. 模板的 MerchantId 映射为"自己输入"<br>2. 已维护大账号列表 | 一次导入 3 个文件 | 弹出大账号/币种选择对话框，列出 3 行，每行显示文件名 | AC4-1 |
| TC4-02 | 按文件名排序 | 同 TC4-01，导入文件名为 c.xlsx、a.xlsx、b.xlsx | 观察对话框行顺序 | 按文件名字母序排列：a.xlsx、b.xlsx、c.xlsx | AC4-2 |
| TC4-03 | 大账号下拉+币种文本输入 | TC4-01 弹出的对话框 | 观察每行控件类型 | 大账号为下拉选择框，币种为文本输入框+自动补全 | AC4-3 |
| TC4-04 | 币种自动补全按大账号过滤 | TC4-01 弹出的对话框 | 选择某大账号后，在币种输入框输入字符 | 自动补全建议仅显示该大账号关联的币种列表中的币种 | AC4-4 |
| TC4-05 | 单币种自动填充 | 对话框中某大账号仅关联 1 个币种 | 选择该大账号 | 币种自动填充为该币种，且不可编辑 | AC4-5 |

### 4.2 场景 B：多行模板字段

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC4-06 | 多行弹出选择框 | 1. MerchantId 为"自己输入"<br>2. 已维护大账号列表<br>3. 源文件解析后有 3 行数据行 | 导入该文件 | 弹出大账号/币种选择对话框，列出 3 行 | AC4-7 |
| TC4-07 | 行标签显示源文件行号 | TC4-06 弹出的对话框 | 观察每行标签 | 显示"第X行的账号为："格式，行号对应源文件中的实际行号 | AC4-8 |

### 4.3 场景 A+B 交叉

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC4-08 | 多文件+多行综合对话框 | 1. MerchantId 为"自己输入"<br>2. 导入 2 个文件，文件 A 有 2 行数据，文件 B 有 3 行数据 | 导入这 2 个文件 | 弹出一个综合对话框，按文件+行号统一列出 5 行选择项 | AC4-11 |

### 4.4 完成与取消

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC4-09 | 未全部选择时提示错误 | TC4-01 弹出的对话框，仅为第 1 行选择了大账号和币种 | 点击"完成" | 提示"请先为每一行选择大账号和币种" | AC4-6 |
| TC4-10 | 全部选择后完成 | TC4-01 弹出的对话框，为所有行选择了大账号和币种 | 点击"完成" | 对话框关闭，系统使用对应的大账号/币种处理各文件 | AC4-9 |
| TC4-11 | 固定开关功能 | TC4-01 弹出的对话框 | 开启"固定"开关后完成选择，再次导入文件 | 之前的选择被记住并复用 | AC4-10 |
| TC4-12 | 关闭对话框取消导入 | TC4-01 弹出的对话框 | 点击 [x] 关闭按钮 | 取消本次导入操作，不进行处理 | AC4-12 |
| TC4-15 | 选择后导出数据正确性 | 1. 多文件导入弹出选择框<br>2. 文件 A 选大账号 X/币种 CNY<br>3. 文件 B 选大账号 Y/币种 HKD | 完成选择后导出明细 | 文件 A 的导出数据使用大账号 X 和币种 CNY；文件 B 的导出数据使用大账号 Y 和币种 HKD | AC4-9 |

### 4.5 异常场景

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC4-13 | 大账号列表为空 | 1. MerchantId 为"自己输入"<br>2. 模板未维护大账号列表 | 导入多个文件 | 提示"请先在映射管理中维护大账号列表"，中止导入 | -- |
| TC4-14 | 单文件单行不弹框 | 1. MerchantId 为"自己输入"<br>2. 已维护大账号列表<br>3. 单文件且仅 1 行数据 | 导入该文件 | 不弹出选择对话框，自动使用已有大账号 | -- |
| TC4-16 | 单文件单行+大账号为空 | 1. MerchantId 为"自己输入"<br>2. 模板未维护大账号列表<br>3. 单文件且仅 1 行数据 | 导入该文件 | 提示"请先在映射管理中维护大账号列表"，中止导入 | -- |

---

## 五、需求五：映射字段新增"需要拼接字段"

### 5.1 选项可见性

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC5-01 | 下拉框包含拼接选项 | 已配置模板且模板有表头，打开映射关系管理对话框 | 展开某非高级字段行的映射字段下拉框 | 选项列表中包含"需要拼接字段" | AC5-1 |
| TC5-02 | Balance字段无拼接选项 | 打开映射关系管理对话框 | 展开 Balance 相关行的映射字段下拉框 | 选项列表中不包含"需要拼接字段" | AC5-1 |
| TC5-03 | MerchantId行无拼接选项 | 打开映射关系管理对话框 | 展开 MerchantId 行的映射字段下拉框 | 选项列表中不包含"需要拼接字段" | AC5-1 |
| TC5-22 | 高级映射字段行无拼接选项 | 打开映射关系管理对话框 | 展开高级映射字段行（如"按正负号拆分的发生额"）的下拉框 | 选项列表中不包含"需要拼接字段" | AC5-1 |

### 5.2 拼接字段选择面板

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC5-04 | 选定后显示多选框 | 映射关系管理对话框已打开 | 为某行选择"需要拼接字段" | 该行右侧出现下拉式多选框（按钮样式） | AC5-2 |
| TC5-05 | 未展开时的样式 | TC5-04 完成，多选框可见 | 观察多选框外观 | 呈现普通下拉框样式（按钮样式） | AC5-3 |
| TC5-06 | 展开面板显示选项 | TC5-04 完成 | 点击多选框按钮 | 展开面板，每个选项前有勾选框和数字序列位 | AC5-4 |
| TC5-07 | 面板仅显示文件表头 | TC5-06 展开面板 | 观察面板中的选项 | 仅包含源文件表头列名，不包含"自己输入"、"需要拼接字段"、高级映射字段等特殊选项 | AC5-4 |

### 5.3 序号排列与取消

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC5-08 | 按点击顺序标记序号 | TC5-06 展开面板 | 依次点击"账号主体"→"校验位"→"账号前缀" | 序号分别为：账号主体 1.、校验位 2.、账号前缀 3. | AC5-5 |
| TC5-09 | 取消勾选后序号重排 | TC5-08 完成（三个字段已勾选） | 取消勾选"校验位"（序号 2） | "账号主体"序号不变为 1.，"账号前缀"序号从 3. 变为 2. | AC5-6 |

### 5.4 预览显示

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC5-10 | 收起后显示预览 | TC5-08 完成，已选 3 个字段 | 点击面板外部收起下拉框 | 映射字段右侧显示拼接预览"账号主体 校验位 账号前缀"（按序排列，空格分隔） | AC5-7, AC5-8 |
| TC5-11 | 预览过长省略 | 选择了多个长字段名使预览总长度超过 40 字符 | 收起下拉框 | 预览文本末尾显示"......" | AC5-9 |
| TC5-12 | tooltip 显示完整内容 | TC5-11 完成，预览被省略 | 鼠标悬停在预览文本上 | tooltip 显示所有已选字段的完整名称 | AC5-10 |

### 5.5 保存与回显

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC5-13 | 保存后回显 | 已选择"需要拼接字段"并配置了 3 个子字段（按序），点击保存 | 重新打开映射关系管理对话框 | 该行映射字段显示"需要拼接字段"，拼接面板正确回显已选字段和顺序 | AC5-11 |

### 5.6 切换行为

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC5-14 | 切换回普通值清空配置 | 已选"需要拼接字段"并配置了子字段 | 将映射字段切换回普通值（如"交易日期"） | 拼接配置自动清空，拼接预览消失，多选框隐藏 | AC5-14 |
| TC5-21 | 未选子字段时保存报错 | 已选"需要拼接字段"但未在面板中勾选任何子字段 | 点击保存 | 提示"请选择至少一个拼接字段"，不允许保存 | AC5-2 |

### 5.7 导出数据拼接

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC5-15 | 按序拼接无分隔符 | 映射配置：AccountNo → 需要拼接字段，子字段按序为["姓","名"]<br>源文件：姓="张", 名="三" | 导入文件并导出明细 | 导出 Excel 中 AccountNo 单元格值为"张三"（直接连接，无分隔符） | AC5-12 |
| TC5-16 | 空值处理 | 映射配置同 TC5-15<br>源文件：姓="张", 名="" | 导入文件并导出明细 | 导出 Excel 中 AccountNo 单元格值为"张"（空值被过滤，不影响其他列） | AC5-13 |
| TC5-17 | 三字段拼接顺序验证 | 子字段按序为["前缀","主体","后缀"]<br>源文件：前缀="A", 主体="123", 后缀="X" | 导入文件并导出明细 | 导出值为"A123X" | AC5-12 |

### 5.8 多行独立性

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC5-18 | 多行拼接配置互不干扰 | 映射对话框中两个不同模板字段都选择"需要拼接字段" | 分别为两行配置不同的子字段和顺序 | 两行各自独立保存和回显，互不影响 | AC5-11 |

### 5.9 与现有多选并存

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC5-19 | 普通多选仍弹排序确认 | 映射对话框中某行通过 Ctrl+点击选择了多个普通字段 | 保存映射 | 弹出排序确认对话框（`createMappingOrderDialog`） | -- |
| TC5-20 | 拼接字段不弹排序确认 | 映射对话框中某行选择"需要拼接字段"并配置了子字段 | 保存映射 | 不弹出排序确认对话框，直接保存成功。重新打开后，拼接面板中子字段顺序与保存时一致 | -- |

---

## 六、需求六：映射关系管理间距调整

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC6-01 | 间距缩短 | 打开映射关系管理对话框 | 观察模板字段列和映射字段列之间的间距 | 模板字段列宽自适应内容，不再占用固定宽比例，两列间距明显缩短 | AC6-1 |
| TC6-02 | 短字段名无截断 | 映射对话框中存在短字段名（如"Date"） | 观察模板字段列 | 内容完整显示，无截断 | AC6-2 |
| TC6-03 | 长字段名无截断 | 映射对话框中存在长字段名（如"TransactionDescription"） | 观察模板字段列 | 内容完整显示，无截断、无溢出 | AC6-2 |
| TC6-04 | 整体布局协调 | 打开映射关系管理对话框 | 观察表格整体布局 | 模板字段列和映射字段列布局协调，不影响映射字段下拉框、拼接预览等其他元素的显示 | AC6-3 |

---

## 七、非功能需求与回归测试

### 7.1 数据兼容性

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC7-01 | 旧模板数据加载 | 使用 v1.4.1 保存的模板和映射数据 | 升级到 v1.4.2 后打开模板 | 模板正常加载，所有映射数据完整，功能正常 | NFR-1 |
| TC7-02 | 旧映射数据无拼接字段 | v1.4.1 保存的映射中无"需要拼接字段"类型 | 升级后打开映射管理 | 所有映射正常回显，"需要拼接字段"选项可见但未被选中 | NFR-1 |
| TC7-03 | 含拼接字段的模板在旧版本打开 | v1.4.2 中创建了含"需要拼接字段"映射的模板，导出为 JSON bundle | 在 v1.4.1 客户端导入该 bundle | 不报错，"需要拼接字段"的映射被当作普通多选处理，拼接结果与预期一致 | NFR-5 |

### 7.2 回归测试要点

| 用例ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 关联AC |
|--------|------|---------|---------|---------|--------|
| TC7-04 | 现有账号映射功能不受影响 | 使用不含新功能的普通账号映射 | 完整流程：添加映射→保存→重新打开→验证回显 | 功能正常，与 v1.4.1 行为一致 | -- |
| TC7-05 | 现有导出功能不受影响 | 使用普通映射（非拼接字段） | 导入文件→导出明细和余额 | 导出文件内容正确，仅文件名中账户号变为后四位 | -- |
| TC7-06 | Drawee/Payee 映射兼容 | 已配置账号映射（含 Drawee/Payee 相关字段映射） | 导入文件并导出 | 导出 Excel 中 Drawee/Payee 字段正确映射为清结算账号（`clearingAccountId`），而非显示 `[object Object]` | -- |
| TC7-07 | 余额账单不受拼接影响 | Balance 字段使用正常映射 | 导出余额文件 | 余额文件正常生成，Balance 字段不支持拼接操作 | -- |
| TC7-08 | 新开账户多币种模式 | 新开账户模块勾选多币种复选框 | 使用多币种下拉面板选择多个币种 | 多币种模式功能正常，不受需求三的单币种下拉移除影响 | -- |

---

## 附录：测试用例统计

| 需求 | 用例数 | 正常流程 | 边界条件 | 异常场景 | 兼容性/回归 |
|------|--------|---------|---------|---------|------------|
| 需求一 | 23 | 10 | 4 | 6 | 3 |
| 需求二 | 7 | 3 | 4 | 0 | 0 |
| 需求三 | 8 | 6 | 1 | 0 | 1 |
| 需求四 | 16 | 8 | 1 | 3 | 4 |
| 需求五 | 22 | 10 | 2 | 2 | 8 |
| 需求六 | 4 | 1 | 2 | 0 | 1 |
| 非功能/回归 | 8 | 0 | 0 | 0 | 8 |
| **合计** | **76** (去重) | **38** | **14** | **11** | **13** |

注：部分用例同时覆盖多个分类，统计按主要分类归入。
